# 生产部署记录（2026-09-11）

状态：成功。应用版本 `/opt/bacoach/releases/20260911T111735Z`，对应代码提交 `11db250f6fc7866cb4d3db2da79346bf294afcbf`。

## 发布方式

- 从已推送并验收的 Git 提交生成发布包；未携带本机环境文件、私钥或测试数据库。
- 在独立版本目录安装前端依赖并完成 Next.js 生产构建。
- 切换前运行隔离 V2 API 冒烟，7 个业务场景通过。
- 原子切换 `/opt/bacoach/current`，依次重启后端和前端；脚本在切换失败时自动恢复上一版本。
- 本次只发布应用，没有运行 DDL、数据库迁移或知识库替换。

上一版本 `/opt/bacoach/releases/20260910T154355Z` 保留，可用于受控回退。上线后已有用户写入时，回退应用仍需先评估新旧代码与写入格式，不能把数据库恢复到旧快照。

## 上线验收

- nginx、bacoach-backend、bacoach-frontend：active。
- 内部 `/health`：通过；DeepSeek、Doubao 配置可用。
- 公网首页：HTTP 200。
- 公网 `/api/modules`：HTTP 200，返回四个模块。
- 公网未登录 `/api/auth/me`：HTTP 401，符合认证预期。
- 发布后十分钟内，前后端 systemd error 等级日志：0 行。
- V2 只读核验：23 份档案和 16 个账号全部读取成功，34 段聊天缺失运行态为 0；核验脚本没有写入数据。
- 中介配置检查：已启用、Prompt 可读取；线上代码包含失败关闭和回复状态校验逻辑。

## 已上线的重要行为

- 中介超时、服务异常、格式错误或无效片段 ID 时，不把原始召回知识交给模块。
- M2 草稿在跳转前生成；信息不完整时不能正式确认。
- 聊天里的口头同意不冒充网页确认。回复生成前、发送前均读取权威运行状态；提前宣称保存、锁定或模块切换会被纠正。
- 流式回复经过相同状态检查，不先把错误文本显示给用户。

## 范围说明

上线检查没有创建生产测试用户、发送真实被试聊天或修改生产知识内容。真实模型端到端验收是在隔离数据库和合成内容上完成，见 `E2E_ACCEPTANCE_FIXED_20260911.md`。

生产测试工作台采集时 `evaluation_cases=0`，表示当前库尚无已导入的评测案例，不影响工作台功能本身，但需要管理员导入测试集后才能在线运行评测。
