# 共享回答 Validator 与测试工作台 UI

版本：`20260909T142523Z`。

## 交付范围

采用一个共享的程序校验器，四个模块加载各自规则。在正常模块回答生成完成后、输出给客户端之前执行。不是新增四个 LLM，也没有新增一次语义判断 LLM 调用。

调用顺序：检索 → 知识中介 → 当前模块生成 → 共享 Validator → 展示并保存。现有风险分流保持原样；危机应答和纯状态快捷路径不经过本次普通模块校验，避免拦截固定安全资源。

## 两级结果

- `blocked`：空文本、超过 50000 字符、显式 `kb:数字` 引用不在本轮交给模块的候选证据内。替换成固定的完整性检查失败提示，保留原有上游错误（如有），不自动重试付费模型调用；本轮不推进模块，不启动常规临床提取。被拦截原文及思考内容不输出。
- `review`：M1 可能过早制定行动计划、M2 宣称已确认、M3 可能替换目标、M4 宣称完成情况，以及诊断或用药相关表述。首版为窄范围文本模式提示，交由测试人员结合上下文复核，不自动删除或改写。
- `passed`：上述程序规则没有命中，**不是内容正确、安全或忠实于证据的认证**。

例如 M2 回答“我们已经确定了”，会提示核对用户是否真正同意；规则本身不理解用户有没有确认，因此既可能误报，也可能漏报。引用 ID 校验只证明编号来自候选材料，不证明回答的说法得到该材料支持；未带编号的自然语言引用不在这条规则能力内。

## 延迟与配置

校验本身不调用 LLM，记录 `duration_ms` 和 `llm_calls=0`。为了真正做到先检查后显示，正常模块的内容及思考事件先缓存在本次请求中，通过后才发布；这会推迟用户看到第一段正文的时间，即使校验计算很快。没有承诺总体等待变短。

`ANSWER_VALIDATOR_ENABLED=true` 默认启用；设为 false 并重启可回到原有输出方式。规则是服务端版本控制代码，不是可由提示词覆盖的安全合同。

遥测位于 `telemetry.answer_validator`，包含 version、module、status、findings、duration_ms、llm_calls 和 semantic_verified=false。测试工作台运行详情用独立卡片展示，旧运行不自动重评。

## 工作台界面

新增概览卡片、视图分组、独立筛选栏、状态筛选、重置筛选、清空选择、选中行高亮、加载及空结果状态。统一模型、模块、工作表和评审结论选单；仍使用原生 select 保留键盘及手机操作。对话详情支持安全 Markdown 排版，原始指标收进高级信息。

桌面显示概览，手机折叠概览并减少说明占位，为表格留空间；宽表仍横向滚动。计数明确区分用例总数与“当前已加载运行记录”，不把分页数据冒充全站统计。既有导入、运行、追问、评审和导出行为不改。

## 后续如何评估

用标注过的测试集分别统计：程序拦截率、人工复核命中率、误报率、漏报率、额外计算耗时、首段正文等待时间。当前首版没有经过专家标注集验证，不报告虚构的准确率提升。后续可在离线测试增加语义 LLM Judge，但本次未实现，也不把正则模式称作语义理解。

不新增或迁移业务数据库字段，不替换知识库，不把 Validator 当作数据库权限校验的替代。

## 验证结果

- 最终后端全量回归 422 通过、1 跳过；包括新增 14 项完整性、分模块提醒、流式拦截及非流式校验测试。原有上游失败错误和空答复处理保持兼容。
- 前端生产构建与类型检查通过；桌面/390px 手机尺寸浏览器回归覆盖新增编辑、导入确认、模块/运行状态筛选、批量运行、评审、追问、导出及 Validator 卡片。深浅主题和手机截图已检查。
- 上述为隔离测试及合成浏览器数据，不是在线真实用户质量评测，没有额外发起真实被试对话或声称临床有效性提升。
