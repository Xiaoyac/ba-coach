# 知识使用中介 LLM 与测试工作台上线报告

日期：2026-09-09。线上版本：`20260909T074652Z`，网站：https://bacoach.xyz/。

## 本次完成与入口

- 管理员登录并强制刷新后，在侧边栏打开「测试工作台」，上传 Excel/CSV、预览、确认导入，再运行用例。普通用户不开放此功能。
- 头像菜单 →「全局提示词管理」→「知识使用中介 LLM」，编辑、保存或恢复默认提示词。修改影响之后的调用；测试运行保存提示词快照，便于比较版本。
- 原始 Excel 的评测集有 41 条有效用例及 6 处问题。现在允许明确勾选只导入有效部分，默认仍阻止带错误直接确认。未擅自上传原文件或自动跳过错误行；生产用例初始数量为 0，需要管理员上传确认。

## 实现方式

主路径：用户输入 → 现有安全检查及意图门控 → 知识检索 → 中介 LLM → 当前 M1/M2/M3/M4 的回答 LLM。

中介实现在共享模块生成节点内，作为独立调用和追踪阶段 `knowledge_mediator`，不是另一个负责流程路由的 Agent。它使用现有 DeepSeek 非思考调用通道，输入当前模块、最新消息、有限近期对话、上下文及检索片段；返回结构化 JSON：`selected_ids`、`guidance`、`cautions`。

服务器校验 JSON 结构及片段 ID：只允许选取本次检索提供的证据。随后把所选原始片段和使用建议交给当前模块。它没有更改模块、确认目标或直接写业务档案的权限。管理员可以调整业务提示词，但服务端继续附加格式和权限约束。

例如，用户问「活动如何影响情绪」：中介可以建议 M1 用活动记录片段解释观察活动与情绪关系，同时提醒不要把书中的个人案例当成该用户经历、不要抢先为用户制定 M2 目标。它给的是使用建议，不是代替模块回答用户。

## 延迟、降级与监控

- 没检索到知识或关闭开关时不额外调用 LLM；安全分支及纯状态路径不调用。
- 默认最长等待 12 秒；异常、超时、无效 JSON 或非法证据 ID 时回退原有知识使用路径，不阻断回答。成功返回空 ID 列表则不注入知识片段，保留澄清建议。
- 输入知识最多 20 段、总计 16000 字符；输出预算 1200 tokens。上下文也按字段限长。
- `KNOWLEDGE_MEDIATOR_ENABLED=false` 可关闭并在重启后生效；`KNOWLEDGE_MEDIATOR_TIMEOUT_SECONDS` 调整超时。
- 追踪及遥测记录 status、reason、duration_ms、model、usage、prompt_sha256、所选 ID。正式对话利用现有 AI 执行事件表记录中介阶段；测试运行保存相关遥测及提示词快照。没有新增专门的中介监控大屏。
- 应持续比较成功率、超时/降级率、额外延迟 P50/P95、Token 成本、人工通过率及不相关知识引用率。一次冒烟测试不能证明检索质量提高。

## 实测结果

线上用真实知识库和真实 DeepSeek 模型执行一次只读中介测试：检索 3 段、选择 2 段，status=completed，耗时 **8168 ms**，模型 `deepseek-v4-pro`，输入 **1251 tokens**、输出 **263 tokens**、reasoning_tokens=0。成功返回使用建议及 4 条限制说明。这不是完整用户对话耗时，也不是具有统计意义的性能基线。

前端生产构建及类型检查通过。后端全量回归：405 通过、1 跳过、1 失败；失败为会话置顶排序测试中的 SQLite 提交异常，该项单独重跑通过。中介专项 11 项全部通过，覆盖各模块接入、自定义提示词、超时、非法 JSON/ID、关闭和空检索等；与上述失败项合并重跑为 12 项通过。不把本次全量结果表述为零失败。

## 困难和限制

1. 网页之前没有入口，是因为实现尚未部署。本次发布前后端，并且仅新增 evaluation_cases、evaluation_runs、evaluation_reviews 三张表；没有运行业务数据库迁移或替换知识库。
2. 服务器未显式关闭启动维护，因此发布预检主动停止。已用独立 systemd EnvironmentFile 设置 STARTUP_DB_MAINTENANCE=false，避免新版启动时执行通用建表或回填；原密钥及连接配置不变。
3. 旧服务长连接导致重启等待，systemd 超时终止旧进程后新版启动正常。刷新旧网页或重试被中断的请求即可。
4. Excel 的全局安全、跨模块用例及历史评分表仍不属于现有固定模块执行器能力。有效行部分导入只是先恢复可用，不代表其余六处问题已解决。

## 优缺点和边界

优势是把「找资料」和「怎么用资料」分开，建议可编辑、来源可追踪、无知识时不增加调用。缺点是多一次串行 LLM 调用，增加成本和等待，还可能错误筛掉有用材料。超时回退保证可用性，但回退时没有中介过滤保障。

结构校验只保证引用来自候选材料，不能证明建议事实正确，也不能保证下游模型一定遵守。因此它不是最终回答 validator，不能替代安全检查、人工评审或分模块离线评测。目前不能宣称精度或无关问题返回空指标已经改善。

发布保留旧版本以便回退；新版本复用旧版本的 Python 虚拟环境，清理旧发布目录前必须先迁移该依赖。新增测试表回退时应保留，不删除已产生的测试记录。
