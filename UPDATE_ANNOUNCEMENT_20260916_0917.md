# BA Coach 更新公告｜2026 年 9 月 16 日晚—9 月 17 日

本公告汇总这段时间新增、完善或完成上线的内容，不把更早已存在的功能重复算作新增。

**状态：已上线** · 网站：[bacoach.xyz](https://bacoach.xyz/) · 最新发布：`20260916T203650Z`。9 月 17 日北京时间 04:47 已完成部署后核验。已打开网页的用户建议刷新一次，以载入新版界面。

## 这次有什么变化

### 目标与执行周期

- 目标现在可区分为**主要目标**和**独立次要目标**：次要目标可以独立存在，不需要人为绑定一个“父目标”。
- 保持自然聊天创建目标，不增加强制填写目标的表单；新对话仍需选择继续已有目标，或与 Agent 讨论新目标。目标档案不会直接创建目标。
- 每个目标的计划、实际执行和复盘周期分开保存。继续原目标时，会沿用已确认的计划与记录约定，进入下一周期；调整目标时，会保留旧版本并新建可编辑计划。
- 目标档案改为全历史卡片：可查看计划版本、执行/复盘周期和活动记录；草稿、历史版本和已更正记录会明确标注，不把它们误当成当前承诺。
- 支持搜索、主/次/待归类筛选、状态筛选、排序及历史分页。临时做的其他活动不冒充当前目标的完成；用户更正经历后，旧事实不再进入当前有效上下文。

### 更轻的聊天与每日记录

- 聊天界面改为更轻的阅读和输入布局：暖色浅色主题为默认，暗色继续保留黑金风格；桌面与手机都优先保证对话、目标档案和输入框的可读性。
- 桌面侧栏缩窄并可收起，进度与账号工具整合到顶栏；AI 回复取消包围框线，用户消息采用柔和气泡，管理员入口收纳，减少对话区域的拥挤。
- 每日记录从两步向导改成单页：桌面双栏、手机纵向滚动，同一页完成活动和整日回顾；仍保留原有评分范围、保存接口、失败重试和未提交内容保留。

### 生日资料

- 注册需要填写完整出生日期，系统拒绝空值、格式错误和未来日期；年龄按完整生日动态计算。
- 只有“历史年龄为 10 且尚未填写生日”的账号，会在登录或恢复会话时被要求补填；其他已有账号不会因此被强制补填。
- 不再默认赋值 10 岁；保留现有 10–120 岁范围。补填成功后不会反复弹窗，也不会代用户填写生日。完整出生日期不送入模型档案提示词，只提供计算后的年龄。

### 模型与知识检索

- 生产 DeepSeek 主回复与共享 Router 已切换到 **DeepSeek 4.1 Flash**，并完成实际响应型号核验。
- **目录语义检索已启用**：明确点名章节时走改良关键词快速检索，其他问题先选允许模块内的真实章节，再核对正文证据；异常、超时或非法 ID 时不放行原始候选。原有意图门控、知识中介和共享回复校验仍保留。
- 旧 Holdout（44 题）上，目录语义方案的**返回片段平均精度**从 49.75% 升至 71.60%，无关问题正确返回空从 68.75% 升至 100%；但 Hit@K 不变，Recall@K 从 82.14% 降至 81.25%，并多了 1 个正例错误空返回。

这些是“召回片段与当前标签的一致性”指标，**不是最终回复正确率、干预安全性或临床效果**。新的冻结合成集仍有漏召回，不能把检索改善写成全面准确率提升。

### M3 与 M4 流程

- M3 的小字介绍改为“**计划执行前记录提醒**”；这是介绍文案修改，不代表新增定时推送服务。
- M4 融合了本轮 Excel 字段标准与提示词：围绕当前焦点 PA 的实际执行，区分 A（实际执行且用户确认情绪改善）、B（尚未开始且有明确执行前障碍）、C（执行窗口已过且有证据排除 A/B）和信息不足。
- “核心 PA”指本段聊天选中的焦点目标，不等于主要目标分类；独立次要目标被选中后同样可以完整复盘。其他未绑定活动仅作辅助反馈，不抵扣焦点目标。
- M4 新增证据门控：ABC 总结必须由用户核对；纠正后旧认可失效并需重新确认；BA 教育要在 ABC 核对之后，策略要在教育和理解之后。完整执行但情绪没有改善会记作“已完成活动 + C”，不会被错误改写成未执行。
- 网页最终确认只确认已完成、证据齐全的复盘；继续、调整、更换、暂停和结束均需用户明确决定。继续原目标不会改写原计划，调整才进入新计划版本。
- 网页以中文展示结构化 ABC、执行结果、核对状态和待补信息；旧周期确认不能直接沿用，新事实纠正或提取失败不能使过期草稿通过确认。旧的未确认草稿可能需要继续聊天补齐证据，不批量篡改历史记录。

## 验收摘要

- 最终完整后端回归：**671 passed、1 skipped**；存在一条既有依赖弃用提示，无失败。前端类型检查、本地及服务器正式构建通过。
- M4 隔离测试覆盖真实保存→步骤核验→网页确认路径、伪造证据、纠正后失效和同计划续周期。生产环境只使用合成文本调用真实 4.1 Flash，A/B/C 三场景均通过提取与契约核验，未写生产业务记录。
- 新检索线上探针：两条 BA 相关问题均返回 2 条并通过中介审核，比特币价格问题返回空；该小样本只证明链路可用，不代表线上准确率。
- 当前发布目录、服务健康与未登录目标接口 401 已核对；**168 个发布源码文件 SHA-256 与本地一致**。生产 V2 预期字段全部可读，知识索引沿用线上原有 1,331 个片段。
- M3/M4 本地合成页面完成桌面/手机检查；公网登录/注册页完成 1440px 与 390px 检查，生日初始为空且必填，无页面运行错误、静态资源失败或横向溢出。完整登录后流程以本地隔离验收为准，没有冒充生产真实账号端到端验收。
- 检索实验保留了失败和退步案例：不能只挑旧题高分，也没有把空返回误记为命中。

## 数据边界

- **没有上传或同步本地测试数据库、演示账号、聊天或测试知识库。** 发布使用应用目录白名单，关闭常规迁移、知识导入和启动自动回填。
- 今晚较早的发布仅新增目标模型所需的四张空表，以及可空的生日日期列；没有复制本地数据。本轮 M4 的 20 个字段生产已存在，无需再改表。
- 本轮允许的配置更新只有发布 M4 提示词、模型和检索开关；M4 提示词原值及环境配置均已在服务器私有位置备份，其他管理员提示词未覆盖。

## 限制与待办

- 本轮没有上线向量数据库、Qdrant 或 BGE 重排服务。该实验本机实际重排平均约 20.31 秒、P95 约 31.36 秒，且开发集表现未胜过 P0；不会把本地实验写成线上能力。
- 目录语义检索会增加模型调用、输入 token 和延迟；其新增模型阶段观测均值约 1.47 秒、P95 约 2.95 秒，且不含原中介、主回复、数据库读取和完整端到端时间。费用应以实际账单和运行监控为准。
- 手机验收使用浏览器触屏视口仿真，不等同于 iOS Safari 真机；真实用户对话也未被当作验收样本。
- M4 证据引用校验可约束来源与先后顺序，但不能从数学上证明模型解释正确；仍需业务抽查。中介、提取和回复都涉及模型调用，不承诺所有回复都加速。
- 本次不新增自动定时提醒、日历打卡结算，也不把一次模型探针称为临床验证。

更多细节：[M4 合并与上线记录](M4_CONTRACT_RELEASE_20260917.md)、[RAG 新旧指标对比](RAG_ACCURACY_UPGRADE_20260917.md)、[UI 与生日上线记录](UI_RELEASE_20260917.md)、[目标与周期改造](GOAL_MODEL_RELEASE_20260916.md)。
