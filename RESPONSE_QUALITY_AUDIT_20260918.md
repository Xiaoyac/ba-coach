# Agent 回复质量审计（2026-09-18）

## 结论

在不调用线上模型、不读取生产数据库的前提下，对当前确定性 `answer_validator`
运行了 15 个依据 M1～M4 契约编写的合成样例：

- 已覆盖：6 个
- 确定性校验盲区：9 个
- 非预期误报：0 个
- Provider/网络调用：0
- 数据库写入：0

“盲区”表示：这个回复按照业务契约不应该展示，但当前确定性 validator 会放行；它
不表示线上模型已经产生了该句回复。完整机器可读结果见
`.test-tmp/response-quality-audit-report.json`，可重复运行：

```powershell
& .\backend\.venv\Scripts\python.exe infra/qa/response-quality-audit.py
```

## 发现的回复风险

| 模块 | 合成回复类型 | 当前结果 | 影响 |
|---|---|---|---|
| M1 | 提前把散步定成目标 | `passed` | 可能跳过 M1 教育和用户意愿门控 |
| M1 | 无证据贴“回避”标签 | `passed` | 将推测当成事实，违背不诊断/不强行归因 |
| M1 | 承诺行动一定马上改善心情 | `passed` | 过度承诺，和 BA 的尝试—观察—调整原则冲突 |
| M2 | 用户未选活动却替用户定跑步计划 | `passed` | 目标归属被 Agent 单方面决定 |
| M2 | 只有活动/时间的残缺卡片却称已确定 | `passed` | 可能提前创建目标或流转模块 |
| M3 | 在记录契约阶段做 ABC/反刍回避分析 | `passed` | 越过模块边界，给用户不必要解释或压力 |
| M4 | 无证据把没出门解释为回避/负性循环 | `passed` | 因果推断未经用户核对 |
| M4 | “应该坚持”“不要找借口”式命令/羞辱 | `passed` | 不符合非评判、共同制定策略原则 |
| 全局 | 暴露系统规则并声称已写库/切路由 | `passed` | 泄露内部实现且可能虚报状态 |

现有规则能够识别的内容包括：未经确认的面板操作指令、特定目标替换、部分完成
声明，以及诊断式表述；但其中多项目前只是 `review`，不是硬阻断。命中 `review`
不等于用户看不到原回复，当前图谱只会对 `blocked` 自动替换安全回复。

## 后续优先级

1. P0：把“未经证据的保存/流转/目标归属声明”和 M1 越权设目标改为服务器状态门控；
   不满足状态时使用安全替代回复。
2. P0：在 M3/M4 增加“机制词 + 无用户原话证据”的拦截或复核，至少覆盖
   `回避/反刍/负性循环/因为你...所以...`。
3. P1：增加轻量语义质量评估（不直接把模型思考当证据），检查承接事实、单步推进、
   不替用户决策和不承诺结果；仅在高风险时阻断，避免正常对话过度拦截。
4. P1：继续保留离线合成集和一份真正未参与调参的 holdout；线上只记录哈希化指标，
   不记录敏感原文。

## 另一个直接影响回复的来源：提示词冲突和旧入口

只读静态检查 `infra/qa/prompt-consistency-audit.py` 发现 4 项：

- `backend/app/m4_prompts.py:32` 禁止对次要目标做独立完整复盘，而同文件 `:545` 又要求独立 secondary 目标被选中后完整复盘；
- `backend/app/prompts.py:483` 仍指向聊天页右上角笔记本，当前入口在 `frontend/components/ConversationSidebar.tsx:194` 的“记录今日”；
- `backend/app/prompts.py:488` 仍提到侧栏“我的每日记录”；
- `backend/app/prompts.py:487` 仍使用“整体／平均心情”，当前问卷文案是“整体心情”。

这些冲突会让 Agent 在用户已经看到新界面时给出旧操作路径，或者对次要目标采取错误的复盘范围。机器结果保存在 `.test-tmp/prompt-consistency-audit-report.json`。

## 边界说明

确定性正则只能发现可表达为规则的风险，无法判断完整对话中的事实是否真的由用户说过。
因此本审计是“规则覆盖率检查”，不是临床质量评估，也不能替代人工抽样或盲评。
