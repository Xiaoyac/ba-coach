# 修复后整轮验收

2026-09-11。本次约定范围验收通过；不是全系统临床有效性、生产知识召回精度或高并发认证。未部署生产、未改生产数据库。

## 修复内容

- 新增只读 reply_workflow，读取当前模块、运行版本及本周期计划是否正式确认。
- 生成前注入系统提交状态；明确聊天同意不等于网页确认接口成功。
- 回复发送前再次读取数据库。发现尚未发生的锁定/保存/模块切换宣称时，替换为由真实状态生成的进度说明。流式片段同样经过校验，原错误文本不先发送。
- 状态不可读时不宣称已保存或已切换。普通其他完整性错误仍按原阻断流程处理，不因修正文案绕过。
- 连带修复 M2 草稿等待循环：以前只有 Router 提议跳转才抽取草稿，现在每轮 M2 后台先抽取，再依据新步骤状态判断准备情况。草稿/对话证据就绪与正式提交分离，网页确认仍是正式推进入口。
- 本次没有新增 LLM 节点。每轮 M2 后台抽取次数可能增加，因此不宣称响应速度改善。

## 真实模型三轮复验

使用隔离内存 SQLite、合成用户和合成知识、本地关键词检索；中介、回复、Router、抽取使用服务器已配置的真实 deepseek-v4-pro。只加载供应商配置，不加载生产数据库连接。

1. 膝盖疼、散步未定：模型先澄清；产生不完整草稿，缺少地点、时长和应对，无法确认。保存并刷新读取一致。整轮后台就绪约 29.54 秒。
2. 用户改选在家沙发读小说，每天饭后 10 分钟，说明意义、障碍和提醒办法：真实抽取完成，面板 can_confirm=true；数据库仍为 M2。约 35.87 秒。
3. 用户再次表达同意：使用真实 SSE 流式接口；回复说明面板仍需点击确认，未宣称系统已锁定或进入 M3。保存与 SSE 文本一致。约 31.69 秒。
4. 调用真实确认 API 后才进入 M3，状态和计划绑定检查通过。

时间包含后台抽取/Router 等待，不是首字延迟、平均值或分位数。

## 真实网页联调

本地 Next.js → SSH 回环隧道 → 隔离 FastAPI → 真实模型，全程没有 page.route 模拟 API。

手机 390×844：继续 M3，发送“请先简单说明记录什么，不要替我确认新的约定”；网页发起真实流式请求，收到非空回复和 persisted=true；数据库消息与流式文本相同，网页刷新后仍显示回复；无横向溢出，无浏览器脚本错误。

测试工具曾遇到 DevTools 无法读取 SSE body，以及用原始 Markdown 匹配渲染文本失败；修正为克隆读取真实 fetch 响应、匹配渲染文字后通过。没有通过替换产品 API 或模拟回复来让测试通过。

## 回归结果

- 后端全量：475 通过、1 跳过，1 条第三方弃用警告。
- 新增回复状态校验：10 项通过，含旧真实反例、条件句、已确认状态、流式/非流式不泄漏错误回复。
- Contract 探针：14/14 通过。
- V2 API 冒烟：7 场景通过，包含 M1—M4 流转。
- 模拟接口 UI 回归：手机目标选择/确认、测试工作台导入预览、编辑、筛选、批量运行、人工评价、追问、下载及移动端通过。
- 前端 TypeScript 检查通过。
- 前端优化生产构建通过，6 个页面生成完成。

## 边界

本次真实模型验收覆盖特定 M2 场景和 M3 网页续聊；M1/M4 为隔离自动化回归，未宣称四模块均完成真实模型长期评测。生产知识库的 1331 个片段未参与本次真实模型场景。

回复校验是状态加规则检查，不是完整语义证明；无法保证穷尽所有自然语言变体。新增真实反例后仍应持续回归。缺少状态时采用保守说明。

## 提交安全检查

当前示例配置中的供应商凭据已替换为占位符。发现初始 Git 提交的示例配置含疑似真实格式的 DeepSeek Key，不能假定有效，但应按已暴露处理并轮换；本轮未擅自调用供应商撤销接口或重写 Git 历史。

本次提交不包含私钥、生产 SQL 备份、环境文件或被试对话。隔离验收使用的账号和内容均为合成数据。
