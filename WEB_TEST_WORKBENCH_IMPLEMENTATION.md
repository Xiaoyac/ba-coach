# 测试记录网页化实施报告

更新日期：2026-09-09

## 1. 本次交付总结

已在现有 BA Coach 网页中实现管理员「测试工作台」，将“维护测试用例 → 发起测试 → 保存回答 → 人工评审”串成一个流程。管理员不需要把用户输入从 Excel 复制到聊天窗口，也不需要把 AI 回答再复制回 Excel。

**2026-09-09 发布更新：已部署至 https://bacoach.xyz/，版本 20260909T074652Z。** 已在 `ba_coach_260908` 创建三张测试专用表，未执行业务表迁移或替换知识库。管理员强制刷新网页后可见侧边栏「测试工作台」。下文较早测试记录保留其当时状态；中介节点新增情况见 `KNOWLEDGE_MEDIATOR_IMPLEMENTATION.md`。

界面按提供的截图保留六列：Case ID、模块、用户类型、测试场景、用户起始输入、AI 应达到的目标。表头固定、长文本自动换行、支持横向滚动，并沿用网站的深色/浅色主题。不是把 WPS 的菜单、公式、合并单元格等完整复制到网页。

已完成的功能：

- 管理员侧边栏新增「测试工作台」；普通用户既看不到入口，也不能调用对应接口。
- 新建、编辑、按模块筛选和搜索测试用例。
- 导入 `.xlsx` / UTF-8 `.csv`：选择工作表、预览、检查错误、确认后统一保存。
- 单条运行、最多 20 条顺序批量运行；调用所选模型，自动保存结果。
- 在某次运行中继续追问：自动携带该次对话上下文，每轮生成独立记录。
- 人工评审：待评审、通过、未通过及备注；保留历次评审，不覆盖同事的评审历史。
- 查看输入、完整对话、用例版本、模型、耗时、Token 用量和检索来源；列表显示最新评审结论。
- 导出用例和测试记录为 Excel/WPS 可打开的 UTF-8 CSV。

初版依据截图实现。2026-09-09 收到原始 `测试集示例.xlsx` 后，已补做原文件测试：其中 41 条用例符合当前运行规则，但整表仍有 6 处校验问题，**不能声称整份工作簿已经完整迁移**。原文件包含“评测指标”“测试汇总（迭代记录）”“单次测试模板”和不同同事的历史记录表，这些尚未映射为网页的评分体系及历史运行。详见第 10 节。

## 2. 同事实际怎么用

### 首次迁移评测集

1. 用网站管理员账号登录，点击侧边栏「测试工作台」。仅测试人员需要管理员权限，不应为了这项功能给普通用户开放管理员权限。
2. 点击「导入 Excel / CSV」，上传原表；多工作表文件优先选择“评测集”，也可切换工作表。
3. 检查有效条数和错误行。有错误时默认不能确认；可修正后重新上传，也可以明确勾选「仅导入有效用例」后导入有效部分。错误行会列出且不会导入，原文件保持不变。原始评测集可选择导入 41 条有效用例，剩余 6 行仍需处理；这不是整份工作簿迁移完成。
4. 点击「确认导入」。如果某个 Case ID 已存在，本次导入全部取消，不会悄悄覆盖旧用例。

### 一次测试示例

例如维护 `M2-001`：

| 列 | 示例值 |
| --- | --- |
| 模块 | M2 |
| 用户类型 | 愿意尝试，但不知道如何开始 |
| 测试场景 | 用户想增加活动，需要设定一个可执行的小目标 |
| 用户起始输入 | 我愿意试试，但不知道做什么。我不喜欢跑步。 |
| AI 应达到的目标 | 尊重偏好，协助形成一个具体、可执行的目标，不替用户强行决定。 |

点击「运行」后，系统发送保存的起始输入并自动记录回答。评审人打开「查看 / 评审」，对照目标填写结论和具体依据。

如果需要追问“那我今天先走五分钟可以吗？”，直接在运行详情中发送。系统会带上前一轮输入、回答与短期记忆，再保存一条带父运行 ID 的新记录，不需要复制聊天记录。每条对话分支最多 20 轮。

如需下一版 Prompt 的独立对照，应回到用例列表重新运行。**追问继承父运行的用例和 Prompt 快照**，不会中途切换到新编辑的 Prompt；追问的模型则采用工具栏当前所选提供商。

### 批量测试

勾选用例后点击「运行选中」。网页逐条提交，每条完成或失败后再执行下一条，最多 20 条。点击「停止后续运行」只停止尚未提交的用例；当前请求继续执行并保存，不等于撤销已产生的模型费用。

关闭面板或浏览器也会停止后续浏览器队列；已经提交的任务由后端继续处理。重新打开工作台可查看其结果。如果提交/轮询的网络请求失败，系统不会自动重发一个新模型请求，应先查看运行记录，避免重复付费。

## 3. 实现方式

### 3.1 前端

核心组件：`frontend/components/TestWorkbench.tsx`。

接口封装：`frontend/lib/adminEvaluations.ts`。

入口通过现有 `ConversationSidebar` 和 `ConversationWorkspace` 挂载，使用现有账号认证和颜色体系，没有引入另一套网站或登录系统。

“评测集”页展示六列表格，“测试记录”页展示运行状态、实际回答摘要和最新评审结论。详情页左侧为对话，右侧为场景、验收目标和评审表单。复杂内容纵向滚动；窄屏下表格横向滚动，不压缩成长文本无法阅读的小列。

### 3.2 后端与数据结构

接口：`backend/app/routes/admin_evaluations.py`。

模型及导入导出处理：`backend/app/test_workbench.py`。

采用三张独立表，而不是将测试输入塞进正式用户的聊天或业务档案：

| 表 | 作用 | 关键字段 |
| --- | --- | --- |
| `evaluation_cases` | 当前用例定义 | 唯一 Case ID、六列内容、额外列 JSON、revision、创建人、更新时间 |
| `evaluation_runs` | 每一次执行 | 请求 UUID、用例 ID、父运行 ID、执行人、状态、用例/Prompt 快照、模型、实际输入/回答、对话、短期记忆、指标、错误代码、时间 |
| `evaluation_reviews` | 追加式人工评审 | 运行 ID、评审人、结论、备注、时间 |

用例修改采用 `revision` 乐观锁。例如甲、乙同时打开 v1，甲先保存成为 v2；乙仍以 v1 提交时收到冲突提示，需要刷新，避免最后一次保存覆盖同事的修改。

运行固定保存当时的用例内容和版本。后续即使改了用例，旧回答也仍对应旧验收目标。每次评审只追加记录，列表和 CSV 使用最新一次结论，详情页可以看到全部评审历史。

这些数据由管理员共享，不是个人私有测试空间；保存执行人和评审人，但本版未增加独立测试员角色或按项目隔离的权限模型。

### 3.3 调用链及隔离边界

```text
保存用例 → 生成请求 UUID → 后端记录 queued
  → 原子领取任务 running
  → 独立临时会话 + 有效 Prompt 快照
  → 现有风险检查 → 所选模块检索与生成（或危机响应分支）
  → 保存回答、上下文、指标与状态
  → 网页展示 → 人工追加评审
```

复用现有 LangGraph 执行图、知识检索和模型提供商封装，没有重新实现一个与正式回答逻辑脱节的演示机器人。所选模型仍需服务器已有对应配置；风险检查沿用现有 DeepSeek 路径。没有为了自动评审增加 LLM 调用。

隔离做法：

- 每次执行创建独立内存会话，不复用管理员的正常聊天会话。
- 不提供真实用户 `subject_id`，并将 `sessionmaker`、`memos` 设为 `None`，禁用真实档案读取、业务写入及长期记忆调用。
- 短期记忆标记 `sandbox_mode=true`，作为额外隔离标志。
- 全局与模块 Prompt 通过新增的可选 `GraphContext.prompt_snapshot` 传入；普通聊天保持原有读取路径。这样既能使用管理员已经编辑的有效 Prompt，又不必为测试图开启数据库访问。
- 用户类型、测试场景会作为“虚构用户”元数据进入上下文；“AI 应达到的目标”不进入 Prompt、用户消息或风险检测输入，仅供评审使用。

用户类型和场景本身也可能给模型提示，因此应写成事实描述，不应暗藏“标准答案”。若要做严格的纯输入评测，可将这两列留空，只填写起始输入和验收目标。

**本版是固定模块评测，不是完整业务集成测试。** 每次强制进入用例指定的 M1～M4，不调度正常聊天回答后的业务 Router；也没有真实档案、目标卡、执行周期与长期记忆。这有利于定位某个模块的回答问题，但不能据此证明模块切换、数据库字段抽取或完整 1→2→3→4 流程正确。相关流程仍应使用专门的集成测试覆盖。

### 3.4 导入导出策略

按照表格安全处理原则，采用“先预览、再确认”的值导入，而非截图 OCR 或执行 Excel 公式：

- 支持 `.xlsx` 和 UTF-8 CSV；旧 `.xls` 需另存为 `.xlsx`。不用先逐行复制粘贴。
- 文件最多 5 MB，XLSX 解压总大小最多 25 MB；选定工作表最多 550 行、50 列，一次最多 500 条有效用例。
- 前 20 行寻找 Case ID 表头，允许中间空行；识别截图中的“用户类型（不一定每个模块都出现）”长表头，M1～M4 转为内部模块名。
- 使用 `openpyxl` 只读模式，不跟随外部链接；发现公式就拒绝，要求先转为值。格式异常、重复编号、缺列、非法模块等会显示错误。
- 附加列保存为 JSON，随用例导出；不保留字体、颜色、公式、图表和合并布局，也没有复刻整份工作簿的计算功能。
- 导出 UTF-8 BOM CSV；以 `= + - @` 开头的单元格加安全前缀，降低 Excel/WPS 将输入或回答执行成公式的风险。该安全前缀意味着危险开头文本往返导入时可能多一个单引号。

### 3.5 超时、幂等与恢复

请求 UUID 是运行主键。重复提交同一请求不会重新调用模型；冲突请求也不能借同一个 UUID 改成另一条测试。后台通过 `queued → running` 条件更新领取，避免相同任务重复执行。

一次运行设置 180 秒超时。同一管理员存在 3 条活跃运行时拒绝继续提交；这是数据库计数的尽力限制，不是分布式严格限流器，极端并发仍可能越界。正常 UI 批量操作是顺序提交。

本版使用进程内后台任务，没有引入 Redis/Celery。数据库保存任务记录，但不是可恢复执行的持久任务队列：服务重启可能中断运行。创建超过 240 秒仍停留 queued/running 的记录，在读取时显示为“已中断”，不会自动补跑、自动计费；这是显示层的保守判断，后端状态字段不会因此被批量改写。

模型调用完成但最终数据库写入失败时，也可能需要人工检查日志与中断记录。后台超时不保证模型提供商已经停止生成或停止计费。这些是后续生产级任务队列和运维告警需要覆盖的边界。

## 4. API 清单

所有接口前缀为 `/api/admin/evaluations`，统一校验管理员 Bearer 身份。

| 方法与路径 | 功能 |
| --- | --- |
| `GET /cases` | 按编号读取用例，当前最多 2,000 条 |
| `POST /cases` | 新建用例 |
| `PUT /cases/{id}` | 按 revision 编辑用例 |
| `POST /import/preview` | 解析文件，返回工作表、有效行和错误 |
| `POST /import` | 单事务保存全部用例，遇到编号冲突全部取消 |
| `GET /cases.csv` | 导出用例及额外列 |
| `POST /cases/{id}/runs` | 创建首轮或追问任务，返回 202 |
| `GET /runs` | 每页 100 条，可按用例 ID 查询 |
| `GET /runs/{id}` | 运行详情与完整评审历史 |
| `POST /runs/{id}/reviews` | 追加评审 |
| `GET /results.csv` | 导出运行、当时用例及最新评审结论 |

网页的搜索/模块筛选对已加载记录生效，较早运行需点击“加载更早记录”；刷新会回到最新一页。尚未实现大规模服务端检索、批次维度分页和流式全量导出。CSV 导出当前一次读取全部记录，规模大时应改为分段读取/流式输出。

## 5. 可查看的指标，以及不能误读的地方

| 数据 | 用途 | 注意事项 |
| --- | --- | --- |
| 运行状态、错误代码 | 发现模型异常、中断、超时 | completed 仅表示执行完成，不代表质量合格 |
| `duration_ms` | 观察一次后台执行总耗时 | 不包含浏览器等待和排队时间，也不是首字延迟 |
| `usage` | 观察主回答 Token 消耗 | 沿用提供商返回；不保证包含风险检查等全部附加费用 |
| `telemetry.retrieval` | 查看门控原因、是否检索、耗时、返回条数等 | 取决于实际执行分支；危机分支可能没有模块检索信息 |
| `retrieved` | 查看片段 ID、来源及检索分数 | 分数不是回答正确概率，不可跨检索器直接比较 |
| `risk_flagged` | 查看是否进入风险响应分支 | 不能单凭标记认定真实临床风险 |
| `prompt_sha256` | 比较全局/模块 Prompt 快照是否变化 | 不是知识库、完整拼装 Prompt、危机 Prompt 或模型版本的完整指纹 |
| 最新人工评审 + 备注 | 判断是否达到用例目标 | 需团队先对齐判定标准，未评审不能按失败或通过处理 |

结果可以导出供团队统计：执行成功率 = completed / 全部运行；评审通过率 = pass / (pass + fail)，待评审单列；耗时可按模型/模块计算均值和 P95。为了避免样本混淆，比较两版时应使用同一组用例，区分首轮、追问、失败和重跑记录。

**本次没有修改 RAG 排序或给出新的检索精度提升结果。** 自动归档减少的是人工操作与记录遗漏；模型回答质量是否改善，需要用实际评测集和人工标注验证。自动化测试里的模拟模型耗时和回复不能当成线上质量或性能指标。

目前保存了全局/模块 Prompt 文本快照、用例快照和检索结果 ID/分数，但未冻结知识库内容、Embedding 版本或整个模型配置。因此可追溯性优于复制粘贴，不等于完全可重放的离线实验平台。

## 6. 遇到的困难与取舍

| 问题 | 本次处理 | 优点与代价 |
| --- | --- | --- |
| 初版只有截图；原文件后来提供 | 六列落地后使用原文件实测，不编造历史 | 41 条符合当前规则；特殊类型、评分和历史迁移仍需适配 |
| 正常聊天会读写档案、长期记忆和业务状态 | 独立会话、禁用临床持久化、冻结模块 | 不污染真实业务；不能替代真实档案驱动的集成测试 |
| 测试运行需要使用管理员最新 Prompt | 增加可选 Prompt 快照上下文 | 保留有效配置和旧版本；知识库版本尚未冻结 |
| 同事同时编辑/评审容易覆盖 | 用例乐观锁，评审追加保存 | 冲突可见，历史可追踪；没有完整差异合并编辑器 |
| 批量任务耗时且可能重复付费 | 顺序批量、UUID 幂等、持久记录、无自动重跑 | 行为简单可控；浏览器关闭不继续提交剩余队列 |
| Excel 格式、公式和恶意单元格 | 限量只读导入、公式拒绝、导出安全转义 | 降低风险；无法无损保留整份 Excel 布局/公式 |
| 编辑已有用例时浏览器标签定位不稳定 | 增加明确 aria-label，补充弹层焦点管理 | 编辑和键盘操作更稳定，已纳入浏览器复测 |
| Windows 默认临时目录无权限 | 使用工作区旁独立测试临时目录 | 测试不触及真实数据库或生产配置 |

## 7. 验证记录

- 后端专用回归：`tests/test_evaluations.py`，**28 项通过**。覆盖全部接口权限、创建/导入事务、乐观锁、有效 Prompt 快照、验收目标不泄漏、上下文继承、真实业务表行数不变化、幂等、重复任务领取、追加评审、错误/超时、风险分支、运行数量限制、20 轮上限、CSV 安全、XLSX/CSV 解析及迁移作用范围。
- 后端全量回归：**395 项通过**，63.21 秒。有 1 条现有 Starlette/AnyIO 弃用警告，没有测试失败。
- 前端 TypeScript 检查及 Next.js 生产构建：通过。
- 浏览器交互回归：**10 类检查通过**：管理员入口、六列新建/编辑、导入预览确认、模块筛选、两条批量运行、人工评审、多轮追问、CSV 下载、手机宽度适配、无页面 JS 异常。
- 浏览器测试使用真实前端和明确标记的模拟 API 数据，后端回归使用真实 HTTP 路由/执行图和模拟模型、隔离 SQLite。**没有在这轮验证里发送真实付费模型请求或访问生产业务数据。**

复现方式（先安装项目依赖）：

```powershell
# backend 目录；显式指定可写临时目录，不使用真实 .env 数据库
python -m pytest -o addopts='' -p no:cacheprovider --basetemp <新的可写临时目录> -q

# frontend 目录
npm run typecheck
npm run build
# 在单独终端启动本地前端
node node_modules/next/dist/bin/next start -H 127.0.0.1 -p 3108
# 配备 Playwright 的开发环境；Windows 可使用已安装的 Edge
$env:WORKBENCH_BROWSER_CHANNEL = 'msedge'
node tests/workbench-smoke.cjs
```

Playwright 是开发验证工具，不是生产依赖；可使用 Codex 自带 Node 依赖目录设置 `NODE_PATH`。截图输出位于 `.test-tmp/workbench-browser/`，为模拟数据，不是线上实测截图。

## 8. 上线准备与回退

本次附带 `backend/scripts/create_evaluation_tables.py`，默认只读检查，只有显式 `--apply` 才创建测试专用表。脚本检查目标数据库名称、账号表存在及已存在测试表的列名集合；不负责全面比较既有索引/字段类型，应先在预发布环境审查 DDL。

生产环境若仍使用 `STARTUP_DB_MAINTENANCE=false`，不得为了这三张表打开全局启动维护。应完成备份及范围确认，使用明确脚本创建测试表后再发布：

```bash
# 在 backend 目录；以下是待上线时执行的示例，本次未在服务器运行
python scripts/create_evaluation_tables.py \
  --env-file /etc/bacoach/backend.env \
  --expected-database ba_coach_260908

# 确认检查结果后，显式应用
python scripts/create_evaluation_tables.py \
  --env-file /etc/bacoach/backend.env \
  --expected-database ba_coach_260908 --apply
```

只创建 `evaluation_cases`、`evaluation_runs`、`evaluation_reviews`，不改 M1～M4 业务字段。MySQL DDL 可能自动提交，并不提供多张表创建的整体事务回滚；部分失败时需检查已创建表，再使用幂等脚本继续。

上线时还需确认：

- 测试人员的管理员账号、所选模型与风险检查提供商已配置。
- HTTPS 可用（客户端 UUID 依赖浏览器安全上下文）；反向代理允许约 7 MB 的 JSON 上传请求，否则较大的 Excel 会被代理以 413 拒绝。
- 模型网络、数据库写入和超时行为在预发布环境做一次真实冒烟验证，再导入正式评测集。
- 管理员日志和导出文件妥善保管；测试输入尽量使用虚构/脱敏信息，避免上传真实用户敏感记录。现有测试工作台没有自动脱敏能力。

若需回退，停用新入口或回退应用代码，保留这三张表和已产生的评测数据，不应通过删表回退。本版不提供直接删除用例/运行的功能；数据保留期限与清理审批策略可作为后续运维建设。

## 9. 后续值得加强的方向

1. 根据现已提供的原 Excel，优先适配全局安全/跨模块类型、说明行和缺字段草稿；再对照完整评分表增加维度评分、评分说明、评审人一致性和历史导入映射。
2. 增加服务器持久任务队列、批次 ID、进度恢复和统一并发/费用上限，使批量测试不依赖浏览器保持打开。
3. 冻结知识库/检索配置/模型参数版本，增加 A/B 实验与按批次成功率、通过率、P95 图表。
4. 提供虚构档案和目标卡夹具，独立实现跨模块及数据库读写集成测试，不放开对真实用户档案的访问。
5. 增加专门的测试员权限、数据保留策略、分页搜索与流式导出，适应更多同事和更大评测集。

## 10. 原始 Excel 实测补充（2026-09-09）

来源：用户提供的 `测试集示例.xlsx`，1,507,160 字节。SHA-256：`00dbb4305d439b12fe287113b258799c6caf266b25f790e2a0f7bd64a3c74d32`。测试前后文件哈希一致，未改写原文件。

工作簿共 11 张工作表（含 WPS 内部图片表）；“评测集”为 54 行、7 列，主要用例区是 A～F 列。

### 10.1 解析结果

41 条满足当前固定模块运行规则：M1 15 条、M2 14 条、M3 6 条、M4 6 条。其余校验结果如下：

| Excel 行 | 编号/内容 | 原因 | 后续处理方向 |
| --- | --- | --- | --- |
| 14 | M2-004 | 起始输入为空，验收目标也未填写 | 由业务补充，不替用户编造输入和目标 |
| 33～35 | SAFE-01～SAFE-03 | 模块写为“全局安全”，而当前用例类型只支持 M1～M4 | 增加独立安全评测类型及执行上下文；不能直接改写标签冒充某模块测试 |
| 36 | FLOW-01 | 跨模块完整流程用例 | 需要能运行 Router、继承虚构业务状态的集成评测模式 |
| 38 | “新增：M1/M2 历史问题回归案例……” | 这是分组说明，不是用例，当前解析器把非空行按用例校验 | 增加明确的说明行识别或导入映射，不应当生成一个测试编号 |

共 6 处校验提示，其中只有 5 条是真正待处理的用例，另一处是说明行。网页当前会阻止整表确认导入，不会自动丢弃这些内容。已将校验消息调整为中文说明，避免只返回难读的 Pydantic 英文报错。

### 10.2 实际做过的验证

1. 原 `.xlsx` 原样发送至真实 `/import/preview` 接口，得到 41 条有效用例及上述 6 处错误。
2. 在独立 SQLite 测试库中，**显式选取有效的 41 条作为测试子集**提交保存；这不是偷偷绕过网页校验向生产导入。逐项比对所有保存字段与解析结果，一致。
3. 选取 `M1-001`，经过真实 HTTP 运行接口和执行图完成一轮**模拟模型**调用，生成 2 条对话消息和完整运行记录，状态 completed，模型名 `stub-1`。
4. 导出 CSV，得到 41 条数据行，和保存数量一致。
5. 浏览器展示后端实测生成的 41 条用例数据；上传原始 XLSX 并回放真实校验结果，确认显示“41 条有效用例，6 处错误”，确认导入按钮被禁用。此处浏览器采用本地 API 响应夹具，真实接口行为由前面的 HTTP 回归覆盖。

原文件专项回归：`backend/tests/test_evaluations_workbook.py`，**1 项通过**。通过 `WORKBENCH_XLSX` 指定原文件才会启用，不把原工作簿、个人历史记录或模型凭证提交到代码仓库。包含原文件专项的最后一轮全量回归为 **396 项通过，59.00 秒**，仍只有 1 条已有依赖弃用警告。

**真实模型测试未完成**：本机检查结果为 Claude、DeepSeek、豆包均未配置可用凭证，没有发送真实模型请求。模拟测试只能证明导入、执行调度和记录保存链路，不能证明回答质量、真实延迟、检索精度或安全用例通过。需要在已配置模型的测试环境补一轮真实调用；不要把 API Key 发到聊天中。

### 10.3 原文件暴露出的额外范围

“单次测试模板”要求多轮对话，并包含“深度思考（跳转）”“深度思考（模块内）”“评价/打分依据”“后续修改/排查”等列；本版覆盖输入/回答、人工结论与备注，未覆盖完整的旧模板字段。

“评测指标”包含按分数分档的评分标准，不等同于本版待评审/通过/未通过三种状态。历史表布局不统一，部分单元格含 WPS `DISPIMG` 图片公式，不能通过通用六列导入器无损迁移。后续应单独设计评分维度及历史记录映射，并明确哪些图片/旧记录需要保留，而不是把这些内容当成可执行的新用例。
