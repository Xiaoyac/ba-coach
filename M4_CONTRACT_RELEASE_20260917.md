# M4 字段、提示词合并与上线记录

日期：2026-09-17；发布：`20260916T203650Z`；生产网站：https://bacoach.xyz/ 。

## 本次调整

- 根据 `M4字段标准(1).xlsx` 与 `M4提示词(1).md` 合并字段含义、ABC 结构、情境判断与五阶段复盘流程。
- 模块三小字介绍更新为“计划执行前记录提醒”，不改变模块标识，不新增自动通知。
- M4 不再仅凭模型报告“已完成步骤”开放确认；提取、数据库保存、Router 步骤、确认接口和网页展示共同使用服务器验证的契约。
- 新 M4 提示词已写入生产管理员可编辑的 `module_4` 配置；仅改源码默认值不足以覆盖已保存的旧提示词，因此执行了独立的受保护发布。其他管理员提示词不覆盖。

## 合并时的业务取舍

1. 附件中的“核心 PA”按当前聊天选中的焦点目标解释，不等于数据库 `primary` 分类。独立 `secondary` 被选中后享有完整复盘；“不单独分析次要活动”只适用于没有绑定到本次焦点的旁支活动。运行契约明确优先于旧词义冲突。
2. 实际执行与情绪效果分开。例如站桩完成十分钟但没有开心，记录“已完成活动”，情境可为 C；不伪造情绪改善，也不把已执行改成未执行。
3. 延续现有周期业务：继续原计划保留目标、计划和 M3 约定，创建下一周期；调整回同一目标的 M2 新版本。更换、暂停、结束不由模型代用户决定。
4. 不强制发明困难。没有困难可以明确记为无；用户决定回 M2 调整时，不要求硬凑“继续原计划”的策略。
5. 附件定义与运行校验分层：可编辑提示词指导自然表达；来源、周期归属、确认状态和并发版本由服务器维护，不交给模型填写。

## 字段落点

生产 `module_four_record` 的预期 20 个字段已存在，本轮不增删列、不批量重写旧记录。

| 内容 | 字段／保存方式 |
| --- | --- |
| 情境与客观完成状态 | `scenario_type`；`execution_result`：1 已完成、2 未开始无明确障碍、3 未开始有障碍、4 部分完成 |
| A 前因 | `phase_a` JSON：实际情境、触发、身体状态、情绪、已知阻碍 |
| B 行为 | `phase_b` JSON：实际活动、实际分钟数、是否开始、完成程度、有依据的内隐行为与应对；不以计划时长补实际时长 |
| C 后果 | `phase_c` JSON：真实短期情绪变化、已表达的后续意愿及有依据的功能分析；不预测长期效果 |
| 待核对 ABC | 提取逻辑名 `ai_abc_chain_summary` 映射至 V2 `abc_chain_summary` |
| 用户核对 | `chain_confirmation_status` 与 `confirmation_message_id`：服务器核验真实用户消息；不接受模型伪造 ID |
| 教育、困难与应对 | `ba_reeducation_content`、`core_difficulty_type`、`difficulty_description`、`next_coping_strategy` |
| 复盘收尾 | `review_summary`、`review_decision`；更细的继续／暂停／替换动作沿用 `pa_review_details` |
| 校验元数据 | `phase_c._m4_contract`：版本 `m4-20260917-v1`、本周期来源、证据引用、已完成步骤与待补项；不作为用户事实直接展示 |

五个依次通过的关口：执行与情境 → 用户核对 ABC → 情境化 BA 教育及理解 → 必要困难处理 → 总结与明确后续方向。

服务器校验说话人、连续原文、消息顺序与当前周期。用户纠正关键事实后撤销旧认可；较晚的教育或后续意愿补充不应误删仍有效的 ABC 认可。网页确认还核对最新提取、草稿摘要和行版本；未满足时不能直接调用接口绕过。

## 验收结果

- 完整后端：671 通过、1 跳过，1 条既有依赖弃用提示；前端类型检查和本地／线上正式构建通过。
- M4 专项：24 项通过，覆盖 A/B/C、已完成但情绪未改善、伪造身份/引用、纠正失效、顺序门控、旧周期隔离和真实保存→步骤→网页确认路径。
- 生产真实 4.1 Flash 使用合成文本验证 A/B/C：三例均返回正确情境、无缺失关口且 ABC 已核对；C 的客观执行结果仍为 1。三次提取约 7.53、5.20、5.39 秒，只是本次探针观测，不是回复延迟承诺。未创建账号或写临床记录。
- 本地合成网页：桌面 1440px、手机 390px；ABC 中文嵌套展示、未确认草稿不能确认、待补信息和模块三说明；浅色与黑金检查。公网另外核对登录／注册与静态资源。
- 生产全部 V2 预期列只读查询通过，M4 有效提示词与本次源码一致。三个服务 active，内部健康检查正常，未登录目标接口返回 401，168 个应用源码文件校验一致；发布后核查未发现后端 traceback。

证据截图位于本地 `.test-tmp/m4-ui-0917/` 和 `.test-tmp/public-release-0917/`，不上传到生产。

## 数据保护、限制与回退

- 此轮跳过通用数据库任务、知识导入；V2 启动维护关闭。未复制本地测试库、演示账号或测试数据。
- 发布 M4 提示词先检查目标库名、预期列、原配置哈希并保存私有备份，避免覆盖发布期间其他人的编辑。
- 现有未完成 M4 草稿没有新证据时需要继续聊天补齐；不默认追认历史记录。
- 引用校验保证来源和顺序，不证明心理学解释必然成立。模型提取仍需人工抽查，合成探针不等于真实用户对话或临床效果评估。
- 目录语义检索仍经过中介；检索/中介超时不会放行原始片段。该路径增加调用与成本，未上线本地向量/BGE 实验。
- 若需回退：先确认当前版本未发生后续变更，再将检索开关恢复为 `p0`、按私有备份还原本次 M4 提示词，切回上一发布 `20260916T175118Z` 并重启前后端。保留用户新记录和既有新增表/生日列，不以删库回滚。环境与提示词备份位于服务器受限的 `/etc/bacoach/`，不公开其内容。
