# 目标周期续用修复 · 2026-09-14

## 本次范围

本次只发布已实现的周期续用修复，不代表主要/次要目标、独立活动记录、长期方向等完整目标重构已经完成。

- M4 确认继续：结束本轮复盘，新建同一目标的下一周期，沿用已确认的计划和匹配的记录约定，进入 M4 等待执行，不重做 M2/M3。
- M4 确认调整：同一目标产生新计划草稿版本，复制旧版本内容作为编辑基线，保留实际存在的理解/意向/活动选择进度，重新核对计划后再执行。
- 原计划和历史复盘不覆盖；拒绝跨用户、跨目标、缺少确认约定、重复续用以及旧周期重放。
- 迟到的提取结果不能向已结束周期写入，也不能在执行中改写计划。

## 数据安全

只发布代码，不执行数据库迁移、知识库导入、提示词配置写入或本地数据库同步。生产现有数据保留。测试使用隔离 SQLite，不使用真实用户资料。

## 验收

- 发布前后端全量测试：545 passed、1 skipped。
- 前端 TypeScript 检查通过；此次没有新的前端改动。
- 新增确认端点集成测试 3 项通过，主代理独立复测通过；Terra 高推理独立审查未发现发布阻塞。
- 已发布版本：`20260914T111517Z`，可回滚版本：`20260914T020238Z`。
- 服务器前端生产构建通过；三份后端变更文件的线上 SHA-256 与本地一致。
- 发布使用 `-SkipDatabaseTasks`；服务重启期间短暂出现 502，随后发布脚本的健康检查和公网 HTTPS 检查通过。

## 仍未完成

主要/次要目标分类、长期方向、独立实际活动记录、想法与目标的证据门控、暂停/结束细化、周期频率与复盘节奏独立建模，均不在本次补丁内。
