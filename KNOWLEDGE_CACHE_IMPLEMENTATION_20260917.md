# 知识库更新一致性与精确检索缓存：实现报告

日期：2026-09-17  
状态：**已部署线上**，版本 `20260917T122211Z`，网站 https://bacoach.xyz/ 。未同步本地测试数据库，未执行生产数据库迁移或知识库导入。

## 上线补充：小型命中率指标

管理员登录后，展开左侧 **管理工具**，在底部可以看到一行 **缓存命中率**。普通用户不可见；展开前不加载指标。

- 默认只占一行，不新增图表卡片，不占用聊天正文空间；浅色与黑金主题沿用现有样式。
- 点击文字展开统计说明、有效命中数／查询数、并发合并数、缓存条目数和统计起始时间。
- 每 30 秒自动刷新，也可以点右侧刷新按钮；页面隐藏时暂停自动查询，关闭管理工具后停止查询。
- 没有查询时显示“暂无数据”，真正零命中显示 `0.0%`；关闭缓存显示“已关闭”，请求失败显示“暂不可用”，不伪装为零。
- 指标是**当前服务进程中，该缓存实例启动以来**的累计数据；不是全天、跨进程或跨重启的汇总。
- 分子为通过最终知识版本检查后实际复用的命中数。分母为命中、未命中和加入并发合并的缓存查询数之和。并发合并不算命中，版本变化被拦截的旧结果也不算有效命中；正常空结果的缓存命中计入。

新接口为 `GET /api/admin/knowledge/cache-stats`，需管理员权限，返回 `Cache-Control: no-store`。仅返回计数与统计起始时间，不返回聊天原文、用户身份或缓存键。

部署及验收：

- 前端类型检查、服务器生产构建通过；最终后端相关回归 **282 项通过，6.82 秒**，含新增的 2 项统计接口与有效命中计数测试。
- 服务器 Python **3.10.12** 实际运行合成缓存验证：首次 2 次检索调用，复用 0 次；独立实例识别更新通过。合成资料写入的仅是内存 SQLite。
- 生产 MySQL 只读索引检查成功，读取 **1,331** 个片段；未写生产知识或用户数据。未验证生产 MySQL 的并发写锁行为。
- 后端、前端与 PA 提醒服务均为 `active`，健康检查正常；公开统计接口未登录请求返回 **401**。
- 本地及正式站点前端资源通过 1440／390／360 像素浏览器检查：布局、文字对比、主题切换、展开交互、刷新、空状态、关闭状态、错误重试、横向溢出均已检查。浏览器验收使用拦截的合成管理员与统计响应，没有创建生产账号或伪造生产统计。
- 回滚参考：前一版本为 `/opt/bacoach/releases/20260917T113924Z`，新版本目录为 `/opt/bacoach/releases/20260917T122211Z`。

实现文件：`frontend/components/KnowledgeCacheMetric.tsx`、`frontend/components/ConversationSidebar.tsx`、`backend/app/routes/admin_knowledge.py`；验证脚本：`infra/qa/knowledge-cache-metric.cjs`、`infra/deploy/verify_knowledge_cache.py`。

## 一、先看结论：这次完成了什么

本次完成两个部分：

1. **管理员更新知识库后的缓存一致性**：不再只靠“更新所在进程清一下缓存，其他进程等 5 分钟”。每个检索进程会检查数据库中的知识版本，发现变化就重建索引并清除旧检索结果。
2. **Exact Retrieval Cache（精确检索结果缓存）**：同一用户、同一会话、相同模块、相同有效查询与检索配置，可以复用之前检索到的片段，减少重复检索。

同时加入：LRU 容量限制、TTL 过期、Single-flight 并发合并、取消隔离、故障不缓存、结构化监控和回归测试。

**白话版：**我们给知识库加了一个“先核对教材版本，再翻之前查找记录”的机制。教材改过，旧查找记录就作废；条件完全一样，才使用上次找到的资料。但这份资料仍然要交给中介节点重新审阅，不能直接拿去回答用户。

本轮没有新增 Redis，没有新增数据库表，没有修改用户档案、目标或每日记录数据，也没有缓存中介结论或最终回复。

## 二、与原来有什么区别

| 项目 | 原实现 | 本次实现 |
|---|---|---|
| 索引缓存 | 内存索引，5 分钟 TTL | 保留；每次检索还检查共享知识版本 |
| 管理员更新 | 清除当前进程索引 | 提交成功后清除当前进程缓存；其他进程在下一次检索时识别新版本 |
| 重复导入完全相同内容 | 仍清除当前进程索引 | 不清缓存，保留已有缓存 |
| 相同检索重复请求 | 重新检索 | 有身份和会话范围时，允许精确命中 |
| 同时到来的相同检索 | 各自检索 | 同一进程、同一缓存键合并计算 |
| 正常无相关资料与检索故障 | 对外都可能表现为 `[]` | 内部区分；正常空结果可短暂缓存，故障不能缓存 |
| 缓存命中后的中介审核 | 无检索结果缓存 | 仍逐次执行，不复用上次的放行决定 |
| 检索故障的监控 | 可能被记录为普通空结果 | 标记为错误，并提供具体检索状态 |

## 三、管理员更新知识库：实现方式

### 3.1 Corpus Revision：共享知识版本指纹

新增 `knowledge_revision()`，从现有表生成 SHA-256 版本指纹。输入包含：

- 知识源 ID、名称、类别、正文 `content_hash`、更新时间；
- 各知识源的片段数量、最大片段 ID；
- 固定排序，避免数据库返回顺序不同造成误判。

不会在每次检查时读取全部片段正文，也不会新建版本表。片段统计使用现有知识片段表；这是数据库查询，不是零成本操作。

加入片段数量和 ID 的原因：部分 MySQL 时间字段只有秒级精度，短时间内更新多次可能产生相同时间；即使正文恢复到之前的版本，重新生成的片段 ID 也可能不同，旧结果不能继续引用旧 ID。

**白话版：**不仅看书的名字和修改日期，还检查“内容指纹”和“页码是否换过”，防止误把重新排版的书当成原来那一本。

### 3.2 Transaction 与 Cache Invalidation：提交成功后才失效

管理员导入继续采用一个数据库事务更新知识源及片段。对已有知识源的更新增加 `SELECT ... FOR UPDATE` 行级锁，减少同时替换同一知识源时的交错写入。

执行顺序是：

1. 写入新知识及片段；
2. `commit` 成功；
3. 内容确实变化时，清除当前进程索引、目录、排序器和结果缓存；
4. 返回导入结果。

事务提交失败不会提前清缓存。内容及类别完全没变时，保留现有缓存。

**白话版：**等新教材真的保存成功，再通知大家旧教材作废；如果保存失败，或者只是重复上传同一本教材，就不必让大家重查一遍。

### 3.3 多进程与检索途中的更新

各进程在使用索引之前读取版本指纹；命中缓存也不能省略这项检查。

重建索引时，前后分别检查版本，避免拼出一个“半旧半新”的索引。若期间有更新，最多重试一次；持续变化则本轮不提供知识，避免无限重试。

检索结束、返回片段前再检查一次：如果模型检索期间知识库发生变化，这轮旧快照会以 `withheld_corpus_changed` 被拦下，下一次请求再查新版本。

**例子：**用户开始检索时资料是 A 版，检索模型还没结束，管理员发布 B 版。这轮不会把已经过期的 A 版检索结果直接传给中介。

边界：这是检索前后的版本校验，不是锁住整个聊天过程。已经通过检查并进入后续生成、或已经发给用户的回复，不会因为后来更新知识而被追溯撤回。

## 四、精确检索缓存：实现方式

### 4.1 缓存放在哪里

```text
意图门控
    ↓ 需要知识
核对数据库知识版本
    ↓
精确缓存查询 ── 命中 ──→ 复用候选片段
    │ 未命中                  │
    ↓                         │
执行实际检索，成功才缓存 ────────┘
    ↓
再次核对知识版本
    ↓
当前中介审核 → 模块回复 → 共享 Validator
```

支持 `p0`、`enhanced` 和 `catalog` 三条检索路径。对 `catalog` 而言，非快捷路径通常需要一次章节选择和一次候选片段判断；命中时可以省去这两次检索模型调用。

这不是向量数据库，也不是新增 Embedding；本轮没有改变检索算法或相关性阈值。

### 4.2 Cache Key：什么才叫“完全一样”

缓存键包含：

| 条件 | 白话解释 |
|---|---|
| 用户、会话、沙盒标记 | 不把另一个人、另一段聊天或测试环境的结果混过来 |
| 模块、`top_k` | 当前模块不同、要返回的片段数量不同，就重新查 |
| 完整有效查询 | 比较真正交给检索器的内容，而不是只看最后一句话 |
| 知识版本、索引代次 | 教材或索引已换，就不用旧结果 |
| 排序模式、策略参数、模块类别规则 | 检索规则变了，就重新查 |
| Catalog 提示词、版本、Provider、模型及路由配置 | 检索模型或指令变了，就重新查 |

“完整有效查询”沿用现有 `_knowledge_query()`：当前输入、最近用户消息和 PA 卡片，整体仍有原有的 6,000 字符上限。本轮没有扩大检索上下文。

不做语义相似匹配，也不删除空格、标点、否定词或数字进行宽松匹配。即使只改变一个影响语义的字，也不会沿用相同键。

**例子：**“可以每天做吗”前面讨论的是散步，或前面讨论的是高强度训练，因为上下文不同，不会命中同一份缓存。

没有明确用户身份或会话范围的调用不使用结果缓存。普通 `search()` 接口保留兼容性、默认绕过结果缓存，现有离线评估脚本不会因为这次改造悄悄复用之前的评估结果。

### 4.3 TTL、LRU 与容量控制

采用进程内存缓存，不写入 Redis、文件或数据库。

| 设置 | 默认值 | 含义 |
|---|---:|---|
| 成功且有片段的结果 TTL | 300 秒 | 最长缓存 5 分钟 |
| 成功但无相关片段的结果 TTL | 45 秒 | 短暂记住“这次确实没找到” |
| LRU 最大条目数 | 256 | 超出时淘汰最近最少使用的条目 |
| 缓存载荷预算 | 8 MiB | 过大条目不缓存，整体超预算时淘汰 |

TTL 从检索成功完成后计算。知识更新、索引 5 分钟刷新、LRU 淘汰、进程重启都可能让结果更早失效；TTL 不是保证保留这么久。

8 MiB 是按文本编码大小及条目开销估算的缓存载荷预算，不是整个 Python 进程的内存上限。索引本身、Python 对象开销和正在执行的请求也会占内存。

缓存内容是不可变的公共知识片段快照及检索状态，保留原有片段顺序。不是用户的个性化回答。

**白话版：**查找记录有保质期，记录本也有页数上限；太旧、太大或很久没用的记录会被丢掉，不会无限堆积。

### 4.4 Single-flight：合并重复请求，同时处理取消

同一进程同时收到相同缓存键时，共用一次检索。

- 其中一个等待者取消，不会误取消其他等待者需要的检索。
- 最后一个等待者也取消时，取消本地共享任务，不再继续等结果或写入正常缓存。
- 清缓存后，旧任务不能重新填入新一代缓存，新请求也不会加入旧任务。
- 进行中请求登记达到上限时，新键绕过合并，不继续扩大登记表；这不是全局限流器。

取消本地任务不等于上游模型供应商必定已停止计算或不计费，仍取决于供应商行为。

### 4.5 成功的空结果不等于失败

新增 `SearchResult`，明确区分片段、状态、是否可缓存及模型调用数。

| 情况 | 是否缓存 |
|---|---|
| 成功检索并找到片段 | 是 |
| 成功解析，模型判断没有适用资料 | 是，45 秒 |
| 超时、服务异常 | 否 |
| 非法 JSON、编造片段 ID、输出截断 | 否 |
| 数据库版本检查失败 | 不使用旧缓存，本轮不提供知识 |
| 检索期间知识库变更 | 拦截旧快照，不供本轮使用 |

**例子：**供应商恰好超时一次，不会让接下来几分钟内所有相同问题都被误认为“知识库没有这方面内容”。

## 五、中介安全与隐私边界没有放宽

1. 命中缓存后，依然调用当前中介节点，使用当前提示词及当前可用的用户安全上下文。
2. 用户新增身体限制等信息时，中介重新判断；不会复用此前“可以放行”的结论。
3. 中介超时、失败或不批准时，不能把缓存里的原始片段直接交给回复模型。
4. 缓存键使用带进程随机密钥的 `HMAC-SHA256`。不把用户原文、身份或明文上下文放进缓存键，也不在新增缓存日志中输出这些内容。
5. 不跨用户或会话共享检索结果；不缓存最终回复、中介推理或临床判断。

白话版：缓存只是“我之前找到哪些资料”，不是“我已经判断这些资料适合你”。第二个判断每次都要重新做。

## 六、测试结果与能证明的收益

### 6.1 验证结果

- 本次新增缓存与一致性测试：**51 项通过**。
- 连同原有检索、知识库接口、意图门控、中介、知识片段快照及 Graph 回归：**280 项通过**，本次运行耗时 **7.12 秒**。
- 8 个修改／新增 Python 文件通过 Python 3.10 语法解析检查。
- 1 条现有 Starlette / AnyIO 弃用警告；不是测试失败。

环境为本地 Python 3.12、隔离 SQLite 和合成模型 Provider。Python 3.10 检查是语法兼容检查，**不是在生产 Python 3.10 + MySQL 上完整跑过这些测试**。多进程一致性通过共享测试数据库的独立检索实例模拟验证；MySQL 行锁的真实并发行为还应在后续预发布环境验收。

重点覆盖：

- 两个独立检索实例在没有互相调用 `invalidate()` 的情况下识别知识更新；
- 重复导入、事务回滚、类别变化、源删除、新增知识源；
- 检索中更新、索引重建时版本连续变化、数据库版本查询失败；
- 用户／会话／模块／历史上下文／模型／提示词等变化时不误命中；
- 正常空结果的短 TTL、失败不缓存、LRU、容量、并发合并与取消；
- 缓存命中仍调用中介，新增安全信息进入中介，中介超时不放行原始知识。

### 6.2 合成测试中的可核实对比

以下是一次正常两阶段 Catalog 检索的调用数，**不包含中介、模块回复及其他模型调用**：

| 场景 | 原方式的检索调用数 | 新方式的检索调用数 |
|---|---:|---:|
| 第一次完整查询 | 2 | 2 |
| 有效期内再次提交完全相同查询 | 2 | 0 |
| 两个相同请求并发，共计 | 4 | 2 |

测试确认缓存命中的片段内容、ID 与顺序等于首次检索结果。该测试证明复用机制正确，**不证明首次检索结果一定准确**。

本轮没有调用真实付费模型，没有跑线上压测，没有重算 Holdout 指标，也没有声称准确率或整体等待时间已经提升多少。测试耗时 7.12 秒是最初测试套件执行时间，不是网页回复耗时；加入小型统计显示后的最终回归见报告开头。

## 七、上线后怎么监控

每轮在 `telemetry.retrieval.exact_cache` 内记录诊断，同时输出结构化 `retrieval_cache` 服务日志。

| 字段／状态 | 用途 |
|---|---|
| `cache=hit` | 精确缓存命中 |
| `cache=miss` | 本次需要实际检索 |
| `cache=coalesced` | 等待同一键正在进行的检索 |
| `bypass_no_scope` / `bypass_disabled` / `bypass_capacity` | 没有隔离范围、关闭缓存或达到合并登记容量 |
| `status=withheld_on_error` | 检索故障，不应当作正常“无资料” |
| `withheld_corpus_changed` | 本轮检索期间资料已更新 |
| `withheld_revision_error` | 无法确认资料版本，拒绝使用旧缓存 |
| `duration_ms` | 本次检索含版本检查、缓存等待的耗时 |
| `model_calls` | 本次请求归属的检索模型调用尝试数 |
| `saved_model_calls` | 按被复用结果估算省掉的检索调用数，不是实际账单金额 |
| `cache_stats` | 当前进程命中、未命中、合并、淘汰、条目数及载荷等计数 |

建议观察：

1. 命中率：有效请求中 `hit` 的比例；并发合并率单独计算。
2. 命中／未命中的检索耗时 p50、p95，分别统计。
3. 完整聊天的首个回复与最终回复耗时，不只看缓存自身。
4. 版本拦截、故障率、缓存淘汰率和进程内缓存大小。

状态为错误或被拦截的结果不要算成“有效缓存命中”。`cache_stats` 为进程内累计值，重启会重置；不能把每条日志里的累计计数再次相加。发生请求取消时，实际发起的 Catalog 调用应结合独立 `catalog_retrieval.model_calls` 日志核对，不能只汇总完成请求的归属值。

最初实现提供日志和本轮诊断数据，后续按需求补充了管理工具中的紧凑命中率指标，见报告开头；没有新增大图表仪表盘。

## 八、配置、局限与下一步

### 8.1 配置与关闭方式

已在 `.env.example` 加入：

```dotenv
KNOWLEDGE_RESULT_CACHE_ENABLED=true
KNOWLEDGE_RESULT_CACHE_TTL_SECONDS=300
KNOWLEDGE_RESULT_CACHE_EMPTY_TTL_SECONDS=45
KNOWLEDGE_RESULT_CACHE_MAX_ENTRIES=256
KNOWLEDGE_RESULT_CACHE_MAX_BYTES=8388608
```

配置在后端启动时读取，修改后需重启对应进程。设置 `KNOWLEDGE_RESULT_CACHE_ENABLED=false` 可关闭结果缓存；知识版本检查、原有索引缓存和中介审核仍保留。

### 8.2 当前刻意保留的边界

- **结果不跨进程共享。**多个进程可能各自首次查询一次，但知识更新的一致性检查不依赖同一进程。以后并发量增加，再考虑 Redis 共享结果。
- **严格匹配会限制命中率。**真实聊天的上下文不断变化，因此不能保证每一轮都变快；这是本轮优先保证隔离和正确性的取舍。
- **每次检索新增版本检查开销。**正常有前、后两次轻量数据库查询；冷加载或更新还需读取片段。对原本很快的词法检索，数据库开销甚至可能抵消缓存收益，要通过线上指标判断。
- **直接改数据库片段正文有边界。**如果绕过管理员导入，直接用 DMS 改 `knowledge_chunks.content`，又不更新知识源哈希／更新时间、片段数量和 ID 也不变，版本指纹不会立刻发现。已有 5 分钟索引 TTL 提供下一次检索时的刷新兜底。需要即时生效的变更应走管理员导入／统一导入函数，不能把本机制当作任意 SQL 编辑的实时监听器。
- **不做语义缓存。**“膝盖不痛”和“膝盖疼”不能因为句子相似就复用相同检索结论。
- **不保证准确率上涨。**缓存减少重复计算，不修正第一次检索本身可能选错的资料。

### 8.3 文件与职责

| 文件 | 本轮职责 |
|---|---|
| `backend/app/knowledge_store.py` | 共享知识版本指纹、已有知识源更新的行级锁 |
| `backend/app/routes/admin_knowledge.py` | 提交成功且内容变化后才清本进程缓存 |
| `backend/app/retrieval_cache.py` | 精确键、LRU、TTL、容量、Single-flight 和取消管理 |
| `backend/app/retrieval.py` | 前后版本校验、统一缓存入口、隔离和诊断 |
| `backend/app/catalog_knowledge_base.py` | 区分成功空结果与故障、检索模型和提示词指纹 |
| `backend/app/graph/nodes.py` | 传入用户／会话隔离范围、保存诊断，不改变中介顺序 |
| `backend/app/config.py`、`backend/.env.example` | 配置开关和限制 |
| `backend/tests/test_retrieval_cache.py` | 本轮新增 51 项验证 |

本次已部署并完成报告开头所列上线验证。下一步观察真实命中率、p95 及完整回复耗时；真实 MySQL 多进程并发写入与真实模型延迟仍需另外验证。没有足够命中收益时，不应仅为“有缓存”继续增加复杂度。
