# V2 上线状态与迁移演练记录

更新时间：2026-09-11（北京时间）。**当前状态：V2 已切换生产，后端与网页已适配。**

## 正式发布结果

- 库仍为 ba_coach_260908；运行版本 `/opt/bacoach/releases/20260910T154355Z`；启用 DATABASE_SCHEMA_VERSION=v2。
- nginx、前后端均 active；公网首页和 /api/modules 返回 200，未登录 /api/auth/me 返回预期 401；内部健康检查通过。
- 16 张 V2 表进入生产，7 张重名旧表归档未删除。user_profile 现为 12 字段，支持者保存到 user_supporters。
- 最新数据核对：23 份档案、16 个账号、34 段聊天、688 条消息。全部档案和账号读取成功，聊天运行状态缺失为 0。
- 正式备份：`/opt/bacoach/backups/before-full-v2-20260910T155432Z`，含 SQL、验证记录、原配置及回滚材料。

### 实现与切换

暂停入口和服务后重新备份，从实际 SQL 恢复隔离库并转换最新数据；对全部 30 张原表逐行摘要核对，再以原子重命名切入生产。切换配置和版本，先验收后端及前端，再开放入口。没有用旧演练快照覆盖新数据。

档案改为规范化读写；每段聊天选择一个目标，周期在 planning 阶段建立，M2—M4 步骤按周期隔离。模型提出草稿，网页确认携带记录摘要和运行状态版本，后端检查归属及版本后推进，避免过期确认。历史 M1 完成直接复用，新 M1 需要真实确认。

网页新增目标与进度面板：选择或新建目标、查看草稿、勾选确认。聊天删除保留业务历史。知识库、每日记录、测试工作台和提示词数据保留。

### 验证和边界

- 后端全量回归曾运行 451 通过、1 跳过；之后少量补丁另经 V2 冒烟检查，该全量结果不代表覆盖每个最终补丁。
- V2 API 7 个场景在独立环境及隔离 MySQL 通过：注册与三个支持者、M1 确认及过期拒绝、历史 M1 复用、M2/M3/M4 流转、多目标和删除聊天保留历史。
- 手机尺寸模拟接口网页测试通过目标选择、勾选限制和确认流转，无横向溢出或脚本报错。
- 隔离 MySQL 16 表正向切换与反向恢复通过摘要核验。
- 生产只作读取和健康验收，没有生产测试账号；未声称完成真实 LLM 全流程或多设备长时压力测试。

迁移保留 36 项待核对事项，主要涉及年龄来源、旧确认依据、计划关联和聊天目标选择，不等于 36 个运行故障。没有伪造历史证据。支持者 API 上限 10；可选人口学编辑、通知调度、记忆管理页、替换目标自动关联仍有后续范围，见 DATABASE.md。

开放入口后可能已有新写入，不能直接执行旧回滚 SQL 而忽略新数据。

### 查看方式

DMS 中刷新 ba_coach_260908 的表列表，重新打开 user_profile，应见 12 字段；支持者在 user_supporters。legacy_* 是归档。网页刷新后查看聊天上方目标与进度面板。完整字段见 DATABASE_LIVE_V2_20260910.md。

---

## 以下为正式切换前的历史记录

**下方“尚未完成”和旧数量仅反映早期阶段，已由上述正式发布结果取代。**

## 为什么此前仍是旧表加扩展

此前代码将 7 张业务表视为外部系统所有，禁止应用自动改表，因此通过侧表保存支持者列表、会话步骤和周期关联。这个方案属于兼容旧系统，不等于用户明确要求的整份 V2。改库名也不等于完成数据模型迁移。用户已经批准整份 V2 后，没有重新处理此约束、完成模型替换，并明确报告未完成范围，是执行和交付说明的问题。

目前生产仍为 `ba_coach_260908`，运行版本 `/opt/bacoach/releases/20260909T142523Z`。本轮未修改生产数据、未切换服务、未重启服务。

## 本轮已经完成

1. 新建一致性备份，并从实际 SQL 备份文件恢复到隔离数据库；逐表比较行数和逐行摘要，30 张表验证一致。
2. 明确定义独立 V2 数据结构，共 16 张表。该结构不参与应用启动时自动建表，不能意外修改线上数据库。
3. 在独立 MySQL 数据库运行转换演练，旧表归档保留，新增 V2 表成为演练数据库的目标结构。演练不是发布。
4. 实现并测试 V2 事务层：目标归属检查、同一用户多目标、先建 planning 周期、周期进度隔离、计划版本追加、确认消息归属检查、目标与周期绑定检查、执行前契约检查，以及记忆去重和替代。
5. 新增 14 项结构及事务层测试，全部通过；后端全量回归为 **441 通过、1 跳过**。补强归属与记忆并发检查后，14 项 V2 测试再次全部通过。现有聊天和网页端到端的 V2 验收尚未完成，不能用这些测试替代。

### 恢复验证

- 备份目录：`/opt/bacoach/backups/before-full-v2-20260910T023310Z`
- 恢复验证库：`ba_coach_v2_restore_20260910t023310z`
- SQL SHA-256：`57389343d936380c31acc3dfe2040acca62c950158a365c6f6067620a501192a`
- 私有备份目录权限 700、SQL 和校验结果文件权限 600；没有把用户正文或凭据下载到报告中。
- 主要核对数量：21 条档案、14 个账号、32 段聊天、678 条消息、1331 个知识片段。
- 此快照创建后生产可继续写入；正式切换必须暂停写入并重新备份、转换与核对，不可以直接把演练库覆盖到生产。

### 演练结果

演练库：`ba_coach_v2_rehearsal_20260910t024139z`。

| 目标表 | 转换数量 | 说明 |
|---|---:|---|
| user_profile | 21 | 不从过期的年龄推算出生年份 |
| user_preferences | 21 | 氛围与活动强度分离 |
| user_supporters | 2 | 独立明细行，不再使用 supporter1 / supporter2 槽位 |
| user_activity_constraints | 39 | 来源和确认状态明确；历史导入尚不冒充新确认 |
| module_one_record | 4 | 缺少确认消息的记录作为草稿导入 |
| user_module_one_state | 21 | 不把旧完成标记伪造为已确认版本 |
| pa_goals | 3 | 2 个历史周期候选目标、1 个未关联计划候选目标；不是 3 个经用户确认的独立目标 |
| module_two_record | 1 | 版本 1 草稿，等待核对目标归属和确认 |
| pa_cycles | 2 | 保留原周期 ID，待核对计划绑定 |
| pa_cycle_progress | 2 | 不复制未经核对的历史步骤为已完成 |
| conversation_runtime_states | 30 | 保留原模块和记忆；缺少明确目标的后续模块暂挂待核对 |
| module_three_record / module_four_record | 0 | 原库也均为 0 |
| ba_memory / ai_decision_logs | 0 | 不从旧摘要虚构已确认记忆或决策证据 |
| v2_migration_issues | 34 | 明确记录待核对项，不静默丢弃或猜测 |

34 条待核对项包括：13 条年龄保留但出生年份未知、4 条 M1 旧完成标记缺少确认依据、4 条 M1 记录导入为草稿、2 条周期缺少可靠计划绑定、1 条未关联计划、10 段聊天需要明确选择目标。并非 34 个用户，也不意味着应强制用户重新完成全部流程。

## 必须完成、目前尚未完成

- 生产后端切换到 V2 的完整读写路径：注册、档案、支持者、约束、聊天运行态、临床抽取、Router、长期记忆、删除与审计。
- 网页支持每段聊天明确选择目标、复用已确认 M1、目标版本与周期展示，以及必要的历史数据补确认。
- 处理后台抽取与路由先后关系：不能在计划或契约尚未落库确认时提前进入执行状态。
- 原有档案中的安全限制在迁移后的连续生效策略，必须在聊天验收中验证，不能因导入状态变化而丢失保护。
- 未明确目标的历史会话续聊策略；不能把演练中的暂停状态直接部署，让被试无法继续而不提供解决入口。
- MySQL 上完整业务事务测试、网页端到端测试、重启与多设备并发测试、回滚演练。
- 正式停写窗口的新快照、迁移、逐表核对、切换与线上验收。
- DMS 展示与最终字段文档同步更新。演练库存在 V2 表不等于生产 DMS 已完成适配。

## 已确认的历史 M1 处理

2026-09-10 用户明确选择第一种方案：**直接沿用旧完成标记，不增加确认步骤，不要求重做 M1**。

- 旧完成标记为真：导入 `status=completed`、`completion_source=legacy_imported`、`evidence_status=missing`。
- 旧完成标记为假：保持 `in_progress`，不能因为存在一条 M1 记录就视为完成。
- 不虚构 `confirmed_formulation_id`，不自动填满 `completed_steps`，旧 M1 记录仍保留真实导入状态。
- V2 新聊天初始模块选择函数允许历史完成状态从 M2 继续；`revisit_needed` 不自动使用户返回 M1。
- 此迁移例外只用于旧数据导入；新完成状态仍需要真实确认版本和证据。
- 对应 4 条迁移核对项记录为“按产品决定解决”，而不是继续要求用户补确认。

此决策已经落实到本地迁移脚本、V2 数据约束和初始模块选择逻辑，相关 **24 项测试通过**。生产后端完整接线和网页适配仍须验收，不能据此认定整版已上线。

更新后的 MySQL 演练库为 `ba_coach_v2_rehearsal_20260910t075319z`。转换事务提交成功，核对结果为 **4 个旧 M1 完成状态完整保留**，均标记 `legacy_imported + missing`，确认版本为空。生产库未改动。上文较早演练中 4 条“完成依据待核对”现已按此次决定解决，不再作为等待产品确认的事项。

## 验收底线

只有生产结构、实际读写路径和网页入口全部切换，且数据核对与功能验收通过，才可以报告“V2 已上线”。保留旧表是为了迁移证据与回滚，不是让旧表继续作为新业务权威模型。
