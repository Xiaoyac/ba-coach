# 每日记录单页调整 · 本地管理员预览

## 设计与范围

保留温暖浅色/黑金主题，不改评分范围、默认值、后台字段及保存接口。活动的 0–5 分与整日的 0–10 分仍分别标明。
取消两步向导。桌面同页双栏呈现活动与整日回顾，手机同页纵向滚动；弹窗按内容占用高度，不再强制填满大屏。
只使用本地 `ui-preview-0917.db`，不部署、不同步测试数据。已登录管理员演示账号 `localadmin`。

## 改动

- 去掉「1/2」「上一步」「下一步」，统一「保存今日记录」。
- 添加/删除活动仍保留，空白活动不保存，历史/返回不会丢失当前未提交的填写。
- 修正半选时间、相同/倒序时间可能通过前端校验的问题。调整开始时间后，不合法的结束时间需要重新选择。
- 保存时禁用编辑/关闭并阻止重复提交；失败保留填写并支持重试。
- 使用原生模态对话框管理键盘焦点与 Escape；时间选单的 Escape 只关闭选单。
- 评分支持方向键，选单支持上下键；取消多余框线、减小间距、增大关闭等点击区域，将「social 联结」改为「人际联结」。
- 时间选单根据表单内的可用空间向上/向下展开，并限制内部列表高度；定位当前时间只滚动选单，不再把整个表单带走。手机第二项活动靠近底部时，也不会被保存栏遮住。

## 验收清单

- 桌面与手机：两部分在同一表单；无页码/下一步；初始、长内容、历史、浅色/黑金截图。
- 活动增删、时间完整/不完整/倒序、五种单项分数、五种整日分数、可选备注。
- 真实本地保存并从历史读取，核对评分、活动与备注未丢失。
- 异常保存与重试；填写后切历史再返回；保存期间不能重复提交。
- Tab 焦点范围、时间菜单 Esc、整个弹窗 Esc、手机 footer 可达、无横向溢出。
- 前端类型检查/构建、后端每日记录相关测试。

## 实际验收结果

2026-09-17，仅对本次单页调整验收；未把之前其他功能的测试数量计入本次结果。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过；Next.js 构建及页面生成成功 |
| `tests/test_assessment_history.py` | 4 passed；1 条现有 Starlette/anyio 弃用提醒 |
| 管理员身份 | 正常登录本地 `localadmin`，账号菜单显示管理员控制台、权限/提示词/反馈管理入口 |
| 单页结构 | 桌面双栏、手机纵排；无上一步/下一步或 1/2 页码 |
| 评分与活动 | 五项 0–5 活动评分、五项 0–10 整日评分、备注、增删活动均可用；空白活动不发送 |
| 时间校验 | 空值、半选、变更开始导致倒序时不可保存；补全有效范围后恢复；选单支持键盘操作 |
| 异常与重试 | 模拟 HTTP 503，显示错误且保留输入；重试期间编辑/关闭/提交禁用，Enter/Escape 不重复提交或误关弹窗 |
| 本地真实保存 | POST 返回 201；从网页历史和返回内容核对全部 10 项评分、活动、时间和两种备注一致 |
| 草稿保留 | 查看历史再返回，尚未提交的活动、评分、备注保留 |
| 键盘 | Tab 不进入背景页面控件；焦点在时间选单内时 Escape 只关选单，再次 Escape 关闭每日记录；原生弹窗允许正常切至浏览器工具栏 |
| 桌面视觉 | 1440×900 和可见 Chrome 原生窗口检查；浅色/黑金均正常，无多余分页空白、重叠或裁切 |
| 手机视觉 | 390×844、360×740 浏览器模拟；无横向溢出，记录/回顾可滚动到达，保存栏保持可见，第二项活动选单遮挡已修复并复验 |

视觉检查覆盖：标题层级、字色与选中状态、双栏比例、卡片与控件间距、触控区域、主题一致性、滚动边界和弹出层遮挡。

### 截图证据

保存在本地 `.test-tmp/ui-qa-0917/`，不发布测试资料：

- `daily-desktop-light.png`、`daily-desktop-dark.png`：桌面单页两主题。
- `daily-mobile-light-top.png`、`daily-mobile-light-summary.png`：手机同页上部与整日回顾。
- `daily-mobile-picker-fixed.png`、`daily-mobile-small-picker.png`：多活动/小屏时间选单修复。
- `daily-history-desktop.png`：保存后的历史记录。

## 修改文件与使用方式

- `frontend/components/DailyAssessmentModal.tsx`：布局、校验、保存状态、时间选单和键盘交互。
- 本文：调整说明和验收结果。

本地预览：`http://localhost:3000/`。可见 Chrome 已保留管理员登录，打开「记录今日」即可查看。演示账号 `localadmin` / `LocalPreview0917!` 仅存在于隔离的本地测试库，不能用于线上登录。

## 边界

- 未部署线上，未上传或同步数据库；后端接口及表结构没有因本次修改而改变。
- 手机测试是浏览器尺寸/触控模拟，不等同于实机软键盘和 Safari 验收。
- 本地预览没有连接真实 LLM；本次验证覆盖界面及每日记录保存，不宣称验收模型对话质量。
- 时间仍沿用现有整小时、同日范围，不在本次调整中新增分钟或跨午夜记录。
