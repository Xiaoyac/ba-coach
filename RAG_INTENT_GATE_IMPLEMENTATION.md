# RAG 意图门控：实现、评测与监控

日期：2026-09-08。规则版本：`rules-v1`。

## 本次调整概览

1. 在四个模块调用知识检索之前增加本地规则门控；没有新增 LLM、embedding、网络请求或依赖。
2. 对明确的问候、结束聊天、无业务上下文的确认语和少量账户操作跳过检索；不确定的消息继续检索。
3. “继续”“可以”“那个呢”不直接拦截；“好的”遇到前文、目标卡、临床上下文等仍放行。
4. 跳过只影响共享知识库。主回复、原消息、业务 Router、用户档案、记忆和已有风险检查仍按现有流程处理。
5. 新增开关、原因代码、结构化日志与 SSE trace；可区分门控跳过、检索为空、命中与检索异常。
6. 新增可重复评测脚本、40 条门控边界样例、模块集成测试及日志汇总功能。

交付状态：代码已接入本地实际聊天模块，完成本地验证。本次没有发布服务器、迁移数据库或替换线上检索。向量/混合检索仍是本地实验。之前完成的生产库改名与本功能相互独立。

## 实现方式

### 在链路中的位置

```text
加载会话、档案和记忆 → 已有风险检查
  → 正常业务模块
      → 当前消息的本地意图门控
          → 跳过：知识片段为空，继续构造 Prompt 并回复
          → 放行：原查询构造 → 原检索算法 → 原筛选 → Prompt → 回复
  → 已有模块流转和后续处理
```

当前生产型检索实现是 P0。门控接在 `knowledge_base.search()` 调用方，因此将来选择向量或混合实现也能复用，不必给每种检索重复写规则。

`app/retrieval_intent.py` 的 `decide_retrieval()` 返回 `retrieve`、`reason` 和 `version`。`app/graph/nodes.py` 在实际检索前调用它，跳过时不构造拼接查询、不调用知识库，也不生成查询向量。

### 具体判定

| 输入和上下文 | 决策 | 原因代码 |
| --- | --- | --- |
| `你好！`，即使旧对话提过运动 | 跳过 | `greeting` |
| `收到`，没有业务上下文 | 跳过 | `standalone_acknowledgement` |
| `嗯嗯，明白了`，没有业务上下文 | 跳过 | `standalone_acknowledgement` |
| 前文是“要不要详细解释两分钟规则？”；现在说`好的` | 检索 | `contextual_acknowledgement` |
| 当前有 PA 目标卡；现在说`谢谢` | 保守放行 | `contextual_acknowledgement` |
| `谢谢你，今天先到这里` | 跳过 | `explicit_pause` |
| `在哪里修改我的昵称？` | 跳过 | `account_navigation` |
| `继续`、`可以`、`那我现在怎么办？` | 放行 | `substantive_or_unknown` |
| `你好，我总是迈不开第一步怎么办？` | 放行 | `substantive_or_unknown` |
| `我想修改昵称，也想了解行为激活` | 放行 | `substantive_or_unknown` |
| `谢谢，但我不想活了` | 门控本身放行；仍由前置风险链处理 | `substantive_or_unknown` |
| `英雄联盟更新失败` | 放行，交给现有检索相关性筛选 | `substantive_or_unknown` |
| 关闭功能开关 | 全部放行 | `disabled` |

只做整句匹配，不能因为一句话包含“谢谢”“密码”“退出”就把整个请求拦住。问候和确认使用有限词表，账户操作使用完整句式匹配。统一全角/半角、英文大小写和空白，去掉边缘的常见陈述标点；保留内容中的否定、引号和问句线索。超过 160 字符的输入直接放行，防止复杂长消息被当成社交用语。

确认语的上下文保护检查最近六条消息（包括助手消息），以及业务记忆、临床上下文、长期记忆和已完成步骤。非空业务状态会保守放行。`turn_count`、`last_module`、`sandbox_mode` 等纯运行标记以及全空步骤列表不单独算业务上下文。放行后，仍沿用原来的“当前输入 + 最近两条用户消息 + PA 卡”查询，最多 6000 字符。

### 开关与恢复

```dotenv
KNOWLEDGE_INTENT_GATE_ENABLED=true
```

代码默认开启。将实际后端环境文件中的值设成 `false` 并重启工作进程，会恢复普通模块每回合都执行原检索的行为，日志原因显示 `disabled`。这不是切回旧类别配额，也不会调整 P0 的三道阈值。

## 评测方法与结果口径

评测复用同一批 1,331 个知识片段、相同模型、相同 collection、相同模块范围和 Top-K；原有标签没有修改。对每个问题分别实际执行门控关闭和开启两条路径，每条路径重复三次，交替执行顺序。先完成模型和索引预热，再计时。允许放行的查询必须得到相同 ID、来源和分数，否则报告检查失败。

三个检索实验分别是 P0、纯向量和 RRF 混合。时延包括规则判断（开启侧）、查询构造及实际检索；不含模型加载、知识库装载、数据库读取、生产日志写入、主模型生成和网络往返，因此不是用户看到回答的端到端延迟。最终计时在回归测试完成后单独运行，减少 CPU 竞争。

评测样本：原 Dev 44 条、原 Holdout 44 条、额外语义挑战 12 条，共 64 条正例和 36 条空结果对照。另有 40 条只用于检查门控行为的边界用例，不混进知识相关性统计。

**此前已经查看过 Holdout，本轮只能称为历史回归集，不是新的盲测。全部标签仍需业务专家审核。** 三次重复用于看时延波动，不能当成三倍独立样本。最终数据保存在 `backend/evals/intent_gate_report.json`，包含逐题结果、全部汇总、P50/P95、语料/数据集/代码哈希和模型指纹。

### 实测质量指标（关闭 → 开启）

| 集合 / 方法 | 正例 Hit@K | 正确返回空 | 非空列表平均精度 | 正例平均精度 |
| --- | --- | --- | --- | --- |
| Dev / P0 | 92.86% → 92.86% | 81.25% → 81.25% | 47.04% → 47.04% | 52.08% → 52.08% |
| Dev / 向量 | 42.86% → 42.86% | 62.50% → 81.25% | 15.86% → 17.56% | 19.67% → 19.67% |
| Dev / 混合 | 92.86% → 92.86% | 50.00% → 68.75% | 31.94% → 34.85% | 41.07% → 41.07% |
| Holdout / P0 | 92.86% → 92.86% | 56.25% → 68.75% | 46.90% → 49.75% | 58.63% → 58.63% |
| Holdout / 向量 | 60.71% → 60.71% | 68.75% → 81.25% | 27.42% → 29.31% | 32.69% → 32.69% |
| Holdout / 混合 | 96.43% → 96.43% | 50.00% → 68.75% | 33.10% → 36.11% | 42.56% → 42.56% |

Dev 跳过 5/44（11.36%），Holdout 跳过 4/44（9.09%）；64 条正例中误拦截 0 条，三个集合的放行结果 ID、来源、分数全部不变。40 条独立门控行为样例全部符合预期；这些是回归边界，不是已证明线上零误判。

具体来说，Holdout 的“我暂时不聊了”“在哪里修改我的昵称”原来被 P0 分别返回 1、2 条知识，现在跳过；混合检索还多修复了“收到”的 2 条误召回。因此 P0 的空结果对照从 9/16 正确变成 11/16，混合从 8/16 变成 11/16。问候“晚上好”原本已经返回空，本次只节省检索，没有额外提高质量。

语义挑战 12 条全部放行，检索质量全部不变：P0/向量/混合的 Hit@K 仍为 12.50% / 50.00% / 50.00%，精度仍为 5.00% / 27.38% / 15.62%，空结果正确率均为 100%。本次没有改善语义改写、否定理解和跨语言候选排序。

Holdout 的 Recall@K 保持 P0 82.14%、向量 53.57%、混合 85.71%；MRR 保持 0.8780 / 0.5714 / 0.8304；nDCG 保持 0.8294 / 0.5377 / 0.7970。所有集合的完整指标均在 JSON 报告中。

### 实测延迟（毫秒，关闭 → 开启）

| 集合 / 方法 | 平均 | P50 | P95 |
| --- | --- | --- | --- |
| Dev / P0 | 23.32 → 21.21 | 27.73 → 10.07 | 43.03 → 43.26 |
| Dev / 向量 | 73.01 → 65.45 | 72.37 → 72.38 | 81.02 → 86.77 |
| Dev / 混合 | 98.63 → 89.03 | 101.54 → 98.04 | 123.26 → 125.07 |
| Holdout / P0 | 22.11 → 20.97 | 9.61 → 9.03 | 42.16 → 41.75 |
| Holdout / 向量 | 72.64 → 66.35 | 71.98 → 71.57 | 82.16 → 81.47 |
| Holdout / 混合 | 95.58 → 89.68 | 94.88 → 94.72 | 118.57 → 123.87 |

门控本身平均耗时：Dev 0.0314 ms、Holdout 0.0292 ms、挑战集 0.0351 ms；对应 P95 为 0.0419 / 0.0399 / 0.0472 ms。没有新增 LLM 调用。

Holdout P0 平均检索阶段耗时下降约 5.1%，混合下降约 6.2%。**混合的 P95 反而从 118.57 增至 123.87 ms，不能宣称所有时延都改善。** 样本较少且本机存在调度波动，均值收益主要来自少执行了 4 次检索；尾延迟需要部署后按较长时间窗验证。挑战集没有跳过：平均 P0 24.42 → 24.78 ms、向量 82.32 → 81.94 ms、混合 108.88 → 108.67 ms，视为基本不变。

Holdout 平均片段数：P0 1.7955 → 1.7273，混合 2.2727 → 2.0909；平均正文字符数：P0 1292.16 → 1267.34，混合 1522.45 → 1407.45。尚未测量主模型 token 或端到端首字延迟，不能将这些数值直接换算成费用或页面提速。

验证记录：完整后端回归 366 项通过；随后细化纯运行状态的上下文判断并补充一项测试，最终门控专项 51 项通过。只有既有 Starlette 弃用警告。`git diff --check` 通过。没有发送真实用户聊天请求。

### 怎样解释精度改善

`precision_returned` 是非空返回列表的相关比例宏平均，空列表不计入，包含误返回的负例。因此把无关消息的错误列表改为空，会提高这个指标；它不代表原本专业问题的排序变好了。报告另外提供 `positive_precision_returned`，单独统计需要知识的正例，避免混淆。

本功能只决定是否发起检索，不重写 query、不改变 P0/向量/RRF 的分数和排序。它不能修复“不要跑步，想换游泳”的否定理解，也不能去除一个有效查询内部的错误候选。因此仍需要后续的查询理解、切片和 reranker 改进。

## 遇到的困难与处理

1. **短回复有歧义。** 之前讨论过直接跳过“继续”，但实际它可能要求继续知识解释。实现采用放行；有前文的“好的”也保留检索。代价是跳过率低于激进的关键词黑名单。
2. **旧上下文会诱发无意义检索。** 明确问候、暂停或页面操作先检查当前整句，再决定是否拼接历史，因此旧目标卡不会把这类消息变成知识查询；确认语则让上下文保守否决跳过。
3. **离线短句缺少线上状态。** 真实聊天有开场白、业务进度和记忆，确认语更可能放行。离线命中率、跳过率不能直接预测线上收益；需按真实会话抽样复核。
4. **减少误召回与修复排序是两种效果。** 专门提供正例精度与放行结果一致性检查，让监控能分清提升来源。
5. **平均延迟可能变好，P95 不一定。** 只节省被跳过的回合，原本慢的检索仍可能落在尾部。报告保留均值、P50、P95，不要求每个耗时数字都下降。
6. **默认测试临时目录权限异常。** Windows 的旧 `pytest-of-admin` 目录不可访问，最初测试在准备阶段报错。使用新的独立 `--basetemp` 目录并禁用旧 pytest 缓存后通过，没有修改或删除原目录。
7. **向量依赖提示 pooling 行为历史变更。** FastEmbed 加载有既有提示；本轮沿用原有版本和模型资产，前后均使用同一实例，并记录模型指纹；未为了消除提示而升级或降级模型。

## 监控怎样使用

每个实际执行门控的业务回合新增一条 `retrieval_gate` JSON 日志，记录：

| 字段 | 用途 |
| --- | --- |
| `gate.version` / `gate.reason` / `gate.retrieve` | 规则版本、原因和是否放行 |
| `module` / `retriever` | 分模块、分检索实现比较 |
| `gate_duration_ms` | 门控本身耗时 |
| `search_duration_ms` | 查询构造与检索耗时；跳过时为 0 |
| `total_duration_ms` | 门控与检索合计，不含后续生成和日志输出 |
| `outcome` | `skipped`、`empty`、`returned`、`error` |
| `returned` / `context_chars` | 片段条数与正文字符数；字符数不等于 token 或费用 |

同样的指标会进入 SSE trace 与图内 telemetry，**没有增加数据库字段，也没有把新增 telemetry 持久化成新的消息列**。长期监控使用服务器日志。日志不写入原问题、聊天全文、用户标识或知识正文；现有 SSE 仍保留检索片段 ID。

统计分母是“实际走到门控的普通业务回合”。危机分支和已有的模块状态快捷回答在门控之前返回，因此不在其中。门控不等于风险检测，也不会减少原有风险检查的调用数。检索失败记录为 `error`，仍沿用原异常处理，不把服务错误伪装成正确空结果。

### 常看哪些指标

| 指标 | 怎么用 |
| --- | --- |
| `skip_rate` | 观察节省多少检索；不是越高越好，需要结合误拦截 |
| `false_skip_rate` | 人工/离线标注中“需要知识但被跳过”的比例；本轮回归验收要求为 0 |
| 正例 Hit@K / Recall / nDCG | 确认没有误伤专业问题；回归验收要求不低于同轮关闭门控的结果 |
| `precision_returned` 与 `positive_precision_returned` | 区分清除无关消息与改善专业检索本身 |
| `correct_empty` | 必须有人工负例标签；不能拿日志里的 `empty` 比例代替 |
| 门控和检索总时延均值 / P50 / P95 | 判断额外开销、平均收益及尾延迟 |
| `error_rate_per_search` | 是否出现检索服务故障；与跳过分开统计 |
| 平均片段数与字符数 | 判断注入量；费用需看实际模型 token 使用 |

建议首批真实流量按各个 `reason` 分层抽样，而非只抽容易判断的问候。审核“收到”前面的助手是否承诺下一步给出方法，特别关注 `standalone_acknowledgement` 的误判。若出现被错误跳过的知识请求，记录脱敏回归样例，必要时关闭门控。日志本身没有相关性标签，不能自动推算回答质量或误拦截率。

### 本地重跑

在 `backend` 目录执行：

```powershell
# 只对比 P0，不加载向量模型
.\.venv\Scripts\python.exe scripts/evaluate_intent_gate.py --output evals/intent_gate_p0_report.json

# 三路比较。已构建索引且缓存齐全时可离线运行；不会自动构建新索引。
$env:HF_HUB_OFFLINE='1'
.\.venv\Scripts\python.exe scripts/evaluate_intent_gate.py --with-vectors --repeats 3 --output evals/intent_gate_report.json

# 汇总已导出的 UTF-8 服务器日志
.\.venv\Scripts\python.exe scripts/evaluate_intent_gate.py --logs path/to/backend.log --output evals/intent_gate_live_summary.json
```

日志汇总按规则版本、检索器、模块和开关状态分组，显示异常日志条数；不要把不同时间流量分布造成的变化直接归因于门控。服务器使用 systemd 时可以在部署后用 `journalctl -u bacoach-backend --since '1 hour ago' --no-pager -o cat` 读取指定时间窗日志并保存为 UTF-8 文件，再运行汇总命令。

新增测试位于 `backend/tests/test_retrieval_intent.py`，验证整句匹配、混合请求、否定和风险表达、上下文确认、四模块跳过时不调用检索、主回复继续、Prompt 知识块省略、开关恢复、SSE、错误区分、日志脱敏及统计分母。

## 文件索引

- `backend/app/retrieval_intent.py`：纯本地规则。
- `backend/app/graph/nodes.py`：模块接入、日志、trace。
- `backend/app/config.py`、`backend/.env.example`：开关。
- `backend/evals/retrieval_intent_cases.json`：40 条独立行为边界。
- `backend/scripts/evaluate_intent_gate.py`：前后评测与运行日志汇总。
- `backend/evals/intent_gate_report.json`：最终实测报告。
- `backend/tests/test_retrieval_intent.py`：单元及集成验证。
