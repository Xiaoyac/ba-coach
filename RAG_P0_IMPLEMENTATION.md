# 知识库 RAG P0 实施说明

日期：2026-09-07。状态：P0 已部署至 https://bacoach.xyz/，版本 `20260906T191709Z`（UTC 命名），北京时间约 03:18 完成启动。

## 本次线上发布记录：不变更数据库

- 以线上旧版本 `20260829T102053Z` 为基础复制独立版本目录，保留现有前端、依赖和其他生产代码，仅更新 `retrieval.py`、`main.py`，向线上原有 `config.py` 添加检索参数和启动维护开关，未整体覆盖本地配置文件。
- 新版本 `backend/.env` 只包含非秘密开关 `STARTUP_DB_MAINTENANCE=false`。生产环境文件和密钥未改动；开关关闭启动时的 `create_all` 和历史开场消息补写，仍允许只读加载知识库。
- 未使用会自动迁移和导入的常规部署脚本；没有执行迁移、改表、知识库导入或历史数据补写，没有发布数据库设计文档。数据库模型文件及 `db.py` 与旧版一致。
- 完整后端测试 **304 项通过**，前端类型检查通过。线上三个服务正常；后端健康检查成功，首页 HTTP 200，未登录会话接口 HTTP 401。启动日志确认维护关闭、现有 **1331** 个知识片段加载完成；线上两个替换文件 SHA-256 与本地一致。
- 没有发送真实聊天请求或写入测试对话，尚未进行线上真实模型回答质量评测。
- 后续发布须继续保留关闭维护的设置；原 `infra/deploy/deploy.ps1` 仍包含迁移和导入步骤，不能直接用于“不动数据库”的发布。回退检索应在保留此维护开关与启动保护的情况下恢复旧 `retrieval.py`，不要直接切回会自动维护数据库的旧启动版本。

## 本次实现的内容

1. 为生产 `DatabaseKnowledgeBase` 加入最低分、有效查询覆盖度、相对分数三道筛选。
2. 取消 BA/PA/BCT/MI 类别保底名额，改为全局相关性排序和每个来源最多两条。
3. 增加基于项目实际 10 份资料、1,331 个切片的 88 条初始评测案例，以及新旧策略对比工具。
4. 增加真实数据库检索到模型提示词的集成回归测试，验证弱结果不会进入提示词。

沿用现有词法分数、模块类别权限、知识导入与切片格式、Top-K=2/4/4/2、5 分钟缓存和图工作流。无需数据库迁移，也无需额外模型调用或向量服务。测试用 StubKnowledgeBase 仍是独立的测试替身，生产实现由 DatabaseKnowledgeBase 执行。

## 一、低相关结果怎样过滤

生产流程：

```text
构造当前查询（沿用：当前消息 + 最近两条用户消息 + PA 卡）
  → 模块类别过滤
  → 原词法排序，同时计算证据覆盖度
  → 最低分 + 最低覆盖度
  → 与最佳合格结果比较，过滤相对弱结果
  → 按全局排序取结果，每来源最多两条
  → 注入提示词；不足 K 条时不补齐，全空时不注入知识块
```

### 参数与具体规则

| 环境变量 | 默认值 | 含义 |
|---|---:|---|
| `KNOWLEDGE_MIN_SCORE` | 0.1 | 原词法分数必须大于等于此值 |
| `KNOWLEDGE_MIN_COVERAGE` | 0.12 | 有效查询的 IDF 加权覆盖度必须大于等于此值 |
| `KNOWLEDGE_RELATIVE_SCORE` | 0.2 | 分数至少达到最佳合格候选分数的 20% |
| `KNOWLEDGE_MAX_PER_SOURCE` | 2 | 同一来源文件最多提供两条参考 |

配置位于 `backend/app/config.py`，示例在 `backend/.env.example`。修改环境变量后重启后端进程。非法范围会被配置校验拒绝；离线命令可以使用对应参数做实验。

原分数仍由 IDF、词频饱和、标题加权和词面覆盖计算，不是完整 BM25，也不是“答对的概率”。一个分数为 3 的切片不能解释为 30% 置信度。这一版保留原分数，方便隔离筛选策略带来的变化。

覆盖度的计算：先从查询中去掉“你好、谢谢、今天、怎么”等一般话语片段，再对剩余内容生成中文二/三元词和英文词。覆盖度为正文或标题实际匹配的词的 IDF 之和，除以全部有效查询词的 IDF 之和。文件名仍能影响原始排序，但仅仅匹配文件名不能作为合格证据。

停止词在分词前用空格替换，避免“谢谢你”虽然去掉“谢谢”，却仍被“谢你”这种跨边界片段误召回。停止词处理仅用于检索合格判断，不改写用户的原消息或模型看到的对话。

中英文扩展词另外计算覆盖度，并与普通覆盖度取较大值。例如“晚饭后散步”会扩展为 `walking`；英文活动分类条目不需要同时包含整句中文才能通过筛选。这个通道只使用现有显式别名字典，不生成新的语义推断。

相对分数门槛以**通过前两道检查的最佳候选**为基准。例如，最高原分数 100 的候选只有 1% 覆盖而被拒绝，不能再用它把分数 5、覆盖很充分的候选全部压掉。第一、第二名的差距不作为直接弃用条件，因为多条结果得分接近也可能都正确。

### 优点和代价

优点：泛词或文件名带来的弱匹配更难进入提示词；可以返回零条；保留中文检索英文活动名称的能力；没有增加付费模型调用。

代价：这是词法证据筛选，不能充分理解同义词、否定或隐含意图。停止词过宽可能移除有用线索，过窄则仍有误召回。阈值对资料分布敏感，新增大量文献后需要重新评测。双语别名覆盖通道有利于跨语言召回，也可能让只匹配“运动”这类宽泛概念的文档通过。

固定分数门槛对小知识库尤其敏感：单篇资料中 IDF 较小，过高门槛会误伤精确命中。开发过程中测试暴露了这一点，最终使用较低的绝对门槛，再通过覆盖度与相对分数约束质量。

## 二、类别配额怎样调整

旧策略先从每个命中的类别取一条，再补齐剩余名额。假设候选是：

| 候选 | 类别 | 分数 |
|---|---|---:|
| A | BA | 10 |
| B | BA | 9 |
| C | MI | 0.12 |
| D | PA | 0.11 |

在 Top-3 下，旧策略可能取 A、C、D；新策略取 A、B，剩余位置留空。

新的类别过滤仍按照现有业务规则执行：BA 可用于所有模块，PA 仅模块二，BCT 仅模块三，MI 用于模块二与三。取消的是“必须分配名额”，并没有扩大模块可用的资料范围。

筛选后按全局分数遍历，同一来源满两条后跳过，不再用低分结果补空位。不同类别的结果如果都足够相关，仍然可以同时入选。

优点：避免低相关类别占位，最终列表按分数有序，同一本长书不会贡献无限多条参考。

代价：来源上限不是语义去重；同一文件的第三条可能确实必要，也会被省略。跨文件重复段落目前仍可能同时进入结果。取消类别配额后，某些有用的辅助类别可能没有入选，未来可用重排或基于任务的查询拆分处理。

## 三、评测集和方法

数据集：`backend/evals/retrieval_cases.json`。

- 88 条初始案例，开发集和预留验证集各 44 条。
- 每个集合各 28 条需要知识的案例、16 条应当为空的对照。
- 包括专业术语、口语、中英文活动名称、多轮指代、PA 卡上下文、否定表达、闲聊、无关请求和模块边界。
- 开发集用于观察结果与选择参数；预留验证集在参数确定后验证，没有依据验证集结果继续调参。
- 同一版本语料与分词、相同查询构造、相同 Top-K 下，对比旧类别优先策略与新筛选策略。

每个正例预先写明预期来源、标题片段或正文片段。工具先从完整语料解析所有符合条件的切片为相关集合，再执行检索，不会把算法命中的结果反过来当答案。若资料变更导致标注失效，评测直接报错，避免把错误标注当作检索失败。

这些案例和标注由本次实现整理，状态明确为 `assistant_authored_pending_domain_review`，尚待业务专家审核，不是临床验证集，也不等同于真实用户流量分布。标题或关键词选择器可能漏标其他同样合理的段落。

### 指标定义

| 指标 | 计算含义 |
|---|---|
| `hit_at_k` | 正例中，前 K 条至少包含一条标注相关内容的比例 |
| `recall_at_k` | 每个正例召回的相关切片数 / 该案例全部标注相关切片数，再取平均 |
| `mrr` | 正例中第一条相关结果的排名倒数，无命中为 0 |
| `ndcg_at_k` | 使用二元相关标注的排序质量，考虑相关内容是否排在前面 |
| `correct_empty` | 应为空的案例中实际返回空的比例 |
| `precision_returned` | 每个非空结果列表中相关切片占比，再取平均；包括误召回的负例，空列表不计入 |
| `returned` | 全部案例平均返回切片数 |

`precision_returned` 不等于标准 Precision@K，空结果也不能计为 100% 精确。命中率、召回率和空结果判断需一起看，不能通过全部返回空来提高“精度”。Compendium 一个活动可能涉及多个相关切片，因此命中一条与召回全部切片是不同指标。

### 本地实际结果

| 指标 | 开发集旧策略 | 开发集 P0 | 验证集旧策略 | 验证集 P0 |
|---|---:|---:|---:|---:|
| 正例命中率 | 92.86%（26/28） | 92.86%（26/28） | 92.86%（26/28） | 92.86%（26/28） |
| 应为空判断正确率 | 18.75%（3/16） | 81.25%（13/16） | 12.50%（2/16） | 56.25%（9/16） |
| 全部标注切片召回率 | 69.73% | 69.73% | 84.23% | 82.14% |
| MRR | 0.8631 | 0.8839 | 0.8810 | 0.8780 |
| nDCG@K | 0.7583 | 0.7779 | 0.8373 | 0.8294 |
| 非空列表平均相关比例 | 25.00% | 47.04% | 25.60% | 46.90% |
| 平均返回切片数 | 2.8864 | 1.7273 | 2.8409 | 1.7955 |

验证集平均切片数减少约 36.8%，它是参考条数变化，不能直接换算为 token 或费用节省。过滤降低了噪声，也舍弃了一些相关内容，验证集召回率下降约 2.09 个百分点。不能宣称所有指标都改善。

报告保存每条案例的命中来源、分数、候选覆盖度和指标，并按问题类型汇总：

- `backend/evals/reports/dev.json`
- `backend/evals/reports/holdout.json`

报告包含语料哈希、数据集哈希和检索代码哈希。报告中的 `kb:N` 是离线语料编号，不是生产数据库 ID，应通过来源和标题对照。

耗时字段共用同一次词法排序及覆盖度计算，再分别计算新旧选择耗时，只用于诊断；不是完整旧版本与新版本的独立性能压测，也不包含索引装载、数据库和模型生成耗时。

### 明确保留的失败案例

- “我不想打篮球，想做瑜伽”：新旧策略都未命中标注的瑜伽证据，说明当前查询拼接及词法排序不理解否定和目标变更。
- “合作在行为激活治疗中是什么意思”：两者都未命中该案例指定的章节。
- “我暂时不聊了”“在哪里修改我的昵称”等仍会误召回，说明停止词和阈值不能完全代替检索意图判断。

这些失败没有通过删除案例或依据验证集反复修改阈值来隐藏，适合作为下一轮查询构造与混合检索的改进目标。验证集已被查看，未来若据此调优，应另外准备新的未见测试集。

## 四、怎样重新运行

在 `backend` 目录执行，Windows 使用项目虚拟环境：

```powershell
.\.venv\Scripts\python.exe -m pytest -o addopts='' -q
.\.venv\Scripts\python.exe scripts/evaluate_retrieval.py --split dev --output evals/reports/dev.json
.\.venv\Scripts\python.exe scripts/evaluate_retrieval.py --split holdout --output evals/reports/holdout.json
```

其他系统在激活虚拟环境后，将 `.\.venv\Scripts\python.exe` 替换为 `python`。依赖仍使用现有 `requirements-dev.txt`。

离线实验参数示例（仅作实验，不改变生产默认配置）：

```powershell
.\.venv\Scripts\python.exe scripts/evaluate_retrieval.py --split dev --min-coverage 0.15 --relative-score 0.3 --output evals/reports/experiment.json
```

评测读取项目知识源文件，不连接线上数据库，不调用模型、MemOS 或外部 API。它复用现有 XLSX 转换和 Markdown 切片逻辑，确保评测的是项目当前语料而非几条人工示例文档。

### 回答忠实度复核

检索评测不等于真实回答已经答对。工具支持导出人工审阅包，包含原查询、模块和新策略实际选择的完整上下文：

```powershell
.\.venv\Scripts\python.exe scripts/evaluate_retrieval.py --split holdout --export-review ../../work/rag_holdout_review.json
```

人工补入实际模型回答、审核人、日期、被支持的结论、未被支持的结论、引用正确性及备注。初始值均为 NULL，不会伪造“已通过”。审核重点是事实是否被资料支持、是否把文献中的案例当成当前用户事实、是否忽略用户明确否定、无证据时是否做出不应有的结论。

本次已经验证“筛选后的内容会怎样进入提示词”，没有运行真实模型的回答忠实度评分；导出包为后续业务复核提供输入。

## 五、测试、运行影响与后续方向

本地完整后端测试：**302 项通过**，一个第三方 Starlette/AnyIO 弃用警告。新增 16 个参数展开后的测试，覆盖：低相关类别占位、来源上限、阈值边界、非法配置、泛词、文件名误命中、双语小语料、环境变量生效、数据库到提示词的弱结果过滤，以及评测指标和过期标注。

索引额外缓存正文/标题的词集合，增加一定进程内存，避免每次查询重新分词整份语料。主模型调用次数与现有方案相同。筛选后没有知识时，模型仍按现有模块提示词继续回复，不会因为知识为空直接拒绝服务。

后续应先由业务方复核初始标注，再补充脱敏真实用户样例。对验证集已经暴露的否定、口语和话题转换问题，适合改进查询构造；对同义表达和跨语言召回，可再对比向量召回、RRF 和重排。线上日志持久化、可点击引用和知识版本管理属于后续 P1/P2，本次没有新增这些依赖。

部署时发布本次后端代码并重启即可，无需修改业务表。若需要回退，应回退本次检索实现对应的代码版本；把阈值调为零不会恢复旧的类别优先算法。
