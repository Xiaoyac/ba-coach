# M2 场景 Contract 核验与跨模块回归

> 后续更新：用户已确定中介失败不得放行原始知识。相关本地修复及验证见 M2_CONTRACT_FIXES_20260911.md；下方为修复前的历史审计结果。

日期：2026-09-11。结论：当前正常流程可以运行，但尚不能视为整份讨论 Contract 已验收。复现 2 项状态/完成校验缺陷，发现 1 项上下文接口缺口和 1 项需要审批的降级策略差异。

## 范围与方法

读取 contract讨论(2).md 和当前本地代码；新增 scripts/audit_m2_contract.py。真实 FastAPI、V2 SQLAlchemy 数据结构及业务函数运行于一次性内存 SQLite；所有账号、消息和知识为合成测试数据。中介使用可控假模型。没有读取或修改生产数据、没有调用真实 LLM、没有重新部署。

这次是可复现的接口和状态机核验，不是完整真实模型聊天、真实知识库召回质量或浏览器验收。Router 错误输出由测试主动注入，用于检验后端防线，不代表实际模型必然输出错误。跨模块回归覆盖现有测试所列场景，不等于风险、记忆、每日记录等全系统 Contract 已定稿。

## M2 贯穿场景

前提：测试用户历史 M1 已完成，创建“晚饭后散步”目标及 planning 周期；另建“阅读小说”目标用于隔离检查。

用户：我想每天晚饭后散步，但最近膝盖疼，还没决定地点和时长。

| 环节 | 应遵循的接口约定 | 观察与结论 |
|---|---|---|
| 运行态 | 明确用户、目标、周期、模块 | 目标及周期建立正常，阅读聊天不携带散步目标 |
| 用户证据 | 当前疼痛是自述，不推断诊断，也不忽略 | 最新纠正能传到中介；未验证真实模型是否正确使用 |
| Context | 事实应带来源、版本、确认和有效性 | 当前主要为字符串；缺少显式 Task、Goal/Cycle ID 和结构化来源信息 |
| Retriever | 召回材料不等于适用建议 | 本次注入合成片段，实际排序/相关性不在新场景测量范围 |
| 中介 | 可以拒绝全部知识，不直接写库 | 空 selected_ids 正确移除片段；空召回跳过调用 |
| 降级 | 必须明确失败后哪些知识还能使用 | 超时回退到原片段，无中介筛选建议；需确定是否允许 |
| Module/抽取 | 信息未知留空，先形成草稿 | 仅草稿不会自动允许确认 |
| Step/Router | 用户纠正后旧完成判断应失效或重审 | 步骤只合并不撤销；后来提出留在 M2，待确认状态仍保留 |
| 确认/写库 | 完成规则与后端校验应一致 | 错误完成信号后，不完整计划确认返回 200 并进入 M3 |
| 并发防护 | 过期和重复确认不重复推进 | 两类请求返回 409；其他目标保持 M2 |

## 问题清单

### F1 高优先级：计划完成标准和后端检查不一致

证据：workflow_contract.py 的 pa_card_completed 要求活动、时间、地点、时长、频率、障碍及应对完整可执行并确认；v2_repository.confirm_plan 只检查活动内容和 schedule_text 非空。

复现：主动注入全部 M2 步骤完成，保存仅有活动和时间的草稿。之后活动字段修改为“暂未决定，用户纠正膝盖仍疼”，刷新并确认仍返回 200。实际流转是 M3，而不是阻止并要求补充。

含义：用户按钮确认与字符串非空不足以证明计划符合业务完成标准。此测试不证明模型会这样输出，也不把未知地点或时长一概认定为所有活动都必须填写。

整改方向：先审批活动类型相关的必需/条件必需字段及明确无障碍的表达；将同一规则用于 Prompt、接口和后端校验。无法由机械规则判断的证据保留来源并由语义层判断，后端不能仅依赖 Router 的一个完成结论。

### F2 高优先级：旧完成状态无法撤销

证据：v2_workflow.record_steps 将旧步骤与新步骤取并集；ready 为 false 时不清除 awaiting_record_confirmation。

复现：完成后更新草稿，再提交 requested_target=module_2、steps=[]，can_confirm 仍为 true。旧草稿 hash 被拒绝，但刷新取得新 hash 后仍能确认。

整改方向：引入明确的证据失效/撤销操作、原因和版本。不能把空步骤数组一概理解为撤销；要区分“本轮没报告”与“用户否定了旧证据”。草稿关键内容改变时重新审查依赖步骤并撤销旧待确认资格。

### F3 接口缺口：中介没有结构化业务语境

证据：knowledge_mediator 的输入键只有 module、user_input、history、confirmed_context、profile_constraints、knowledge。业务上下文被转为截断字符串；没有显式 task、goal_id、cycle_id 或统一 provenance 对象。

测试放入 unconfirmed / ai_inference 记忆，文本标签仍存在，但外层字段名为 confirmed_context，容易造成误解。不能据此声称模型已经把推断当事实。

整改方向：采用 facts 数组，包含 canonical_name、source、record_id、version、confirmation、status、validity；附任务和目标/周期。未知保持 null。中介只读这些已授权的数据，不开放自由查库。

### F4 策略待审批：中介失败时直接使用未筛选知识

证据：超时后返回原始 knowledge，block 为空，reason=timeout。现有单元测试也明确验证这一行为，因此是现有有意降级策略，不应误报成随机故障。

按“必须经过中介筛选”的严格候选 Contract，此项不通过；但讨论未正式确定超时策略。

建议：涉及身体限制、知识冲突等情形，失败时不注入未审查的建议；仍可依据用户输入澄清，不应让聊天整体崩溃。普通低风险场景是否保留受限知识需业务审批。无论选择哪种策略，都记录错误、降级路径和耗时。

## 数据指标

| 测试组 | 结果 | 解释 |
|---|---|---|
| 新增 Contract 探针 | 12 项，8 满足候选断言，4 不满足 | 两项缺陷、一项接口缺口、一项策略差异；不是 RAG 准确率 |
| 现有针对性回归 | 116 通过 | 中介、回复校验、V2 仓储/结构、意图门控、检索策略 |
| V2 API 冒烟 | 7 场景通过 | 注册支持者、M1 确认、历史复用、M2/M3/M4 流转、多目标及删除保留历史 |
| 真实 LLM/生产/浏览器测试 | 未执行 | 不能报告生成质量、真实召回精度或线上延迟改善 |

探针明细：目标上下文隔离通过；仅草稿不完成通过；旧 hash 拒绝通过；纠正后撤销完成未通过；不完整计划拦截未通过；重复确认拒绝通过；另一目标不推进通过；最新纠正传入中介通过；明确不适用移除知识通过；结构化任务来源未通过；超时不使用未筛选知识未通过；空知识跳过通过。

首次 pytest 因系统临时目录无权限导致 fixture 初始化失败；使用独立测试临时目录后 116 项通过。仍有缓存目录权限警告与依赖弃用警告，不影响本次断言结果。

## 第四步的回归扩展与剩余工作

已执行：M1 新确认和历史复用；M2/M3/M4 正常状态链；跨目标隔离；过期/重复确认；中介在四模块传递；知识不适用、空知识、超时；已有回复校验及检索策略。

未完成验收：真实用户自然语言经真实模型自动形成 Step 证据；膝盖疼场景在真实检索及生成链上的行为；网页交互；跨设备并发；完整风险/每日记录/长期记忆来源与删除 Contract。需先定稿接口和业务规则，再补对应测试，不能以 116 项绿灯代替。

## 建议处理顺序

1. 先修 F2 状态失效，再统一 F1 的完成条件；这两项影响是否错误推进。
2. 补 F3 上下文及中介输出 schema，清楚区分知识中介和回复校验器。
3. 审批 F4 失败降级策略，并加入接口测试。
4. 修复后重跑同一探针和回归，再用合成用户运行真实模型端到端评测，记录任务完成正确性、事实误用、错误推进、降级率和响应耗时。

本轮仅增加诊断脚本及报告，不修改产品实现，也不发布生产。
