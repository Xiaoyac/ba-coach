# 每日记录强调与 PA 目标时间线

> 发布／最终确认：所有最终确认改动已于 `20260917T084630Z` 上线至 https://bacoach.xyz/ ，包括「4项选填」和移除「不填也可以」。三项总体评分继续必填；「完成程度选填」已全部撤回，相关迁移仅执行 dry-run，未改数据库。

> 最新状态：文末「每日记录协调性调整」覆盖本页较早的活动内容大框、心情强调色块和左右非等宽设计；「总体评分文案与必填调整」进一步移除新表单的不适用选项。时间线与选单调整保持不变。

## 本轮范围

本地界面调整，尚未部署。不改数据库字段、不导入本地资料、不改目标创建／选择规则。目标仍由教练在聊天中写入，档案页只读。

设计沿用现有的暖色与黑金主题，依照 frontend-design-codex 区分主次信息，不引入新图片、依赖或业务步骤。

## 每日记录

- 时间字段明确标注「活动时间 · 必填」，选择前显示「选择活动时间」。
- 活动内容从一条底线改为有背景、完整边界的多行输入区，增加独立标题、必填标识、实例和聚焦轮廓。
- 做完活动后的心情使用更醒目的背景和标题，评分按钮加高至 48px，保留 0–5 共六个选项；不预选分数。
- 成就、联结、愉悦、重要四项一直展示，不再折叠。可选区域淡化背景、缩小单项体积；手机同行排列标题与评分，电脑使用两列。明确提示可留空、再次点击可取消。
- 保留三项必填、可选值为 null 而非 0、总体三项评分、计划不适用、历史量表版本和提交校验。

## 目标档案

- 默认改为纵向时间线：节点展示「第 N 个目标」、创建日期、状态、执行内容与安排、最近执行轮次；点开仍是完整 PA 卡片与历史详情。
- 默认最近设定在前，可选「从第一次开始」。序号在全部现存有日期目标中按创建时间升序计算，之后才筛选；最近更新时间、标题排序、筛选均不更改序号。同一时刻用目标 ID 稳定排序。
- 编号是现存档案的展示顺序，不是新的永久数据库编号；删除早期档案后其余编号可能前移。缺失或无效日期不臆造先后，放在末尾标「顺序未知」。
- 一项目标可以执行多轮。第 N 个目标与第 N 轮执行保持区分；界面显示数据库记录的目标状态，不根据轮次凭空计算完成率。
- 保留「卡片总览」。状态／类型移到可展开的筛选区域，默认无需先选分类，直接查看历史；搜索可直接使用。
- 从详情返回时保留筛选、顺序、滚动位置和键盘焦点。计划版本、执行／复盘、活动记录与历史分页仍可访问。

## 文件

- `frontend/components/DailyAssessmentModal.tsx`：表单视觉层级。
- `frontend/components/GoalOverview.tsx`：时间线、排序编号、筛选与返回恢复。
- `frontend/app/globals.css`：响应式时间轴样式，复用主题变量。
- `infra/qa/daily-record-0917.cjs`、`infra/qa/goal-timeline-0917.cjs`：模拟 API 浏览器验收。

## 验收清单

- 1440、390、360 浅色，1440、390 暗色：必填输入显著、可选常显、评分清晰、边距与对齐、无横向溢出、保存／关闭可达。
- 每日记录：空白、0 分、null、计划不适用、半填活动、删除空卡、保存失败重试、旧新版历史与 M3 入口。
- 目标：创建顺序与更新顺序不同、正反排序、筛选后保留序号、无日期、空档案、无匹配、加载失败重试、只读、详情与跨页记录、返回原位置、切换卡片视图。
- 测试资料全部是合成数据，不与生产 API 或数据库连接；实际浏览器预览沿用已有本地管理员会话。

## 验收结果

- `npm run build`、`npm run typecheck`、`git diff --check` 全部通过。构建主页面 64.4 kB、首次加载 171 kB。
- `node infra/qa/daily-record-0917.cjs`：五组视口／主题通过，包含三项必填、四项常显可选、0/null/不适用、半填卡、失败重试与历史版本。
- `infra/qa/goal-timeline-0917.cjs` 通过持久 Node REPL 的 Playwright 浏览器运行：五组通过，包含创建时间正反序、与更新时间不同、筛选稳定编号、缺日期、空档案、错误重试、详情分页及返回位置／焦点；所有请求只读。
- 补充 360×740：每日记录保存按钮完整位于屏幕内；Home/End 可将心情分别选为 0/5，时间线无横向溢出，模态框底部未超出屏幕。
- 目视检查涵盖活动输入边界、必填提示、主次评分尺寸、主题字色、时间轴节点对齐、手机顶部占位与滚动边界。查看截图后进一步缩短了手机可选评分区，并将目标分类移入次级筛选区。
- 第一次目标回归在手机重新打开档案时，脚本未处理侧栏自动收起，导致等待超时；修正测试入口后整轮重跑通过，不属于产品故障。
- 本地 `http://127.0.0.1:3000/` 已刷新到新版，保留 `localadmin` 管理员会话并打开「记录今日」。本地服务有意没有连接真实 LLM，因此预览用于界面体验，不能据此验证真实模型回复。
- 截图：`.test-tmp/daily-record-0917/` 与 `.test-tmp/goal-timeline-0917/`，均不进入发布包。

未部署线上；没有本轮后端修改或数据库迁移，未重复执行全量后端测试。手机验收为 Chromium 触控／视口模拟，未涵盖真实手机软键盘及 Safari。

## 补充：目标页面选单样式

- 新增 `frontend/components/ArchiveSelect.tsx`，替换目标页面的时间线顺序、卡片排序、目标状态、目标类型四个原生下拉框。展开列表也使用主题样式，不再出现系统蓝色选中背景。
- 沿用暖色／黑金主题变量；统一圆角、字色、悬停底色、选中勾号、阴影与触控高度。菜单限制在对话框横向范围内，纵向根据剩余空间自动向上或向下展开，长列表内部滚动。
- 支持方向键、Home／End、Enter／空格确认、Escape 取消、Tab 离开、点击外部关闭；取消不改变已选值，也不关闭目标弹窗。选单状态同步使用 `beforetoggle`，避免外部关闭后短暂仍显示展开状态。
- `GoalOverview.tsx` 仅替换选单组件，既有排序、筛选、编号和目标详情规则保持不变；不改数据库、不引入组件库。
- 最新 `npm run build` 与类型检查通过：主页面 66.1 kB、首次加载 173 kB；`git diff --check` 通过。
- `node infra/qa/goal-timeline-0917.cjs` 六组通过：1440×900、390×900、360×900 暖色，1440×900、390×900 黑金，以及 360×540 矮屏。覆盖菜单边界、向上展开、键盘确认／取消、外部关闭、筛选、详情分页、返回位置、空态与错误重试；无页面运行错误。
- 已目视核对展开菜单的主题字色、选中对比、圆角阴影、触控尺寸、对齐及手机边界。600px 高屏仍有足够空间向下展开，测试最初错误要求它向上；改用真正不足空间的 540px 高屏后通过，未因此改变正常定位逻辑。
- 本地管理员预览已刷新，并打开「我的目标 → 时间线顺序」展示新版。保留六条跨日期的本地合成目标；未向线上上传测试资料，未部署。本次浏览器证据为 Chromium，尚未验证真实 Safari／旧版不支持 Popover API 的浏览器。

## 每日记录协调性调整（最新）

### 设计依据与改动

反馈指出，活动时间、内容、心情的表现形式不一致，而大文本框、独立心情色块及过高的可选区域，让左侧比右侧更重。本次使用 frontend-design-codex 的统一组件语言与主次层级原则，保留暖色／黑金，不增加装饰或业务步骤。

- 三项必填统一使用 14px 标签、文字型「必填」标记、相同的圆角／细边界／背景及焦点轮廓。不再同时混用时间胶囊、大文本框和心情色块。
- 活动内容默认为两行，初始高度约 66px；保持多行输入、内部滚动与手动调高，不截断保存内容。时间控件 44px 高，与内容框等宽。
- 活动心情和当天整体评分共用 44px 高的评分按钮；量表解释统一放在按钮下方。仍是 0–5 六个值，不预选。
- 电脑端两栏等宽且顶部对齐。右侧由三个独立小卡片合并为一块完整的「整体感受」区域，与左侧活动卡使用相同背景和内边距。
- 四项可选感受一直可见，电脑采用紧凑的两列评分，手机采用单列横排。省去每项重复的可见解释行，解释保留为标题悬停提示与读屏说明；保留「可选、不填也可以」和 0–5 提示。
- 可选评分高 36px，并检查最窄桌面下点击宽度至少 24px；必填评分维持 44px。计划不适用勾选框跟随主题强调色。
- 手机仍是一页顺序填写，不添加分页。矮屏和多条活动允许表单内部滚动，保存入口固定可达。

### 实现范围

- `frontend/components/DailyAssessmentModal.tsx`：新增共用 `FIELD_LABEL`／`FIELD_CONTROL` 样式，调整 ActivityCard、SegmentedScore、TimeSlotSelect 和总体评分布局。
- `infra/qa/daily-record-0917.cjs`：改为可复用持久浏览器的 `run(browser)`，保留命令行入口，增加协调性与触控尺寸断言。
- 本文记录改动与验收。本轮没有修改后端、字段、评分数据、目标系统或提交规则，也没有上传本地测试资料。

### 验收依据和结果

使用 playwright-interactive 的持久浏览器执行同一套功能／视觉清单，API 全部由合成数据拦截。浏览器提交测试不会写入本地真实账号或生产环境。

- 九组通过：1440×900、390×900、360×900 暖色；1440×900、390×900 黑金；1366×768、1024×768、820×900、360×740 暖色。
- 布局断言：两栏等宽同顶部、三项必填标签字号／字重／行高一致、内容高度 60–80px、时间和内容等宽、主要评分同高度、可选评分最小宽度、保存入口边界、无横向溢出。
- 原有功能回归：必填拦截、0 分与 null 区分、不适用勾选／取消、可选取消、空白／半填活动、删除、保存失败保留输入并重试、旧新版历史、M3 入口与确认流程全部通过，无页面运行错误。
- 额外探索：较长多行输入、新增再删除第二条活动、Home／End／方向键心情评分；第一条内容完整保留，键盘选中结果正确。
- 目视检查：标签层级、两栏比例、空白与已填写状态、选中对比、圆角与边界、可选区密度、时间选单覆盖位置、手机纵向滚动与固定底栏。已查看真实本地管理员窗口，不只看模拟截图。
- `npm run build`（含类型检查）通过：主页面 66.1 kB、首次加载 172 kB；`npm run typecheck`、`git diff --check` 通过。
- 截图目录仍为 `.test-tmp/daily-record-0917/`，新增 `before-balance-desktop.png`、`balanced-desktop.png`，九组 `required-empty-*`／`time-picker-*` 等截图；不进入发布包。

本地页面已刷新并为管理员打开「记录今日」。未部署线上。真实手机软键盘、Safari 及完整后端测试不在本次纯布局验收范围内；多条活动时左右内容高度不强制相同，避免人为拉伸产生空白。

## 总体评分文案与必填调整（最新）

- 删除填写页的「今天没有预定计划／不适用」勾选项。
- 两个题目严格改为「想做的事情完成程度」和「回顾今天，你今天整体心情如何？」；身体活动题目不变。
- 三项总体评分均需显式选择 0–5，不预选。移除与不适用有关的条件渲染及底部提示，新提交固定 `completion_not_applicable: false`。
- 不删除数据库字段、不清洗历史资料，旧记录中的不适用仍按原意显示，不转成 0 分。本轮仅修改前端新建表单，不禁止旧客户端通过现有 API 提交历史兼容格式。
- 沿用 frontend-design-codex 的既有组件样式，没有扩大设计范围。修改 `DailyAssessmentModal.tsx`、`infra/qa/daily-record-0917.cjs` 和本文。
- `npm run build`（含类型检查）、`git diff --check` 通过；九组浏览器测试全通过，新增校验无勾选项、文案正确、完成度未选不可保存、选 0 可保存且提交不适用为 false，历史不适用依然正确显示。
- 目视核对桌面与手机的题目换行、间距、选中对比、两栏对齐和固定保存入口。构建主页面 65.9 kB、首次加载 172 kB。测试请求仅使用合成数据。
- 已另开本地管理员新版预览，保留原页面未保存的评分，没有强制刷新该表单。未部署线上，未改生产或测试数据库。

## 最终发布：20260917T084630Z

- 发布前与 `20260917T082904Z` 对比，业务源码仅 DailyAssessmentModal 的「4项选填」文案不同；此前的每日记录布局、目标时间线及主题选单已在生产。
- 在独立版本目录复制已验证生产前端，只替换该组件，再执行 npm ci／build／prune。后端链接到原版本，不重新安装依赖、不重启后端。构建成功后切换版本并重启前端，保留失败回退逻辑。
- 本轮只上传前端组件及临时发布脚本，没有上传 .test-tmp 数据、测试账号、SQLite、环境密钥或知识库，也没有执行迁移或资料导入。
- 发布后 127 个业务源码文件与本地确认版本逐一 SHA256 校验一致。首页 HTTP 200，后端 health 为 ok，未登录访问对话及管理员记录接口均为 401；前后端服务 active，后端 PID 328430 保持不变。
- 本次构建主页面 65.9 kB、首次加载 172 kB。
- 线上前端回归：每日记录 9/9 组、目标时间线 6/6 组全部通过，覆盖桌面／手机、暖色／黑金和矮屏，无页面运行错误。确认三项总体评分仍必填、文案为「4项选填」、无「不填也可以」说明；时间线顺序、主题选单、筛选和详情回看正常。
- 上述浏览器回归加载 https://bacoach.xyz/ 实际发布的 JS/CSS，API 由合成数据拦截；未写入生产数据库，不代表真实账号保存或真实 LLM 链路的端到端验收。
- 如需回退界面，previous 为 `/opt/bacoach/releases/20260917T082904Z`。当前版本的 backend 链接复用该目录，清理旧发布时必须保留这个依赖目录。

## 每日记录入口合并（2026-09-17）

- 将聊天右上角「记录今日」移到左侧导航「我的目标」下方，与目标入口统一使用图标、字号、间距和至少 44px 点击高度，沿用 frontend-design-codex 的既有设计规范。
- 移除重复的「我的每日记录」导航入口。只调整入口，不删除用户记录；历史仍从「记录今日 → 查看历史」进入，再次打开导航入口回到今日填写页。
- 手机点击入口自动关闭侧栏；桌面收起后仍可通过侧栏切换按钮重新展开。保留模块三的「打开每日记录」快捷按钮和管理员「每日记录数据」。
- 应用修改仅涉及 `frontend/components/ConversationSidebar.tsx`、`Chat.tsx`、`ConversationWorkspace.tsx`。更新每日记录回归脚本与旧聊天可靠性测试的入口路径，不改评分、接口、权限或数据库。
- 本地 `npm run build` 通过（页面 65.8 kB，首次加载 172 kB）。每日记录 Playwright 九组回归通过：包含桌面／手机、暖色／黑金、普通用户／管理员、收起再展开、进入历史再重新打开今日记录、模块三确认、必填校验、失败重试，无页面运行错误。
- 目视检查桌面 1440px 与手机 390px 导航截图：图标对齐、字号、间距、对比度、点击区域和视口边界正常，没有重复入口或遮挡。截图位于 `.test-tmp/daily-record-0917/sidebar-record-entry-*`。当前会话没有 js_repl，使用现有 Playwright 脚本替代交互 skill。
- 浏览器测试只使用合成 API，不向生产数据库写数据；未覆盖真实手机 Safari、真实账号保存和模型调用。
- 已上线版本 `20260917T085435Z`：服务器构建通过，三个组件 SHA256 校验一致；首页 HTTP 200、管理员接口未登录 HTTP 401，前后端服务 active，后端 PID 328430 未变。线上实际前端资源的九组回归全部通过，无页面运行错误。
- 本轮仅发布三个前端组件，无迁移或资料导入。前一版 `/opt/bacoach/releases/20260917T084630Z` 可用于前端回退；后端仍直接复用 `/opt/bacoach/releases/20260917T082904Z/backend`，不得清除此依赖目录。
