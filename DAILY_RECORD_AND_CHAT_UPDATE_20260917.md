# 每日记录与聊天体验调整 · 2026-09-17

## 当前状态

**已部署线上，版本 `20260917T051029Z`，已执行必要的每日记录结构迁移。** 发布前服务器备份两张表；发布后历史 3 份记录与 4 项活动与备份逐项核对一致，网站 HTTP 200、服务正常。未上传本地测试资料或导入知识库。

本文所列每日记录、停止生成、计时同步与旅程精简已发布。后续新增管理员数据页的状态另见 `ADMIN_DAILY_RECORDS_20260917.md`。浏览器功能验收使用真实前端构建和模拟 API；保存与读取另外通过隔离数据库测试。

## 每日记录有哪些变化

| 部分 | 新规则 |
| --- | --- |
| 每项活动 | 时间、活动内容、做完活动后的心情必填；使用醒目的必填提示与独立心情区 |
| 四项感受 | 成就、联结、愉悦、重要性可选，默认折叠在“更多感受”中；不填保存为 `null`，不是 0 分 |
| 计划完成 | “计划／想做事情的完成程度”，0–5 分；可以选择“今天没有预定计划／不适用” |
| 身体活动 | “今天总体身体活动程度”，0–5 分，端点为“几乎没有活动”与“活动很多” |
| 整体心情 | “回顾今天，你今天整体／平均心情如何？”，0–5 分，端点为“很低落”与“很愉快” |
| 默认值 | 所有评分初始未选，不再替用户填分 |
| 历史记录 | 新版总体显示 `/5`；旧版仍显示 `/10`、五项原评分及“旧版量表”，不折算旧数据 |

仍然一页填写：电脑双栏，手机同页向下滚动，保存按钮固定在底部。可选项默认折叠，展开后再次点击已选分数可清空。手机底部校验提示单独一行，避免挤压按钮。

### 三种情况不会混淆

- 今天有计划，但完全没完成：用户明确选择完成度 **0 分**。
- 今天没有预定计划：用户勾选“不适用”，保存 `completion_rate=null`；新版已完成记录读取时还会返回 `completion_not_applicable=true`。
- 还没有回答：不能提交，提示完成总体评分；不会默认为 0 或“不适用”。

新增的完全空白活动卡可以略过；已经开始填写却缺时间、内容或心情的卡片不能悄悄丢弃。保存失败保留输入，允许重试。原有登录隔离、每日一份、防重复提交仍保留。

## M3 如何引导用户

M3 回复区增加“打开每日记录”入口，并显示“选时间 → 记活动与心情 → 回顾当天”。右上角“记录今日”原入口继续保留。

M3 的系统提示增加网页操作说明，即使管理员替换模块提示词，这段产品操作说明仍会拼入请求。首次介绍或用户询问时说明入口、三项必填、四项可选、三项总体评分与保存方法，不要求每轮重复，也不把填写当作进入后续模块的强制门槛。

示例引导：

> 每天可以花一点时间回顾。点右上角“记录今日”，或下面“打开每日记录”。例如选 19:00–20:00，写“散步”，再选做完后的心情。后面四项可以先不填，最后回顾今天的完成程度、活动量和整体心情；今天没计划就勾选“不适用”，然后保存。

提示词测试确认引导被拼入 M3、不会拼入其他模块；**没有用生产模型验证每次实际措辞**，也不保证模型逐字复述示例。

## 同批聊天改动

- 支持按当前轮次“停止生成”：停止未完成模型工作，保留用户问题，不把未验证的部分回复保存成完整回答，不启动该轮后续 Router。进入最终保存阶段则保留完整结果，避免半保存。
- 修复计时单独更新不显示：此前前端只比较消息文字、思考等字段，漏比较计时；现在仅计时变化也会刷新。历史确实没有计时的数据仍显示未记录，不伪造耗时。
- 删除聊天顶部“当前旅程”整块。必要的草稿核对与确认移到账号菜单“本次记录与确认”，保留版本号、内容哈希和勾选确认保护，切换聊天关闭弹窗。

停止控制当前适配单后端 worker。将来多 worker 时，必须为控制指令与会话状态增加共享存储／消息通道；网络拥堵、同会话锁等待或资源清理仍可能让停止确认有延迟，不能承诺瞬间结束。

## 实现文件

- `frontend/components/DailyAssessmentModal.tsx`：表单层级、三项总体评分、校验、N/A 与空值交互。
- `frontend/components/AssessmentHistory.tsx`、`frontend/lib/assessment.ts`：新旧量表展示及数据类型。
- `backend/app/models.py`、`backend/app/schemas.py`、`backend/app/routes/assessment.py`：版本、空值、接口校验与保存读取。
- `backend/app/prompts.py`、`frontend/components/Chat.tsx`：M3 引导和入口。
- `frontend/components/ConversationWorkspace.tsx`、`frontend/components/ProgramPanel.tsx`、`frontend/lib/api.ts`：计时同步、停止交互、紧凑确认入口。
- `backend/app/generation_control.py`、`backend/app/routes/chat.py`、图节点与 provider：后台取消和模型流清理。

## 验收与边界

- 最终完整后端回归：**719 passed, 1 skipped, 1 warning，90.61 秒**；警告为测试依赖的弃用提醒。
- 每日记录功能与历史测试：22 项通过；迁移计划安全测试：5 项通过。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。
- 浏览器脚本 `infra/qa/daily-record-0917.cjs`：1440/390/360 浅色与 1440/390 黑金，共五种组合，覆盖必填、无默认分、N/A 往返、可选清空、半填卡阻止、失败重试、新旧历史、M3 入口与确认请求。
- `infra/qa/stop-timing-0917.cjs`：1440/390/360，验证仅计时更新、停止与续聊、草稿保留、停止失败重试、完成边界竞态。
- 已检查必填层级、选填折叠、文字可读性、按钮间距、弹窗滚动、浅深色对比；未见页面横向溢出或浏览器运行时异常。截图位于 `.test-tmp/daily-record-0917/` 和 `.test-tmp/stop-timing-0917/`。
- 浏览器为本地 Playwright 脚本验收；当前会话未提供 `js_repl`，没有为此改动 Codex 配置。

## 上线前必须处理的数据库结构

脚本：`backend/scripts/migrate_daily_record_0917.py`。默认只打印迁移计划；必须明确给定目标数据库名，只有加 `--apply` 才执行。

1. 给 `assessment_entries` 增加 `scale_version`，历史默认 1，新代码写 2。
2. 把 `activity_logs` 的 `achievement`、`connection`、`enjoyment`、`importance` 改为允许 NULL。
3. 原情绪必填不变，不删历史字段，不改历史分数，不导入测试数据。

必须先备份并核对目标库、执行 dry-run，验证 MySQL 版本及在线 DDL 支持后再迁移和发布前后端。脚本拒绝错误库名和非 MySQL；指定短锁等待与在线 DDL，不支持时会报错而不是自动退化为阻塞式迁移。MySQL DDL 可能逐条提交，失败后应检查已生效结构；重跑会跳过已完成部分。

**已在生产 MySQL 8.0.36、`ba_coach_260908` 完成在线 DDL，复查无待执行迁移。** 服务器备份：`/opt/bacoach/backups/daily-record-before-20260917T051016Z.json`（0600），未下载到本地。其他环境不可跳过此结构迁移直接发布新 ORM。不适用记录和可选空值上线后，不能直接退回不支持这些值的旧后端；需要保留兼容读逻辑，不能用补 0 的方式回滚。
