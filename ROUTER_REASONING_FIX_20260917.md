# Router 异常修复、双思考入口与耗时显示

状态：已部署到 https://bacoach.xyz/ 并完成发布后验证，版本 `20260917T035754Z`（北京时间 2026-09-17 11:57:54 的发布批次）。没有同步数据库、修改模型思考参数、关闭中介或回复校验。

## Router 修复

线上错误为 `UnboundLocalError: runtime_for`。该函数只在“提取到了新目标”的分支导入，却在其他 V2 路径中也会被调用，导致回复之后的路由结果无法提交。

将导入移到所有 V2 路径都会经过的位置，保留原有事务、最新记忆读取、步骤门控和用户确认规则；不补写历史失败记录。

新增测试以独立解释器初始化真实 V2 列映射，在内存数据库执行后台任务调度、Router 判断和实际提交，验证 M1–M4 非新建目标路径均能保存路由思考、模型名称与耗时，更新同步版本号，保持已有新鲜记忆，不改写回复正文。模型使用替身，未连接外部服务。

另外在隔离解释器中还原旧分支错误，确认新测试会捕获 `UnboundLocalError` 和未提交的路由结果；没有为此改动磁盘源码或生产代码。

## 两个思考入口

- “查看回复深度思考”：只显示回复模型提供的内容与型号。
- “查看路由深度思考”：只显示后台路由提供的内容与型号。
- 两个按钮分别控制对应面板，同一时刻只展开一份，避免两类长内容挤在一起；再次点击可收起。
- 生成／路由等待与缺失内容分开提示；缺失时不拿另一模型的内容补充，不伪造思考。
- 保持暖色／黑金设计，窄屏按钮自然换行，长文本在独立面板内滚动；键盘 Enter/Space、展开状态及面板关联可用。复制消息仍只复制回复正文。

## 耗时显示与统计口径

回复面板显示两项：

- **思考阶段耗时（估算）**：首个思考片段至首个正文片段的时间差。例如 952ms 至 59766ms，显示 58.8 秒。这是可观测的流式输出间隔，不等于模型内部纯计算时间。
- **回复总耗时**：既有 `main_generation_duration_ms`，例如 61614ms 显示 61.6 秒；不含前置风险检查。

路由面板显示 **路由处理耗时**，来自 `router_duration_ms`，例如 17000ms 显示 17.0 秒。该指标包含字段提取和模块判断，不能解释成纯思考时间。

生成中且指标尚未返回显示“记录中”；历史没有记录或记录非法显示“未记录”，不伪造 0 秒。真实零值正常显示。思考阶段没有独立思考内容、起止缺失或时间倒序时不估算。

后端读取既有消息指标列，在对话详情的 `messages[].timing` 中投影三项白名单字段；使用专门的响应模型，不把耗时塞入给模型的通用消息或 prompt。没有新增数据库字段，不需要迁移，也不会上传本地数据。

## 验收

| 检查 | 结果 |
| --- | --- |
| 后端完整回归 | 682 passed、1 skipped，82.85 秒；1 条既有依赖弃用提示 |
| 新增时长投影测试 | 7 项通过，覆盖正常值、历史缺失、非法/不完整/倒序/负数、真实零值及字段白名单 |
| 新增 V2 后台路由集成测试 | M1–M4 共 4 项通过；旧错误可被新测试捕获 |
| 前端类型检查与正式构建 | `npm run typecheck`、`npm run build` 通过 |
| 本地浏览器 | 在 `http://127.0.0.1:3000/` 的正式构建、合成 API 下验收；1440、390、360px 三种宽度通过 |
| 交互 | 两入口切换/收起、键盘、复制正文、只有一种思考、两种均缺失、模拟 SSE 等待→返回、刷新后重新展开；时长分区、记录中、未记录文案均通过 |
| 视觉 | 查看按钮与文本层级、两主题对比、间距、长词换行、面板滚动边界、输入框可达性；无横向溢出或页面运行错误 |

实现遵循 frontend-design-codex 的现有设计系统复用原则。playwright-interactive 所需交互工具不可用，使用本地 Node Playwright 完成功能和视觉验收，没有修改 Codex 配置。浏览器截图在 `.test-tmp/reasoning-split-0917/`，均为合成内容；手机为浏览器模拟，不是实机 Safari。

本次文件：

- `backend/app/graph/nodes.py`：修复导入作用域。
- `backend/tests/test_background_router_v2_0917.py`：后台路由持久化回归。
- `frontend/components/Chat.tsx`、`ReasoningDetails.tsx`：两个独立入口及分开显示。
- `backend/app/schemas.py`、`backend/app/routes/conversations.py`、`frontend/lib/api.ts`：既有耗时指标的安全响应投影及前端类型。
- `backend/tests/test_message_timing_0917.py`、`backend/tests/test_conversations.py`：时长及兼容性回归。
- `infra/qa/reasoning-split-0917.cjs`：合成浏览器验收。

## 与长等待问题的区别

本次修复的是主回复完成后的 Router 代码异常，不是主模型约 60 秒的深度思考耗时。强制深度思考设置保持原样，不能宣称回复已经加速。主模型等待预算需另行调整；补充核实，前端已有 180 秒请求中止保护，但这不能改善本次 57 秒的等待体验。

## 线上发布与验证

- 使用 `infra/deploy/deploy.ps1 -SkipChecks -SkipDatabaseTasks`；本地完整检查已独立执行，服务器另行成功构建前端。
- 发布从 `20260916T203650Z` 切换至 `20260917T035754Z`，`/opt/bacoach/current` 指向新版本。
- 打包清单共 186 项，确认没有本地数据库、KnowledgeBase、测试集或验收数据；发布日志确认跳过数据库迁移与知识库导入。
- 后端和前端服务均 active，内部 `/health` 为 ok，公网首页 HTTP 200，未登录会话接口 HTTP 401。
- 线上 OpenAPI 已包含三项 `MessageTiming` 字段及消息详情关联。
- Router、API schema/投影、前端双入口组件和 API 类型共六个关键文件的 SHA-256 与本地逐一一致。
- 在真实线上构建上再次运行 1440、390、360px 浏览器验收，全部通过，无页面运行错误；API 全程拦截为合成数据，没有创建生产账号、聊天或测试记录。线上构建截图保存在 `.test-tmp/reasoning-split-0917-online/`。
- 这属于线上部署健康检查、代码一致性检查及合成 UI 验收，不冒称已经用真实用户、真实模型完成生产聊天端到端测试。Router 实际事务提交在隔离 V2 数据库中验证。

没有回滚工作区其他既有改动。本轮临时浏览器和本地 3000 前端验收进程均已关闭，线上服务保持运行。
