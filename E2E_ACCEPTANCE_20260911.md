# 真实模型端到端验收：暂不通过

> 历史失败记录。相关问题已修复并重新验收，当前结果见 E2E_ACCEPTANCE_FIXED_20260911.md。

日期：2026-09-11。GitHub 推送条件未满足，因此未提交或推送本轮项目进度。生产未部署、未重启，生产数据库未访问。

## 验收环境

使用服务器上的真实模型配置，仅加载 DeepSeek/Doubao 前缀的供应商参数，不加载生产数据库连接。当前实际返回的模型为 deepseek-v4-pro，经已配置的兼容端点调用。

代码在 /opt/bacoach/acceptance/contract-20260911 隔离目录运行。数据库为一次性内存 SQLite；账号、对话、知识材料均为合成测试。真实 FastAPI → 图编排 → 检索接口 → 真实中介模型 → 真实模块模型 → 真实 Router/抽取 → 数据库 → 计划确认 API。

检索使用合成材料的本地关键词检索，不代表生产知识库召回精度。API 调用为非流式。网页手机测试另用模拟 API，不将两者宣称为同一条真实浏览器到后端的连通测试。

## 三轮对话及结果

1. 用户提出晚饭后散步但膝盖疼、尚未确定计划。模型先询问身体情况，没有强迫执行；数据库留在 M2，没有待确认草稿。
2. 用户自主改选在家沙发读小说，每天晚饭后十分钟，并给出忘记的障碍和放书提醒的应对。模型完整展示目标卡，数据库尚无草稿。
3. 用户在聊天中确认上述计划和个人意义。后台随后完成草稿抽取，全部必要字段存在，can_confirm=true，数据库仍在 M2；真实确认 API 成功后才进入 M3。

三轮回复均成功持久化，并在重新读取聊天时保持一致。各轮从请求开始至后台状态和抽取完成分别约 35.96、34.94、23.76 秒，不是首字延迟或统计分位数。中介三轮约 7.932、7.726、4.577 秒；第三轮明确不采用知识，最终通过片段数为 0。

## 阻塞问题 E2E-01：回复提前宣称计划锁定和模块切换

第三轮真实回复：

> 收到确认，目标卡片已锁定。接下来进入模块三，我们一起把计划落实到行动中，看看实际执行时会有哪些发现和调整。

同一时刻实际数据库是 current_module=module_2，can_confirm=true，尚未发生网页确认写入。

用户在聊天中表达同意确实存在，因此不能简单说用户没确认过；问题是当前产品把正式记录提交及模块推进放在网页确认接口，而模型把对话中的同意表述成已完成系统提交，导致两套状态不一致。

代码核实：answer_validator 的 M2 正则没有覆盖“目标卡片已锁定”“接下来进入模块三”。已有 confirmation_claim 检查也仅为 review，不会阻止发送。校验器接口没有接收运行态和实际确认状态，不能可靠核对提交宣称。

## 整改方向

1. 保持当前正式网页确认机制：Prompt 明确区分口头同意与系统保存，在按钮确认前只引导“请在目标面板核对并确认”。
2. 回复校验器获得最小权威状态，检查计划是否正式确认、当前模块和允许的动作，不能只扩大一条正则就宣称语义验收完成。
3. 将此次真实回复作为固定反例，并覆盖非流式、流式、刷新前后；重跑真实模型场景。
4. 另验证草稿修改/撤销与通知用语，再通过真实网页连接隔离后端验收。全部通过后再进行密钥与数据检查、Git 提交和推送。

现有脚本最初只断言 API 流程通过；人工审阅发现上述问题后，已增加该反例的验收拦截，不把 API 成功标成完整验收通过。

## 已通过与未覆盖

- 已通过：真实模型澄清身体不适、三轮回复持久化、完整计划抽取、正式确认后 M2→M3。
- 已通过：手机 390×844 模拟 API 网页目标选择、确认勾选、流转和无横向溢出。
- 已通过：测试工作台模拟 API 网页入口、六列创建编辑、导入预览确认、筛选、批量运行、人工评价、追问历史、CSV 下载、移动端及无浏览器异常；没有使用原始 Excel 文件重新验收导入内容。
- 未通过：回复宣称与数据库权威状态一致性。
- 未覆盖：真实浏览器连接隔离后端全链路、真实模型流式、多用户长时间并发、生产知识库召回质量。

结论：暂停 GitHub 推送不是连接或权限问题，而是遵守“验收没问题后再更新仓库”的发布条件。
