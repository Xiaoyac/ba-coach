# RAG 准确性增强与 4.1 Flash 切换报告

日期：2026-09-17。结论：取得了可复现的检索改善，但不能把旧 Holdout 的高分当成真实用户准确率。本次保留了失败实验、退步案例、调用成本和新增冻结测试结果。

> 后续上线状态（北京时间 9/17 04:47 更新）：用户随后要求将包含本轮 M4 的全部应用改动上线。发布 `20260916T203650Z` 已完成，生产已显式启用 `KNOWLEDGE_RETRIEVAL_MODE=catalog`，工厂实际创建 `CatalogDatabaseKnowledgeBase`；4.1 Flash 与下游中介保持启用。生产原有 1,331 个知识片段正常加载，没有导入本地数据。两条 BA 合成问题检索并通过中介，比特币价格问题返回空。下文“尚未上线／只有模型配置已上线”等措辞记录的是原实验报告完成时的状态，已被本条发布记录更新；离线指标没有因上线而重算，也不变成线上准确率。

## 1. 先说做了什么，以及哪些已经上线

1. **已上线的只有模型配置切换**：生产 DeepSeek 主模型和共享 Router 均明确改为 `deepseek-v4.1-flash`。默认 provider 为 DeepSeek，其他 provider 未替换。服务正常，实际响应型号确认是 4.1 Flash。
2. 实现了新版 **BM25F 关键词检索**：正文长度归一化、标题证据、否定活动处理、独立阈值、索引刷新和模块隔离。接入了数据库检索的显式 `enhanced` 开关。
3. 实测了 **本地向量召回＋BGE cross-encoder 重排**。它在本次开发集上没有胜出，而且很慢，保留为实验，不启用到用户请求。
4. 发现 BM25F 虽然把旧 Holdout 命中率提高到 100%，却在新问法上明显失败。因此继续实现了 **“明确标题走快速检索，其余问题走目录语义选节＋正文证据判定”**。
5. 新语义方案使用 4.1 Flash，输入只有用户问题/上下文、真实目录和候选正文，**没有测试标准答案、gold 标签或案例 ID**。增加了真实数据库运行适配器，支持显式注入聊天流程。
6. 新增两份先冻结、后测试的合成题集；旧案例、旧标签全部保留，没有删难题、改答案或把空返回算成正例命中。
7. 完整后端验证：**649 passed，1 skipped**。有一个既有 Starlette 弃用提示，无测试失败。

**上线边界必须明确：新检索算法尚未部署，也没有自动替换生产 P0 检索。** 当前生产只切换模型；本地默认检索开关仍为 `p0`。目录语义方案有可用的运行适配器，但没有接入生产默认实例。本轮没有同步本地测试数据库、用户记录或数据库结构。

## 2. 最关心的新旧指标

### 2.1 旧 Holdout：44 题，28 个正例、16 个应返回空的对照

“旧方法”是当前 P0＋意图门控，不是更早的未过滤版本；“新语义方案”是本次冻结的目录语义方案＋标题快速通道。

| 指标 | 旧 P0＋门控 | 新语义方案 | 差距 |
|---|---:|---:|---:|
| Hit@K：正例至少命中一个已标相关片段 | 92.86% | 92.86% | 不变 |
| Recall@K：已标相关片段的召回率 | 82.14% | 81.25% | **-0.89 个百分点** |
| 返回结果平均精度 | 49.75% | 71.60% | **+21.85 个百分点** |
| 仅正例的返回结果平均精度 | 58.63% | 71.60% | +12.97 个百分点 |
| 无关问题正确返回空 | 68.75%（11/16） | 100%（16/16） | **+31.25 个百分点** |
| MRR：首个相关结果的排序质量 | 0.8780 | 0.9286 | +0.0506 |
| nDCG@K：整体排序质量 | 0.8294 | 0.8440 | +0.0146 |
| 正例错误返回空 | 0/28 | 1/28 | **增加 1 题** |
| 意图门控误跳过正例 | 0 | 0 | 不变 |

因此不能说“所有指标都提高了”。这版明显减少无关知识、改善排序，但在这份旧集上牺牲了一小部分召回。

具体退步：`holdout-12`“太极活动是什么类型？”旧方法找到了 Compendium 的相应条目；新语义选节没有形成有效结果，返回空。它体现了**大类目录没有充分暴露具体活动名称**的缺陷。没有在验收后临时补一条“太极答案”再改写成绩。

报告采用 2,000 次固定种子的配对 bootstrap。旧 Holdout 精度提升的近似 95% 区间为 **+5.77～+38.33 个百分点**；正确返回空提升为 **+8.33～+53.85 个百分点**。Hit/Recall 的差值区间跨过 0，不能宣称这两项有可靠统计提升。小样本和合成题偏差不会因为计算区间而消失。

### 2.2 不能只看旧 Holdout：自然表达和新题

以下各组保持相同语料、模块范围、K 值和原始标签，分别报告，不合并成一个好看的大分数。

| 数据集 | 正例/负例 | Hit：旧→新 | Recall：旧→新 | 返回精度：旧→新 | 正确返回空：旧→新 |
|---|---:|---:|---:|---:|---:|
| Dev，开发选择用 | 28/16 | 92.86%→92.86% | 69.73%→77.19% | 47.04%→71.43% | 81.25%→100% |
| 原语义压力集 | 8/4 | **12.50%→87.50%** | 3.12%→79.17% | 5.00%→62.50% | 100%→100% |
| 第一份新增集，48 题 | 30/18 | **13.33%→50.00%** | 8.07%→45.56% | 8.04%→26.04% | 55.56%→88.89% |
| 第二份新增冻结集，24 题 | 16/8 | **0%→62.50%** | 0%→55.21% | 0%→31.25% | 62.50%→100% |

重要解释：

- 第二份新集的 62.5% 是 **16 个正例命中 10 个**，不是整个系统准确率；仍有 6 个正例未命中原标签。
- 第一份新增集在发现 BM25F 失败后已用于问题分析，所以第二阶段把它明确降格为**回归集**，不再叫未见过的盲测。
- 第二份新集在新方案冻结后首次评测；没有看完结果再改参数。它仍是助手编写的合成题，不是独立专家认证的临床测试集。
- 所有指标都衡量检索片段与当前标签的一致性，**不等于最终回复正确率、心理干预安全性或临床效果**。

## 3. 为什么没有直接选“旧 Holdout 命中 100%”的那版

第一轮 BM25F 的确拿到了更漂亮的旧集成绩：

| 旧 Holdout 指标 | P0＋门控 | BM25F＋门控 |
|---|---:|---:|
| Hit@K | 92.86% | **100%** |
| Recall@K | 82.14% | **88.39%** |
| 返回精度 | 49.75% | **75.27%** |
| 正确返回空 | 68.75% | **81.25%** |

但是，第一份新集上 BM25F 的 Hit 只有 **3.33%**，低于 P0 的 13.33%；正例空返回从 10/30 增加到 20/30。原压力集 Hit 也仍只有 12.5%。这说明旧题中较多专业术语/标题表达让词法方法占优，不能外推到自然说法。

例如，资料写“行为激活的结构：由外而内”，用户却说“为什么不是先等心情变好才安排事情？”语义相关但字面重合不足。单纯提高覆盖度和相对分数阈值，反而会把需要的材料挡掉。

所以本次保留 BM25F 作为快速路径和可回退的独立候选，**没有因为旧 Holdout 100% 就宣布它适合全量上线**。

## 4. 新旧方法具体怎么运作

### 旧方法

意图门控 → 中文 n-gram / 英文词匹配与少量双语扩展 → 最低分、覆盖度、相对分数和来源上限过滤 → 取 K 个候选 → 现有中介给使用建议 → 模块回复 → 共享 validator。

优点是便宜、确定、延迟低。缺点是“意思一样但用词不同”时很容易漏召回；通用词、历史上下文和长文本也可能干扰排序。

### 新方法 A：改良关键词检索

- 对正文长度做归一化，降低长片段仅因出现很多词而占优的情况。
- 标题与正文都可以提供证据；文件名不能单独充当证据。
- 定义问题适当优先定义章节；保留双语活动名扩展。
- 对“我不想跑步，想游泳”这类明确否定做保守处理，不再把被否定的活动当正向扩展词。
- 独立参数为最低分 0.1、覆盖度 0.25、相对分数 0.55、每来源最多 2 条。这些分数不是概率。
- 只在 Dev 上探索 16 组阈值组合，选取表现稳定的区间；保留完整网格结果。

### 新方法 B：目录语义检索＋证据判定（更值得继续完善的候选）

1. 沿用当前意图门控，不增加一个单独的 LLM 意图分类器。
2. 如果用户当前这句话明确包含首个命中章节的完整标题，走改良词法快速路径；历史消息中出现标题不算当前明确点名。
3. 其他问题交给 4.1 Flash，输入是**当前模块允许的真实章节目录**。模型理解口语、同义词、否定和跨语言表达，最多选 4 个章节。
4. 根据选中的章节从同一知识快照中取正文，轮流分配候选位置，防止大章节挤掉其他章节；最多 32 个候选。
5. 再让 4.1 Flash 检查正文是否真的能帮助回答，输出排序后的合法片段 ID，不让它自己编造“知识”。
6. 校验 ID、模块、数量及每来源上限。无相关材料、异常、非法输出或超时均不放行原始候选。
7. 接入聊天时仍须经过现有中介与共享 validator。**本次没有绕过原来的中介，也没有给新检索器写用户档案或推进模块的权限。**

调用数量：快速路径 0 次新调用；语义路径选节 1 次，存在候选时再判定 1 次。现有中介仍是另外一次调用，不假装已经把它们合并。

运行适配器为 `app/catalog_knowledge_base.py` 的 `CatalogDatabaseKnowledgeBase`，以真实数据库知识和运行时 provider 为输入。它不读取离线题目、标准答案、分数缓存或 SSH 实验脚本；总检索超时为 12 秒，失败返回空。

## 5. 向量数据库和 BGE 为什么没有直接启用

这次真实尝试了本地 multilingual MiniLM embedding、Qdrant、关键词/向量 RRF 融合，以及 `BAAI/bge-reranker-base` cross-encoder。不是只写了方案。

- RRF 只融合排名，不把它误当成相关概率。
- BGE 对完整长片段做 token 窗口覆盖，防止正文尾部被静默截断；分数是 logit，不是校准后的概率。
- 融合后的候选再次检查模块范围；未知 ID 明确报错。
- 模型/候选内容进入缓存指纹，缓存结果与真实推理耗时分别记录；缓存写入做原子替换。

Dev 上探索 48 组重排阈值。该网格中最高 Hit 只有 **89.29%**，低于 P0 的 92.86%；一个最高 Hit 的配置（最低 logit=-6、分差=3）返回精度 52.38%、正确返回空仅 56.25%。不能仅因用了向量或 reranker 就认为更准确。

本机 CPU 对实际进行了重排的 38 个查询，原始推理均值约 **20.31 秒**、P95 约 **31.36 秒**。重放命中缓存的速度不是这些数字的替代品。该方案不进入生产默认依赖，也没有把本地 Qdrant 当成已经上线的向量服务。

百炼 embedding 的小请求在当前 Key 上返回了 AccessDenied；没有擅自改权限或开通新收费项。4.1 Flash 的可用性与 embedding 的权限是两件事。

## 6. 延迟、调用量和成本

旧 Holdout 的观测值：

| 方案 | 均值 | P95 | 口径 |
|---|---:|---:|---|
| P0 本地检索 | 17.53 ms | 33.61 ms | 不含数据库、网络、主回复 |
| BM25F 本地检索 | 9.92 ms | 21.62 ms | 不含数据库、网络、主回复；不同实验轮次 |
| 新语义方案的新增模型阶段 | **1,470.82 ms** | **2,953.93 ms** | 原始选节＋判定 API 耗时；无调用轮次按 0 计 |

新语义方案的时间**还不包含原中介、主回复、数据库读取、索引初始化和本地 CPU 开销**，也不是网站端到端 SLA。它不是“免费增加准确率”。

目录较长，成本主要是输入 token：旧 Holdout 44 题的逻辑工作量为 54 次新模型调用、499,810 个输入 token、681 个输出 token；其中 7 题走标题快速路径，4 题被原门控跳过。

本次目录实验的本地唯一请求缓存共记录 **259 次请求、2,399,249 个输入 token、3,700 个输出 token、0 次记录到的 provider 错误**；包含全语义 Dev 和快速通道实验，不包括前面的少量连通性探针。不把重放缓存重复算作新的付费调用。金额应以百炼账单和实际缓存计费为准，未假定单价。

后续成本优化应优先做目录摘要/分层、静态前缀缓存，以及把“正文相关性判定”和现有中介合并成一个受约束调用；合并后需要重新验收，不能直接沿用本报告分数。

## 7. 仍然存在的问题，不能隐藏

1. **活动名称藏在大类正文里**：太极、瑜伽、爬楼等名称未必出现在章节标题，目录选择有时选成一般目标建议，未取到活动分类条目。下一步应做通用的活动实体索引/章节内容摘要，而不是给测试题硬编码答案。
2. **多轮指代仍可能偏离**：“我想选散步”之后问“这个能用来做目标吗”，新方法可能只找目标理论，丢失具体活动这个实体。
3. **标签不完备**：例如“从活动记录里看什么”，返回“活动与情绪的联系”章节可能同样相关，但单一 gold 只接受“活动监测的回顾”。这会低估 precision。小样本证据见 `backend/evals/reports/rag0917/label_audit.md`；本次没有改标签给自己加分。
4. **模块边界标签需要业务裁决**：BCT 的某些主题在允许共享的 BA 材料里也有支持，不能一概因为某个专门类别不在本模块就认定必须空。仍按原标签计分，待专家复核。
5. **新题绝对成绩还不够高**：第二份新集返回精度仅 31.25%、命中 10/16。虽然比旧版明显改善，仍不能把它称为成熟的高准确率生产 RAG。
6. **缺少完整回答质量评测**：当前比较的是检索结果，新方法使用额外 LLM，旧基线是中介前的 P0 输出，推理预算不同。没有把“现有中介后的实际旧回复”和“新方案完整最终回复”做盲评，因此不能推断最终回答精度也提高 21.85 个百分点。
7. **LLM 有波动**：本次模型温度设为 0，也不等于完全确定。每题保留本次首次响应作配对评估；生产滚动稳定性还需重复抽样验证。

本次所有失败、未命中和退步样本均在 JSON 中，未过滤；`summary.json` 列出了逐项改善与退步。

## 8. 4.1 Flash 切换的证据和限制

- 当前生产 Key 直接请求旧名称 `deepseek-v4-pro`：403、API Key restriction。这个观察不等于否定其他网关可能配置了旧名映射。
- 直接请求 `deepseek-v4.1-flash`：成功，响应型号也是该名称。
- 仅修改 `/etc/bacoach/backend.env` 的 `DEEPSEEK_MODEL` 与 `DEEPSEEK_ROUTER_MODEL`，随后只重启后端。私有备份：`/etc/bacoach/backend.env.before-flash-20260916T192433Z`。
- 后端服务 active，健康检查正常。健康检查本身不证明模型可用，因此额外验证了实际 provider 调用。
- 主回复、流式回复、思考式路由通过合成连通性检查；真实中介接口两次完成合法选片，真实 Router 契约返回 `module_2`、`error_code=null`，没有写业务记录。
- 一个极短非思考 JSON 复读探针出现过特殊结束标记，另一个等价措辞及 8 个参数/标签探针正常；没有把这次异常藏起来，也没有据此声称模型格式输出绝对可靠。实际中介继续严格校验，异常不放行知识。

## 9. 代码、测试与复现

核心文件：

- `backend/app/retrieval_enhanced.py`：改良词法排名。
- `backend/app/semantic_retrieval.py`：本地向量＋BGE 实验。
- `backend/app/semantic_catalog.py`：目录构建、选节与片段 ID 校验、快速路径。
- `backend/app/catalog_knowledge_base.py`：显式运行适配器、真实 DB 索引、超时关闭。
- `backend/app/retrieval.py`、`config.py`、`.env.example`：可选 BM25F 模式和独立阈值。
- `backend/app/graph/nodes.py`：遥测增加实际 `ranking_mode`；保留其他既有变动。
- `backend/scripts/compare_retrieval_0917.py`：配对指标、逐例记录与置信区间。
- `backend/scripts/tune_retrieval_0917.py`：仅 Dev 的网格记录。
- `backend/scripts/accept_retrieval_0917.py`、`accept_catalog_0917.py`：先冻结后验收，拒绝静默覆盖首次成绩。
- `backend/scripts/experiment_catalog_0917.py`：显式授权的真实模型离线实验，provider 错误不伪装成基线成功。
- `backend/scripts/summarize_rag_0917.py`：汇总既有结果，不运行模型、不修改标签。

完整后端验证命令（从 `backend` 运行，使用隔离测试目录）：

```powershell
.\.venv\Scripts\python.exe -m pytest -q -o addopts= --basetemp=../.test-tmp/rag-new-run -p no:cacheprovider
```

结果：649 passed，1 skipped。重点覆盖未知/越权 ID、模块范围、空选节不回退、每来源上限、标题唯一证据、候选预算、索引导入刷新、超时、损坏评分、token 尾部覆盖，以及中介超时后原始片段不得进入模块 prompt。

比较当前代码与旧方法的离线 Dev（不调用模型、不打开业务数据库）：

```powershell
.\.venv\Scripts\python.exe scripts/compare_retrieval_0917.py --dataset evals/retrieval_cases.json --split dev --mode both --output evals/reports/rag0917/dev_recheck.json
```

目录语义实验会产生真实模型费用，默认不会随应用启动运行；重放已保存响应不会产生新调用。重新调用服务可能得到不同结果，应保存为新实验，不覆盖本次冻结成绩。

主要证据都在 `backend/evals/reports/rag0917/`：

- `selection_freeze.json`、`catalog_selection_freeze.json`：源码、题集哈希及选择依据。
- `dev_tuning.json`：16 组词法、48 组 BGE 实验及真实重排耗时。
- `holdout_accepted.json`、`blind_accepted.json`：BM25F 成功和失败两面。
- `cascade_holdout.json`、`cascade_stress.json`、`cascade_regression_blind_v1.json`、`cascade_fresh_v2.json`：新语义方案首轮成绩、逐例结果、模型用量和原始调用耗时。
- `summary.json`、`label_audit.md`：差值、置信区间、退步清单、标签审计。

第一份新集 SHA-256：`935df5faf15dedcecf9d4c6585ca77dff2692b0864b77f42167f1d3562b3208b`。

第二份新集 SHA-256：`c57727328b89ad7d090fc418682bc40b81fb45ff34f2d141164f540aef7a6502`。

## 10. 建议的下一步与上线门槛

不是继续只追旧 Holdout 的 100%，而是优先：

1. 让业务组复核相关片段标签，允许一个问题对应多个合理来源；修正需要专家裁决的模块边界，并另建真正未参与开发的测试集。
2. 做语料驱动的活动实体索引、目录内容摘要和多轮实体保留，解决目前已公开的失败类型。
3. 把目录判定与现有中介的重叠工作合并，降低额外延迟和输入 token；保持失败时不放行原始召回知识。
4. 增加最终回复的证据一致性、错误建议、模块越权、完整响应时间和真实成本验收，再做小范围灰度。

监控至少分开看：Hit/Recall、正例错误空返回、负例正确空返回、返回精度、目录/中介异常率、模型调用数、输入 token、检索及完整回复 P50/P95。**空返回率高本身不是优化成功，必须和正例漏答一起看。**

本次结论是：已经找到了比当前词法检索更能处理自然表达的实现，并量化了收益与成本；但尚未证明其达到全面替换生产检索的质量门槛。模型切换已上线，检索代码保留在本地待进一步验收与明确部署。
