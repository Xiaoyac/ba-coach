# 消息、档案与每日记录修复

2026-09-09；发布版本 `20260909T140038Z`。

## 修复内容与原因

1. **回复一直转圈**：原有补偿轮询在发送过程中暂停，且流式客户端一直等连接结束。手机网络或代理不关闭连接时会卡住。新增保存完成 `persisted` 事件，收到后结束读取；发送期间每 5 秒检查持久化回复，匹配本次输入及消息位置后恢复。设置 180 秒等待上限、20 秒后的等待文字，避免无限转圈。单次补偿请求也有超时。
2. **消息一闪而过**：旧快照可能晚到并覆盖新回复，失效凭证也可能静默进入不保存数据的匿名路径。会话详情增加现有 revision 字段，前端拒绝旧版本；未同步完成时保留当前回复，不接受更短快照。携带失效凭证的聊天现在返回 401，不再降级匿名；无凭证的既有匿名 API 能力保留。
3. **我的每日记录**：所有已登录被试的侧边栏增加独立入口，打开即显示历史记录。沿用现有按登录身份隔离的查询，不允许指定他人身份。记录今日入口保留，手机图标增加可访问名称。
4. **档案 Not authenticated**：401 引导重新登录并说明已保存记录不会丢失；存储被浏览器禁止时保留页内凭证。档案读取增加 15 秒超时及重新加载按钮，服务端异常显示中文提示。截图能确定是身份验证失败，不能仅凭截图断言一定由过期而非撤销或凭证丢失造成。
5. **Markdown 源格式露出**：助手消息渲染标题、加粗、列表、引用、代码及 HTTP(S) 链接；用户原文、复制内容不改。渲染为 React 文本与元素，不执行模型输出的 HTML 或脚本。不是完整 Markdown 方言实现，复杂表格等尚未支持。

## 验证

- 主修复全量后端回归：406 通过、1 跳过；追加失效凭证保护后，认证及新增专项测试 50 通过。
- 前端生产构建及类型检查通过。
- 390×844 手机尺寸的 Edge 浏览器合成测试通过：保存完成但连接无 EOF、流停住但服务器已有回复、旧 revision 晚到、Markdown 安全渲染、普通被试每日记录入口、档案 401 后登录提示。不是实际 iPhone Safari 设备实测，也不使用真实被试数据。
- 新版本只发布代码，不执行数据库建表、字段迁移、数据回填或知识库替换。启动维护保持关闭。

## 使用与限制

访问 https://bacoach.xyz/ 后刷新；手机如仍持有旧页面可关闭该标签再打开。登录失效时需要本人重新登录，不可能安全地绕过验证。

本次修复连接恢复及状态一致性，不代表模型推理变快。中介、风险判断和主模型仍会产生真实等待。180 秒超时后客户端停止等待并继续同步；不会自动重发用户消息，以免重复执行。已保存记录可以恢复，服务器从未保存的内容不能保证恢复。

发布保留旧版本。代码级补丁额外更新了该版本的 `backend/app/identity.py` 并重启服务；后续打包应以本地完整源码为准，而非早先上传的压缩包。
