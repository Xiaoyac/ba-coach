# 本地 UI 与 PA 目标档案验收

## 范围与设计

仅本地修改，不部署线上、不上传或覆盖现有数据库。保留聊天、目标确认、账号与管理功能。
视觉方向为温暖、安静的行动练习空间：燕麦色侧栏、象牙色对话区、无框 AI 回复、柔和用户气泡；暗色保留黑金。
侧栏宽 232px，可收起；顶部合并进度与账号工具；主题和问题反馈收进侧栏。
目标卡片是可点击的档案入口，不加入打卡排名、惩罚或强制填表。目标仍只能从 Agent 对话产生。

## 验收范围

- 桌面 1440×900、宽屏 1840×900、手机 390×844 和窄屏 360×740：检查可读性、对齐、气泡、顶部、输入框、无横向溢出或遮挡。
- 侧栏收起/恢复、手机抽屉和键盘关闭、浅色/黑金切换、普通账号与管理员工具。
- 目标列表打开/关闭、搜索与无结果、清除筛选、类型/状态/排序；覆盖草稿、暂停和归档。
- 目标详情三种历史、旧版本、支持条件、执行周期翻页、已更正活动标识；回列表保留筛选。
- 异常探索：接口失败后重试；快速切换标签/目标；空账号无目标；窄屏长内容。
- 保留的流程：新对话选择目标、当前记录展开、每日记录、账号入口、帮助反馈；不调用外部模型进行视觉验收。
- 前端类型与构建；后端全量自动化测试；审查权限边界和只读历史接口。

## 本地演示

`infra/qa/seed_local_ui_preview.py` 只能创建固定的 `.test-tmp/ui-preview-0917.db`，拒绝覆盖已有文件，不读取数据库连接配置。
资料均为合成演示，演示聊天首条注明；SQLite 文件被 Git 忽略，不应发布。
预览账号 `localpreview`，密码 `LocalPreview0917!`；管理体验 `localadmin`，空档案体验 `localempty`，密码相同。
后端启动时强制指定此 SQLite 与 V2、关闭启动维护，仅监听本机。无外部模型密钥；预览不代表真实模型回答验收。

## 实现方式

### UI

1. 原先上下两层的进度栏与聊天工具栏合并为一个 76px 顶栏；进度详情按需展开，不一直挤占聊天高度。
2. 侧栏固定为 232px，桌面支持隐藏/恢复，手机使用抽屉并限制键盘焦点在抽屉内。普通用户不显示管理工具，管理员默认收起工具。
3. AI 消息去掉包围边框，用户使用燕麦色/金色柔和气泡；正文和输入框独立限制阅读宽度，宽屏不会无限拉长文字。
4. 帮助反馈和主题切换不再悬浮遮挡输入框。默认浅色；已保存的主题偏好仍有效。
5. 档案采用任务卡片列表，支持关键词、主/次/待归类、生命周期与排序。点卡片进入计划、执行与复盘、活动三类历史。
6. 手机详情减少重复标题区，三个历史入口在 360px 宽度可见；黑金弹窗使用不透明底色，避免透出后方对话。

### 目标档案与边界

- 新增 `GET /api/program/goals/{goal_id}/history`，登录后先验证目标归属。未知/其他用户目标返回 404，未登录返回 401。
- 计划、周期、活动分别计数与分页。页面每次取 12 条；历史版本和被更正活动不会被静默覆盖或截断。
- 只返回供界面展示的字段，不返回原始审计引用、内部用户标识或来源消息 ID。周期关联计划时再次限制同一目标，防止异常关联泄露。
- 草稿、历史版本和被更正活动分别标注，避免把未确认计划当成承诺、旧事实当成当前记录。
- 打开档案不切换当前聊天目标；仍在新建对话时选择继续已有目标，或回到聊天讨论新目标。
- 当前聊天信息只是「当前对话目标」角标的辅助信息，加载失败不再拖累整个档案。
- 未归属目标的活动仅展示近期条目，并明确标注为「近期」，不是全部临时活动档案。
- 本次无数据库迁移，没有创建手工目标写入入口，也没有改动生产数据。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 后端全量测试 | 567 passed，1 skipped；1 条既有依赖弃用警告 |
| 前端类型检查 | 通过 |
| 前端构建 | 通过；包含框架类型与 lint 检查 |
| 历史/权限专项 | 包含在全量测试中：历史版本、更正记录、分页边界、未登录/其他用户、异常跨目标关联 |
| 桌面 | 1440×900、1840×900，侧栏收起/恢复、合并顶栏、长输入框正常 |
| 手机 | 390×844、360×740，聊天、抽屉、目标列表/详情/分页正常；无页面横向溢出 |
| 主题与视觉 | 浅色、黑金均检查：正文对比、气泡间距、侧栏比例、弹窗层级、输入框/按钮不遮挡 |
| 目标交互 | 六种目标样例；关键词、无结果/清除、类型、五种状态筛选、三种排序、返回保留筛选、历史翻页通过 |
| 异常探索 | 模拟 503 后重试恢复；快速返回/更换目标未显示旧目标内容；辅助聊天接口失败仍可读档案 |
| 现有入口回归 | 新聊天选已有目标、记录进度、每日填写/历史、账号菜单、反馈截图窗口、管理工具展开/收起通过 |
| 用户隔离 | 第二个真实本地账号显示空档案，没有看到第一个账号的六个目标 |
| 浏览器运行错误 | 已测试页面未出现未捕获 JavaScript 错误 |
| 可见窗口 | 已打开 Chrome 的 `http://localhost:3000/` 并登录 `localpreview`；实际内容区 1424×855，无横向溢出，输入框可见 |
| 演示资料 | 新建隔离 SQLite；演示生成脚本与外键校验通过；数据库/截图目录被 Git 忽略 |

截图位于 `.test-tmp/ui-qa-0917/`：`desktop-chat.png`、`desktop-goals.png`、`desktop-plan.png`、`dark-chat.png`、`dark-goals.png`、`wide-chat.png`、`mobile-chat.png`、`mobile-goals.png`、`mobile-360-plan.png`、`admin-chat.png`。

## 遇到并解决的问题

- 初版黑金档案沿用半透明面板，能看见背后的聊天：新增主题化的不透明弹层色。
- 360px 手机详情重复标题占用过高：详情模式压缩装饰文字和标签间距，不压缩正文。
- 聊天状态接口异常会让已加载的目标列表也报错：辅助请求与档案主体加载分离。
- 演示邮箱使用 `.invalid` 被后端校验拒绝：改成文档专用 `example.com` 地址，不发送邮件。
- 早期演示中「进行中目标」缺少周期，被后端拒绝续聊：补全合成数据的周期/复盘关系，没有放宽业务校验。

## 本次文件

- `frontend/components/GoalOverview.tsx`、`frontend/lib/program.ts`：目标档案与历史读取。
- `frontend/components/Chat.tsx`、`ConversationSidebar.tsx`、`ConversationWorkspace.tsx`、`ProgramPanel.tsx`：对话布局、抽屉与入口整合。
- `frontend/components/ThemeToggle.tsx`、`AuthGate.tsx`、`frontend/app/page.tsx`、`globals.css`：主题入口、壳布局、共用控件样式。
- `backend/app/goal_history.py`、`backend/app/routes/program.py`、`backend/tests/test_goal_history_0917.py`：只读历史接口及权限/历史测试。
- `infra/qa/seed_local_ui_preview.py`：固定本地位置的合成演示资料生成脚本。

工作区本来已有其他未提交改动，本次没有清理、回滚或发布它们。

实现遵循 frontend-design-codex 的一致性与轻量布局原则；验收按 playwright-interactive 分开做功能、视觉和窗口尺寸检查，不以构建成功替代浏览器验收。后台验收浏览器已关闭，用户可见浏览器和本地服务有意保持运行。

当前本地前端为开发预览（端口 3000），独立测试后端为端口 8000；最终构建也已另行验证通过。`http://localhost:3000/` 实测返回 200。没有启动或修改线上服务。

## 尚未覆盖 / 使用说明

- 当前是本机视觉/交互预览，不是线上部署。原线上网页不会因为这次操作改变。
- 本地没有配置外部模型密钥，不支持验收真实 LLM 回答；没有把合成聊天冒充实时模型生成。
- 手机使用 Chromium 触屏视口仿真，不等同于 iOS Safari 真机测试；极端长标题/超大系统字体仍建议上线前做真机回归。
- 未重测所有原有管理后台流程；本次验证的是改变的界面及相关业务入口。
- 页面布局允许聊天记录、历史卡片在各自内容区滚动；不是把长历史压进一个屏幕。
