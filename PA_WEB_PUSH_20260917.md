# PA 活动后提醒：实现与启用说明

## 当前状态

**后续更新：现在线上版本为`20260917T100414Z`，新增「一分钟后测试通知」及关页显示回执，另增独立诊断表`pa_push_checks`。**使用方法、验证证据和真实限制见[关闭网页后接收提醒报告](PA_CLOSED_PAGE_DELIVERY_20260917.md)。仍未获得真实设备送达验收证据。

## 首次上线记录（20260917T094115Z）

**已于2026-09-17部署至 https://bacoach.xyz/，复用现有广州阿里云服务器。** 发布版本为`20260917T094115Z`，前后端及独立提醒worker均已启用。生产开关已开启，但每位用户仍须主动授权；验收时订阅数、发送记录数均为0，没有代用户授权或发送测试通知。没有上传本地测试资料或密钥。

**重要限制：服务器实测无法连接Google FCM（443连接超时）**，因此Chrome及其他依赖FCM的浏览器不能保证收到通知。苹果和Firefox入口返回HTTP响应，只证明网络可达，**不等于真机送达验收通过**。目前尚未完成真实设备关闭网页后的收通知测试。

规则：**预计开始时间 + 明确时长 + 15 分钟**。到时未反馈才提醒；一次执行在每个已授权浏览器最多尝试发送一次。多个已授权设备可能各收到一次，不是强制只选择其中一台。

例如用户明确说「今天下午4点散步15分钟」，预计16:15结束，16:30进入可提醒窗口；只说「下午4点散步」而没有时长，不安排。周期性目标不会为了提醒另建目标或复盘周期。

## 已实现

### 用户界面

- 「我的目标」以及右上角账号菜单都提供「活动后提醒」设置入口。
- 只有用户主动点击开启，才申请系统通知权限；聊天中的口头同意不能替代授权。
- 支持关闭当前浏览器、关闭所有设备；退出登录或登录失效后停止该登录状态的提醒。
- 展示已识别的近期时间。不明确的计划仍保留，不要求用户另填表。
- iPhone/iPad未以主屏幕应用打开、权限被拒绝、浏览器不支持、服务未启用、保存失败都有说明。
- 遵循现有 frontend-design-codex 风格：暖色／黑金主题、字体层级、圆角、44px按钮、手机可滚动弹窗。没有更改聊天或每日记录的业务规则。

### 后台安排

- 单独的 Python worker 每轮完成后等待30秒，不依赖网页是否打开，不依赖浏览器的计时器。实际延迟还包括扫描和网络请求，不是精确秒级闹钟。
- 只考虑进行中目标的当前已确认计划，以及最新等待执行／复盘中的周期。计划草稿、已暂停、取消、被替换、失去来源对话或确认依据的记录不触发。
- 自然语言安排须能在用户原话中找到依据。相对日期按该原话的发送时间和计划时区解释，不使用模型猜出的日期直接发送。
- 支持明确日期／今天／明天、每天、明确每周星期几，以及明确钟点。UTC存储和判断；夏令时重复／不存在的本地时间不猜测。
- 有时长但没有开始时间、只有钟点没有日期／周期、每周五天、每周三次、晚饭后、左右、大概、多个备选钟点等，不安排。
- 新开启订阅不追发开启前已经开始的活动。
- 档案中「暂时不需要提醒」「仅在我主动找你时提醒」优先；已有提醒频率和时段也会限制发送。超过有效窗口不补发。

### 反馈、取消与去重

- 活动事件必须对应同一用户、同一目标、同一活动和本次日期；昨天的反馈或另一项活动不会自动抵消今天的提醒。
- 每日记录必须活动名称相符且明确时间段覆盖计划开始时间，不能因为填过当天记录就当作目标已反馈。
- 当前对话里明确的「做完了」「今天没做」「取消」「改到明天」等提供保守的抑制判断，不写入完成状态。
- 新用户消息到达后的两分钟内，如尚未识别反馈，会暂缓发送，给现有后台抽取留出时间。
- 发送前再检查计划、反馈、订阅、登录会话；同一目标＋本次开始时间＋设备的数据库唯一键防止重启或并行进程重复尝试。
- 推送请求超时、响应不明或进程在发送过程中崩溃，不自动重发，以免同一条通知重复打扰。代价是可能漏发，需要监控。
- 发送窗口为应提醒时刻后的30分钟，推送TTL不超过剩余窗口，Service Worker再检查过期时间；设备离线过久不堆积旧提醒。

### 隐私和安全

- 锁屏统一显示：

  > BA Coach · 活动后提醒
  > 如果方便，回来聊聊今天的活动吧。做了、没做或有变化，都可以。

- 通知没有目标名称、心理状态、聊天内容或用户姓名。内容使用标准Web Push加密；VAPID用于服务器身份签名。
- 点击只打开本站目标总览的新窗口，不覆盖已有未保存表单，也不跳转到推送载荷中的任意URL。
- API身份来自登录会话；不接受用户自己声明user_id。不允许把其他账号的订阅直接转绑。
- 仅允许已知浏览器推送服务的HTTPS地址，禁止自定义端口、凭证、片段及HTTP重定向，防止把订阅接口用于探测内网。
- 私钥只留服务器；读取接口不返回推送endpoint、加密密钥或登录凭证。Service Worker不缓存聊天页面、接口或登录token。
- 404/410订阅立即移除；退出／过期会话的endpoint由worker清理。发送元数据保留30天；已删除账号的发送元数据也会清理。

## 数据库与代码

仅新增两张独立表，不改已有业务表，不导入本地记录：

| 表 | 用途 |
| --- | --- |
| `pa_push_devices` | 用户主动授权的浏览器订阅、绑定登录会话、开启时间 |
| `pa_push_deliveries` | 每次执行／设备的唯一发送尝试及状态，不存活动正文 |

新metadata不参与业务启动自动建表；迁移脚本默认只打印计划，必须指定精确数据库名称并显式`--apply`。

主要文件：

- 后端：`app/pa_schedule.py`、`app/pa_push.py`、`app/push_schema.py`、`app/routes/push.py`；配置和主路由接入。
- 抽取与契约：`app/clinical_fields.py`优先保留用户时间原话；`app/goal_contract.py`禁止教练未经后台确认承诺提醒。
- 运行工具：`scripts/migrate_pa_push.py`、`scripts/generate_pa_vapid.py`、`scripts/pa_push_worker.py`、`infra/deploy/bacoach-pa-push.service`。
- 前端：`PushReminderModal.tsx`、`lib/push.ts`、`public/pa-push-sw.js`、manifest和安装图标；目标总览、账号菜单和退出登录接入。

## 验收

- 后端135项通过：新提醒测试＋目标历史＋每日记录＋账号认证；仅有既有Starlette弃用警告。
- 测试涵盖15分钟缓冲、重启去重、并行worker、周期性目标、反馈区分、取消、改期、删除来源、过期订阅、SSRF、账号隔离、提醒偏好、迁移dry-run与幂等、真实加密和VAPID签名。
- 加密测试运行真实pywebpush库，但拦截网络发送；没有联系真实浏览器推送服务。
- Service Worker隔离测试通过：授权、来源和订阅隔离、过期丢弃、隐私文案、点击同源地址、撤销、无业务数据离线缓存。
- `npm run build`含类型检查通过：主页面约69.4kB、首次加载176kB（原约65.8kB／172kB）。
- 提醒界面8组桌面／手机回归通过，涵盖暖色／黑金、正常开启关闭、退出登录清理、iOS安装引导、拒绝权限、服务未配置、保存失败回退重试、档案拒绝提醒。API和浏览器权限均为合成数据。
- 已目视检查桌面1440px、手机360px：文字层级、对比度、按钮对齐、换行、弹窗边界、滚动与关闭入口正常。证据位于`.test-tmp/pa-push/`，不打包上线。
- 最终构建上的原功能回归：每日记录9/9、目标时间线6/6通过，未破坏之前的导航移动、评分必填和目标历史入口。
- 本地HTTP检查：Service Worker、manifest、PNG安装图标均为200，Service Worker包含no-cache/no-store响应头。本地前后端预览服务已刷新，推送仍明确关闭，没有启动发送worker。

### 本次生产发布核验

- 发布前再次运行上述135项后端测试，全部通过。生产Linux环境重新安装依赖并构建Next.js成功；Service Worker隔离测试再次通过。
- 发布包约478KB，白名单只包含生产源码、依赖声明及静态资源；复核没有`.env`、私钥、SQLite、Excel、测试目录或`KnowledgeBase/`，没有知识库导入。
- 生产目标明确为`ba_coach_260908`，V2业务表只读检查通过，`STARTUP_DB_MAINTENANCE=false`保持不变。
- 显式迁移先dry-run再apply，仅创建`pa_push_devices`、`pa_push_deliveries`两张空表和索引。没有修改其他业务表，也没有导入本地账号、聊天、目标或每日记录。
- VAPID密钥在服务器生成，持久保存于`/opt/bacoach/secrets/`，目录700、私钥600、服务账号`bacoach`所有；未输出或下载私钥。
- 独立配置`/etc/bacoach/pa-push.env`为600、root所有。后端通过`40-pa-push.conf`加载；worker加载相同的生产环境文件与安全配置，未改动已有数据库、模型密钥配置文件。
- `bacoach-backend`、`bacoach-frontend`、`bacoach-pa-push`均为active；worker已开机自启，观察期间重启次数0、多轮输出均为0，无真实推送发送。
- worker `--check`以实际服务账号运行，密钥匹配、真实加密库及表结构检查通过，检查本身不发送通知。
- 针对不存在的随机账号身份做只读查询冒烟，状态／历史查询均正常返回空列表；未创建测试账号或登录会话。
- 公网首页、`/pa-push-sw.js`、`/manifest.webmanifest`、`/manifest-icon/192`均HTTP200；SW响应包含`no-cache, no-store, must-revalidate`与`Service-Worker-Allowed: /`。未登录访问状态／历史接口均401。
- 对已发布的网页资源重跑提醒界面8组测试全部通过（API、订阅和权限均在测试浏览器内模拟，不向生产写测试数据）。已复查1440px暖色、360px黑金、390px iOS引导截图，未发现遮挡、横向溢出、按钮裁切或文字难以辨认。
- 同一线上版本每日记录9/9、目标时间线6/6也通过，合计23组浏览器回归。包含导航入口、必填评分、旧记录兼容、失败重试、主题、选单键盘操作、筛选、历史详情分页和手机视口；未向生产写合成记录。
- 核对新旧生产后端源码差异，仅4个原文件的提醒接入改动（`clinical_fields.py`、`config.py`、`goal_contract.py`、`main.py`）及4个提醒新文件；没有夹带其他未确认的后端业务改动。
- 第一轮线上UI检查与服务重启并行，曾因未定位到账号菜单超时；不能仅凭此确定根因。服务稳定后独立重跑上述8组全部通过，首次失败未计为通过。
- 真实推送通道探测：Firefox入口HTTP406、苹果入口HTTP405，均建立HTTPS连接；Google FCM在约4.7秒后连接超时。没有发送带用户订阅或活动内容的探测请求。

## 部署与后续维护

本轮已完成**前后端完整发布**，不是复用旧后端的前端补丁。下面保留首次启用流程供以后维护；已存在密钥、配置与表时，不要盲目重新初始化。

1. 保持`PA_PUSH_ENABLED=false`，部署后端／前端／public静态资源及依赖（新增pywebpush）。按原安全清单打包，不上传`.env`、测试数据、SQLite或本地密钥。
2. 在服务器确定生产库名称，对迁移运行dry-run；检查两张空表计划后再`--apply`。已有MySQL结构不做删除、改列或业务数据导入；DDL可能自动提交，失败应检查后重跑，不能假装事务回滚。
3. 在服务器的持久秘密目录生成VAPID密钥，不要放发布目录；脚本不会覆盖已有密钥。仅给bacoach服务账号必要读取权限。不要随意换钥，换钥会影响已有订阅。
4. 在服务器独立配置`/etc/bacoach/pa-push.env`（不要在本地`.env`中复制生产密钥）：

   ```dotenv
   PA_PUSH_ENABLED=true
   PA_PUSH_VAPID_PUBLIC_KEY=<服务器生成的公钥>
   PA_PUSH_VAPID_PRIVATE_KEY_PATH=/opt/bacoach/secrets/pa-vapid-private.pem
   PA_PUSH_VAPID_SUBJECT=https://bacoach.xyz
   ```

5. 使用目标发布的Python环境，`PYTHONPATH=.`运行`python scripts/pa_push_worker.py --check`。只验证密钥和表，无通知发送；不要把`--once`误当dry-run，它会发送符合条件的真实提醒。
6. 首次安装独立systemd服务并重启后端使配置生效。本轮使用`infra/deploy/activate-pa-push.sh`完成：只接收已切换的明确版本，先校验再启用，失败会关闭提醒并保留密钥和表。`server-deploy.sh`已补齐正在运行的worker随代码更新／失败回退一起重启；它不会擅自开启尚未配置的worker。确认反向代理能提供`/pa-push-sw.js`、`/manifest.webmanifest`和图标，SW使用不缓存响应头。
7. 用明确同意的测试账号和真实设备验收，再扩大开放。关闭网页／后台、iPhone主屏幕、权限撤销、改期、先反馈再到期、退出、离线后恢复均需测试。不能仅凭HTTP201认定送达。

回退：关闭功能开关并停止worker，保留新增表，不删用户目标／记录或混入旧库。已经进入浏览器厂商队列的通知无法保证撤回，TTL和客户端过期检查只能限制影响。

当前可直接在服务器执行以下命令关闭提醒功能（不删除数据）：

```bash
/opt/bacoach/current/backend/.venv/bin/python /opt/bacoach/acceptance/pa-push-20260917/configure_pa_push.py --disable
systemctl disable --now bacoach-pa-push.service
systemctl restart bacoach-backend.service
```

代码发布前版本为`20260917T085435Z`，其backend仍引用`20260917T082904Z/backend`；两者都保留，不要单独删除后者。若回退到没有推送代码的旧版本，必须先停用worker，避免旧路径下找不到运行脚本。

## 监控与边界

worker每轮输出`accepted`、`failed`、`suppressed`、`duplicate`计数；异常只输出异常类型，避免日志泄漏endpoint或内容。`/api/push/history`供本人查看最近30条发送元数据，不能读取别人的状态。

- `accepted`：推送厂商返回2xx，**不是手机送达率或阅读率**。
- `failed`：明确非2xx；404/410清理订阅。
- `unknown`：请求结果不明或发送中崩溃，不自动重试。
- `feedback`：已有对应反馈而抑制。
- 没有订阅、计划时间不明确、偏好不允许、过期等不会创建发送任务，暂未实现各类跳过原因的管理员统计面板。

首版仍有明确限制：

1. 未完成真实订阅发送／真机离线验收；已确认服务器到FCM连接超时。国内网络及浏览器后台限制可能导致连接失败或延迟。微信内置浏览器不在承诺支持范围内，iOS通常需16.4+且添加到主屏幕。
2. 时间识别重准确、轻覆盖；跨消息拼接、未被保留的原话、复杂周期、备选时间、跨午夜的复杂反馈可能无法识别。宁愿漏提醒，不让模型猜一个时刻发送。
3. 反馈活动名称目前保守匹配，不是语义理解。复杂别称可能漏匹配；新消息有两分钟缓冲，但不是语义正确性的保证。
4. 同一活动的已接受通知可能正处于厂商队列中，后续反馈／取消无法保证撤回；检查与网络发送之间也有极短竞争窗口。
5. 每个设备最多一次发送尝试，网络不明不重试；若产品以后需要可靠重试，需要另定“防重复”和“送达率”的权衡。
6. 后台规则不调用额外LLM。推送不增加聊天模型调用次数；只对原有抽取提示增加保留时间原话的要求。

因此当前交付状态是“现有服务器上的代码、数据库新增空表、配置、密钥及后台服务已上线；需继续真机授权与送达验证”，不是宣称所有混合设备都已稳定收到通知。国内Chrome/FCM网络问题仍然存在；本轮没有擅自购买新服务器、外部代理或新的第三方推送服务。
