# 知识库缓存改进与本地验收（2026-09-18）

## 结论先行

本轮修复了“知识库没有变化，但索引定时刷新仍清空刚产生的缓存”的问题，延长了结果缓存的默认保留时间，补充未命中原因与节省调用计数，并增加公共目录准备复用。

**只在本地修改和验收，未部署线上，未连接或写入生产数据库。**

重复查询的模拟测试已从不命中变为命中。但正常多轮聊天的内容和上下文不断变化，完整结果命中率依然可能很低。这不是把 0% 强行改成漂亮数字的改动。

## 一、做了什么

### 1. 索引检查不再无条件清空结果

技术实现：每次检索前后仍检查知识库版本（revision）。每隔 5 分钟，额外读取完整知识片段并计算内容指纹（SHA-256 fingerprint）；若版本和实际内容都没有变化，保留同一个索引对象、公共目录和结果缓存。内容、分类、来源、片段身份发生变化时才失效。

浅白说明：检查书架上的书有没有换，不等于每次检查都把书签扔掉。

例子：10:00 建好索引，10:04:50 缓存一条结果；旧方式可能在 10:05:01 就把它清掉，新方式会保留到这条结果自己的有效期结束。

管理员通过应用导入知识的更新会在后续检索的版本检查中生效；直接在 DMS 修改片段却不更新父文档元数据的情况，仍由最多约 5 分钟的完整内容检查发现，不能宣称此类直接 SQL 修改会立即生效。

### 2. 正常结果默认保留 15 分钟

正结果默认 TTL 从 300 秒改为 900 秒。正常空结果仍为 45 秒；超时、格式错误等异常结果仍不缓存。容量继续限制为 256 条、8 MiB 结果载荷，使用 LRU 淘汰。

这只是默认值。环境中若已经显式配置 `KNOWLEDGE_RESULT_CACHE_TTL_SECONDS=300`，配置优先，不会被源码默认值覆盖。本轮没有修改生产配置。

延长 TTL 的代价是条目可能占用内存更久；数量和载荷上限不变。知识更新检查不因延长 TTL 而放宽。

### 3. 未命中原因可查看

保留原有简洁的命中率一行，管理员展开后可以看到：

- 首次或未追踪的查询：当前完整查询没有近期诊断记录，可能是新上下文、新聊天室、重启或指纹淘汰，不能武断断言为“首次提问”。
- 相同查询已过期、容量淘汰、知识更新或主动清空。
- 知识版本或检索配置变化。
- 上次请求被取消、失败、结果不可缓存或过大。
- 有效命中实际省下的检索模型调用次数，并发合并另算。
- 无变化检查保留次数、清空次数、绕过缓存、安全拦截和异常结果未缓存次数。

诊断只存带进程随机盐的 HMAC 指纹，不记录提问、用户身份或聊天室明文。指纹有时间与数量上限，失去追踪后如实显示“未追踪”，不猜测原因。

详情区可独立滚动，默认仍保持一行，不长期占用聊天空间。

### 4. 公共目录准备缓存

本轮新增的是**按模块复用公共目录结构和允许的章节 ID 集合**，只含知识库公共材料；最多四个模块条目，随知识版本失效。不同用户和问题可以复用同一模块的公共结构。

当前问题仍重新进入选章模型，候选排序和相关性判断仍根据完整输入重新运行。没有缓存或共享用户的个性化选章结论，没有直接放行旧片段。

它减少的是目录遍历和准备工作，**不减少检索 LLM 调用**，因此以“公共目录准备复用”单独展示，不并入上方的结果命中率。它也不等于完整的语义候选缓存；本轮没有新增跨用户的模型判断缓存。

## 二、本地新旧对比

方法：内存 SQLite、合成知识、确定性模拟模型、可控时钟；重放旧版的 5 分钟 TTL 与刷新清空策略，对比新策略。没有真实用户对话、真实模型请求或生产读写。

| 场景 | 查询次数 | 旧结果命中率 | 新结果命中率 | 检索模型调用：旧 → 新 |
| --- | ---: | ---: | ---: | ---: |
| 4 分 50 秒查询，5 分 10 秒重复，跨过索引检查边界 | 2 | 0% | 50% | 4 → 2 |
| 同一完整问题，隔 8 分钟重复 | 2 | 0% | 50% | 4 → 2 |
| 连续改变提问、历史上下文或聊天室 | 4 | 0% | 0% | 8 → 8 |
| 知识更新后再次问相同问题 | 2 | 0% | 0% | 4 → 4 |

四组中的新旧返回片段及顺序均一致；知识更新场景返回的是更新后的内容，没有旧片段。连续改变上下文组的公共目录准备复用为 3/4（75%），但结果命中率仍如实为 0%，模型调用数也没有减少。

**上面的 50% 只代表特定的“两次请求、第二次可复用”测试，不是预计线上命中率；模拟模型调用数也不能换算成已验证的真实延迟、token 节省或 Holdout 准确率。**

复现入口：`backend/scripts/compare_cache_0918.py`。原始本地输出：`.test-tmp/cache-comparison-0918.json`。

## 三、安全和回归验收

- 完整后端测试：935 项收集，934 通过、1 项跳过，无失败；最后的诊断补充又跑过缓存相关 64 项。
- 前端类型检查与生产构建通过。
- 管理员缓存指标：1440 / 390 / 360px，正常、暂无数据、真实零命中、关闭、错误重试、浅色和暗色均通过，无横向溢出或页面 JavaScript 错误。
- 聊天室生成隔离：1440 / 390px 再次回归通过。
- 验证相同查询命中；上下文、聊天室、用户、模块及配置变化不误复用。
- 验证跨索引检查边界仍能正常返回，DMS 直接内容修改可被完整检查发现。
- 验证管理员更新、模块重分类、删除、读取失败和检索期间知识变更不能继续放行旧片段。
- 验证命中后仍运行当前中介节点；中介超时仍扣留知识，绝不直接放行原始片段。

浏览器测试的登录和 API 是本地合成夹具；`measured-*` 截图展示来自上述缓存机制测试的实际计数，而不是线上统计。普通 `details-*` 截图中的 25% 等数值只用于 UI 状态覆盖。

## 四、仍然存在的限制

1. 精确结果继续按用户、聊天室、模块、完整检索输入隔离；不同说法和不断变化的历史通常不会命中。
2. 缓存与统计仍在单进程内存，服务重启归零，不跨进程共享。Redis 尚未加入。
3. 本轮公共缓存只优化目录准备，不能解决新问题的两次检索模型调用开销。
4. 没有改变查询文本或丢弃否定、身体状况、目标等上下文来追求高命中率。

下一步应先观察真实流量的未命中构成、实际节省调用数及延迟，再决定是否增加有语义安全约束的候选检索缓存。仅换 Redis 或无条件提高命中率，都不能保证答案正确性与实际收益。

## 五、主要实现位置

- `backend/app/retrieval.py`：版本检查、全量指纹审计、保留与失效、监控汇总。
- `backend/app/retrieval_cache.py`：精确结果缓存、有限期诊断指纹、未命中原因、有效节省计数。
- `backend/app/semantic_catalog.py`、`catalog_knowledge_base.py`：公共目录准备复用及独立统计。
- `frontend/components/KnowledgeCacheMetric.tsx`：折叠的管理员诊断详情。
- `backend/tests/test_cache_improvement_0918.py`：新增安全与诊断回归。
- `backend/scripts/compare_cache_0918.py`：离线旧策略与新策略对比。

生产网站仍保持此前部署版本，本轮需要另行确认后才会发布。
