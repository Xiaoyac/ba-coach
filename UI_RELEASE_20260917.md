# UI 与目标档案发布记录 · 2026-09-17

## 范围

用户确认本地效果后授权部署：轻量聊天布局、目标全历史档案、单页每日记录及对应交互修复。
追加范围：注册改为必填出生日期；历史年龄为 10 且未填写生日的账号，登录或恢复会话时先完成生日补填。
本次不导入本地数据库、测试账号、演示数据或知识库，不覆盖线上环境与密钥。
为保存完整生日，仅执行一项明确的增量结构变更：`user_profile.birth_date DATE NULL`。不回填、不批量更新现有用户记录。

发布前已连接生产核实，当前版本为 `20260916T143404Z`，三项服务均正常。保留该版本作为回滚目标。

## 相对生产的差异

逐文件 SHA-256 对比确认：

- 后端：新增 `backend/app/goal_history.py`，`backend/app/routes/program.py` 增加只读历史接口。
- 前端：页面壳、全局样式、目标档案、主题入口、每日记录、聊天工作区、侧栏、进度栏与目标接口适配，以及依赖锁文件。
- 其他此前已有的本地后端改动与生产一致；没有借这次发布引入额外业务流程改造。
- 追加生日相关的认证、档案、日期校验与前端弹窗；新增 nullable DATE 字段。生产沿用 V2，启动过程强制关闭自动维护并只读检查结构。
- 修复验收中发现的 V2 档案模块查询：使用 `conversation_runtime_states.current_module`，不再读取旧版 `module` 列。

## 发布方式与回滚

使用项目发布脚本的 `-SkipDatabaseTasks` 模式。包内目录白名单排除环境文件、数据库、测试目录、演示生成脚本和知识库。
发布前独立完成类型检查及完整后端回归，再以 `-SkipChecks` 避免重复测试；服务端仍重新安装依赖和正式构建。

正式切换前保留旧站运行；服务重启可能短暂中断请求。部署脚本在切换后健康检查失败时恢复旧版本。
人工回滚时把 `/opt/bacoach/current` 指回 `/opt/bacoach/releases/20260916T143404Z` 并重启前后端；保留新增的 nullable DATE 列，不删除用户已提交的生日。

为纳入追加需求，发布脚本在新版本构建之前暂停，线上旧版本未停服。新代码验收后覆盖到待发布目录；迁移独立执行 dry-run、明确目标库校验、MySQL INSTANT 加列，之后再继续原发布进程。常规迁移和知识库导入仍然跳过。

## 生日规则与实现

- `RegisterRequest.birth_date` 必填，前后端没有默认日期/默认年龄；拒绝空值、格式错误、未来日期。
- 保留原有 10–120 岁支持范围，不在这次发布擅自调整用户年龄政策。
- 年龄按北京时间当天和完整出生日期动态计算；`reported_age` 保留提交时快照，`birth_year` 同步生日年份。
- 登录和 `/api/auth/me` 返回 `birth_date_required`：仅匹配未填生日且历史年龄等于 10。其他已有用户不强制补填。真正 10 岁的用户填写正确生日后也不会重复弹窗。
- `AuthGate` 在挂载聊天工作区前展示必填弹窗：不提供跳过，Esc/背景点击不能关闭，可退出登录；再次登录仍会提示。
- `PUT /api/auth/birth-date` 只允许更新当前已登录用户。保存成功才解除提示；失败保留输入，提交中防重复点击。
- 在「我的档案」可更正生日，已知生日时不能再单独覆盖年龄、清空生日。完整出生日期不进入 LLM 档案提示词，只提供计算后的年龄。
- 这是网页进入流程约束，不是新增的全 API 授权封锁；不会强制注销正在聊天的用户，旧页面需刷新或再次登录后使用新流程。

## 追加生日验收

- 完整后端回归：588 passed、1 skipped（75.30 秒）；1 条既有 anyio 弃用警告。
- 测试覆盖：缺失/空/非法生日不创建账号，10/37/120 边界，生日当天与闰日计算，历史年龄 10/28/未知分流，持久化、认证保护、V2 注册/补填、迁移目标守卫与幂等性。
- 本地真实浏览器：有效生日注册成功且档案一致；空值和未来日期拒绝；年龄 10 旧账号退出/重登仍需补填；503 失败保留输入；保存忙态输入/退出/重复提交禁用；成功后刷新与重新登录不再提示。
- 浏览器发现并修复 Esc 关闭原生 dialog 后白屏的问题：使用 `closedby=none` 并在原生 keydown/cancel 阻止关闭，重复 Esc 和背景点击验收通过。
- 视觉验收：1440×900 桌面、360×740 手机；浅色/暗色主题、日期字段、按钮层级、文字对比、错误换行、边距与弹窗边界均检查，未见横向溢出或主要控件遮挡。生日流程无页面运行时异常。
- 本地截图位于 `.test-tmp/ui-qa-0917/birthday-required-desktop.png`、`birthday-mobile-light-error.png`、`birthday-mobile-dark-invalid.png`，不随发布包上传。
- 线上仅做无账号写入的冒烟检查，不能把本地模拟账号验收称为生产真实账号验收。

## DMS 库名核实

线上连接的是 `ba_coach_260908`（V2）。已只读检查 `user_profile`、`pa_goals`、`module_two_record`、`pa_cycles`、`pa_activity_events` 可读。
截图里的 `ba_coach_v2_restore_20260910t155432z` 不是当前实际存在的业务库，DMS 列表是残留登记/元数据；应选 `ba_coach_260908` 并刷新元数据。`legacy_20260910t155426z_...` 属于备份表，不是当前业务表。本次未删除 DMS 条目、旧库或备份表。

## 本次验收清单

- 发布前：完整后端回归、前端类型检查、相对生产差异及测试资料排除检查。
- 发布中：正式构建、跳过数据库任务、版本指向、服务状态及健康检查。
- 发布后：公网 HTTPS、静态资源、部署文件校验和、未登录接口保护；不创建生产测试账号、不写每日记录/聊天。
- 浏览器：桌面/手机正式登录页、主题切换与返回、显示边界、控制台错误。
- 异常检查：未认证请求历史接口和畸形/不存在目标路径，不泄露真实数据。
- 登录后的完整功能与视觉结果参见本地 `UI_GOAL_ARCHIVE_LOCAL_20260917.md` 和 `DAILY_RECORD_SINGLE_PAGE_20260917.md`；不能把未登录线上检查说成生产账号端到端验收。

## 执行结果

发布版本：`20260916T175118Z`（UTC 编号，北京时间为 2026-09-17）。

- UI 初次回归：567 passed，1 skipped；纳入生日后的最终后端回归：588 passed，1 skipped。
- 前端 `npm run typecheck`、本地 `npm run build`：通过。
- 上传前复核归档 168 个条目，不含数据库、环境文件、知识库、本地 QA/预览目录。
- 正式构建与切换：已完成，线上 current 指向 `20260916T175118Z`。前端、后端、nginx 均 active，健康检查为 ok；DeepSeek/豆包配置可用状态保持不变。
- 暂停/继续发布期间原 SSH 传输连接未正常返回。确认远端发布进程已退出、发布包已按成功流程清理、新版本和公网验证通过后，仅关闭残留的本地 SSH 进程。因此外层 PowerShell 显示了连接终止错误，不能将它记作命令正常退出；发布是否成功以以下独立检查为准。未重复执行切换或停止线上服务。
- 161 个实际发布源码文件 SHA-256 与本地一致（包括追加的 16 文件生日覆盖包），无不一致。
- `ba_coach_260908.user_profile.birth_date DATE NULL` 已用 INSTANT 方式增加；重跑检查为 EXISTS/no changes。全部 V2 预期字段只读查询通过；服务日志明确显示 startup database maintenance disabled。
- 线上 OpenAPI 确认注册 birth_date 必填、AccountInfo 包含 birth_date_required、补填 PUT 接口存在。
- 只读汇总检查：检查时有 3 个已注册账号符合历史年龄 10 且未填写生日的条件，不输出账号身份。数量会随用户提交生日变化。
- 公网首页 200；未登录 PUT 生日和 GET 目标历史均返回 401，没有创建任何生产测试账号或模拟业务记录。
- 公网桌面/手机注册页确认空白生日字段为 required；暖色/暗色往返、日期字段、手机换行及边界检查通过，无页面运行时异常。登录后的生日补填全流程以本地隔离数据验收，未代真实用户填写生日。
- 临时无头 QA 浏览器已关闭；保留用户可见的本地管理员预览。为验证实际构建，`localhost:3000` 当前运行本地正式构建（不是热更新开发模式）；之后继续修改源码时需重建或重新启动开发服务。
