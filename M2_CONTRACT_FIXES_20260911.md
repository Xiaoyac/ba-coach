# M2 Contract 修复报告

日期：2026-09-11。状态：本地实现及隔离验证；未部署生产、未改数据库结构。

## 本次修复

1. 中介失败不放行知识：超时、服务异常、输出格式错误、伪造片段 ID 均返回空知识；关闭中介也不成为绕过审核的通道。模块继续依据用户输入、业务上下文和原有安全规则对话，不使用本轮未经审核的召回材料。图输出中的知识列表同样只包含通过审核的片段。
2. M2 草稿改变后撤销旧确认资格：变更会撤销活动选择/目标卡完成步骤，递增运行版本并记审计日志。Router 提议留在本模块时清除旧待确认标记。空步骤数组不被解释成撤销全部步骤。
3. 增加明确的步骤撤销协议：Router 输出 revoked_steps 与 revocation_evidence；只接受合法步骤键，且证据必须是当前用户输入中的原文。证据被撤销后重新讨论，不靠模型自己修改数据库。原话匹配只能验证来源，不保证语义判断一定正确。
4. 计划确认增加后端检查：按现有 M2 注册表检查活动、时间、地点、正数时长、频率结构、障碍及逐项应对。仅有非空活动/时间不能确认。频率采用 schema_version=1、text；没谈到障碍不能把空数组当作“没有障碍”。明确没有障碍时需保存用户明确表达及无需额外应对的说明。
5. 界面显示执行频率和待补字段，未知空数组显示“尚未明确”，不再显示成“无”。不完整计划隐藏确认入口，后端仍独立拒绝绕过网页的请求。
6. 中介 Context 带 schema_version、模块、步骤注册表版本、待完成步骤、目标/周期 ID；V2 事实包含表名、记录 ID、版本、来源、确认、状态及有效期。读取当前目标的计划、已确认 M1、活动限制和有效记忆。旧文字上下文明确归为 unverified_context，不再称为 confirmed_context。

## 例子

用户原先准备确认散步计划，后来明确说“先别执行，我改主意了”。Router 可以引用这段原话撤销 pa_card_completed；旧确认资格失效。若后续抽取改变活动或安排，也会撤销相应步骤并要求重新审核。

若中介等待超时，即使召回了一段“散步有助于……”的材料，也不会把该片段注入 Module Prompt。模块仍可依据用户自述询问情况，而不是使用未经中介审核的材料继续给建议。

## 工程边界与代价

- 不增加新的 LLM 节点。中介成功仍只允许引用输入中存在的片段。
- 失败时可能减少知识引用，但不会用原始召回来换取表面可用性。
- 草稿变化较频繁时，用户可能需要多一轮讨论后才能确认；这是防止旧确认依据继续生效的代价。
- 字段完整不等于临床上适合，也不等于用户真的表达过。模型证据判断和用户确认仍不可省略。
- 当前最小计划要求沿用现有注册表；不同活动不适用时长/地点等字段的特殊业务模型仍需单独设计，不能填虚假值绕过检查。
- Context 有预算：超过 20 条有效活动限制或总体上下文超过 40000 字符时，不静默裁掉安全信息后继续使用知识，而是停止本轮知识使用。
- 本次未完成真实 LLM 评测、跨设备压力测试或线上发布，不能宣称真实召回精度或延迟改善。

## 验证

- 后端全量回归：465 通过、1 跳过；剩余 1 条第三方依赖弃用警告，非断言失败。

- Contract 隔离探针 14/14 通过，包含不完整计划拦截、完整计划确认、草稿变更、过期/重复确认、目标隔离、事实来源、空知识和超时处理。
- V2 API 冒烟 7 场景通过，含 M1—M4 正常流转。
- 四模块各有超时测试，检查未审核片段不会进入实际组装的 Module Prompt。
- 新增计划缺字段、障碍与应对匹配、Router 原话证据及伪造证据测试。
- 前端 TypeScript 检查通过。

旧测试里依赖中介无效输出仍放行知识的成功路径已改为明确模拟中介审核成功；失败场景单独保留严格拦截断言，不通过降低产品校验让测试变绿。

## 可监控指标

中介日志新增 raw_chunk_count、approved_chunk_count、withheld_on_error，并保留 status、reason 和 duration_ms。

- 严格不变量：status=fallback 时 approved_chunk_count 必须为 0。
- 超时率：reason=timeout / 实际中介调用数。
- 错误阻断率：withheld_on_error=true / 实际中介调用数。
- 无适用知识率：reason=no_applicable_evidence / 成功中介调用数。
- 计划证据失效：ai_decision_logs 中 plan_evidence_invalidated；记录字段名和被撤销步骤，不记录原始草稿正文。

以上是可采集的日志字段与计算定义，不是已上线的监控面板，也不是本轮测出的线上指标。
