# Copy to .env.local
# Base URL of the FastAPI backend. NEXT_PUBLIC_ vars are exposed to the browser,
# so never put API keys here — keys stay server-side in backend/.env.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
