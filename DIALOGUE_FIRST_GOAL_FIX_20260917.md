# 目标回归对话内确认：修复说明

## 结论与范围

截图中的两个回复都有问题。用户没有要求增加“本次记录与确认”入口，也没有要求在目标面板确认后才进入下一模块。此前实现额外加入网页提交门槛，随后把它写进系统提示及兜底回复，偏离了“在对话中制定，在面板里回顾”的需求。用户提出质疑后，回复又将错误流程解释成用户误会，这是错误的解释，不是合理的业务要求。

本次代码已部署到生产版本 `20260917T134619Z`；没有执行数据库迁移、导入测试数据、修改生产用户记录或发布管理员提示词版本。保留此前用户明确要求的新建聊天目标选择入口：它选择继续哪段目标上下文，不承担新目标制定或计划确认。目标总览维持只读。

## 做了哪些调整

1. 删除账号菜单中的“本次记录与确认”，移除不再使用的 `ProgramPanel` 组件。
2. 修正 M1、Router、M4、流程契约及每轮注入的状态说明：不再要求去网页确认、保存或提交。管理员旧提示词或历史回复有冲突时，以新的运行时规则为准。
3. 增加共享 Validator 规则，拦截“去目标面板确认”“网页确认成功后才能继续”等引导；条件句不再绕过该规则。只读面板说明、明确纠正错误的语句仍允许。
4. 提取共享确认事务，将原来的模块推进和目标周期更新用于真实聊天确认；新路径引用实际用户消息 ID，不伪造网页确认发言。
5. 把 M3 抽取前置到当前轮的后台核验，增加最新轮证据检查，保证移除网页按钮后不会卡住。
6. 修正流程完成后的内存同步和周期证据边界，避免下一周期沿用上一周期收尾回复。

### 浅白地说

以前是“聊天里说愿意 → 还要找面板再点一次”。现在是“聊天里把事情说清楚 → 在聊天里明确同意 → 后台检查并记录”。面板只负责回顾，不负责替用户做决定。

## 各模块如何处理

| 阶段 | 可以推进的依据 | 不代表什么 |
| --- | --- | --- |
| M1 | 当前有效的 M1 契约及用户明确愿意开始目标讨论 | 不代表选好了活动，也不创建目标 |
| M2 | 已讨论的完整计划、紧邻的计划摘要、用户明确同意、草稿内容未改变、当轮抽取成功 | 不是对任意“好的”自动保存目标 |
| M3 | 明确的记录约定、对应摘要及用户同意、当轮抽取成功 | 不是未讨论就直接进入执行 |
| M4 | 当前周期完整证据、用户实际确认的下一步选择 | 不把随口提到另一项活动当作更换或结束目标 |

例：用户说“我理解了，愿意开始讨论目标”，只让 M1 进入 M2，目标数不增加。之后用户与教练讨论散步、时间、地点、时长、频率及障碍应对，教练复述完整安排，用户说“就按这个计划试试”，才确认计划。用户若说“好的，但是改成20分钟”，必须更新安排并再次在聊天里核对，不能拿旧同意保存新计划。

## 工程保护与取舍

- 保留账号归属、聊天归属、执行周期、行版本、记录哈希、最新助手消息及抽取来源检查。
- 使用数据库事务和现有对话锁；重复或迟到的 Router 不能重复确认或覆盖新状态。
- M2/M3 对话摘要与未变更草稿绑定。确定性匹配较保守：复杂同意表达、摘要改写或字段变化可能要求再核对一次；不确定时不推进，也不会导向网页按钮。后续可通过脱敏失败分类统计扩大安全匹配范围，不能以放宽用户同意为代价。
- 未增加用于判断同意的新 LLM；M3 使用既有字段抽取机制，但现在放在每轮后台核验，完整流程可能增加 M3 阶段的抽取调用数。下一条消息仍可能等待后台路由完成。
- 原 `POST /api/program/{session_id}/confirm` 标记为 deprecated，仅兼容尚未更新的旧客户端；新界面无入口、新对话流程不调用它。此接口暂未删除，不应将“移除菜单”误报为“所有后台兼容接口已删除”。
- 语言规则与引用核验不能证明全部语义正确；尚未进行真实供应商多轮自由对话评测，不承诺所有表达均一次识别。

## 验证结果

- 相关后端回归：**265 passed**，1 个既有依赖弃用警告。涵盖目标归属、M1/M2/M3/M4、周期、删除隔离、Validator、chunk 分数和检索缓存。
- 新增 26 项对话确认测试：包括截图复现、明确/模糊/否定同意、计划改变、旧抽取、跨用户、迟到路由、重复调用、M2→M3→执行和 M4 继续/调整。
- 前端 `npm run build` 通过。
- 浏览器合成接口验收：1440、390、360 像素宽度通过；账户菜单没有多余确认入口；调试功能、分数说明、加载失败重试、空结果、中介超时和流式消息保存正常；无页面运行错误和横向溢出。
- 验收使用隔离数据库和模拟浏览器接口，不是生产数据库验收，也不是线上已发布证明。

## 同时保留的 chunk 分数修复

LLM 目录筛选没有数值相似度，过去填入 `0.0` 使界面误显示全部低分。现在模型筛选返回 `score=null`、`score_type=model_selection`，界面显示“模型筛选 · 未计算数值分数”。词法、BM25F、RRF、重排分数标明各自类型，不能直接横向比较；真实零分和负分保留。旧数据中来源未知的零分不再伪装成有效精度指标。

分数接口和引用片段接口仍被实际使用，因此保留；删除的是虚假零分填充值，而不是仍在使用的调试能力。前端沿用原有紧凑排版，只调整分数说明，没有新增面板。

## 生产部署验收补记

- 网站：https://bacoach.xyz/ ，HTTPS 返回 200。
- 当前版本：`/opt/bacoach/releases/20260917T134619Z`；保留的上一版：`20260917T122211Z`。
- 使用代码发布参数 `-SkipDatabaseTasks`，发布日志确认跳过数据库迁移和知识库导入；启动维护开关仍为 `STARTUP_DB_MAINTENANCE=false`。
- 后端、前端、PA 推送服务均为 `active/running`，`NRestarts=0`。
- 服务器 Python 3.10.12 下执行 `infra/deploy/verify_dialogue_first.py`：隔离内存 SQLite 验证 M1 不创建目标、M2→M3、M3→M4、真实用户确认消息引用、错误面板引导拦截以及目录筛选分数为 null，全部通过。
- 服务器缓存验收通过：只读加载生产 1331 个 chunk；合成缓存冷查询 2 次模型调用、命中 0 次；知识库更新后两个独立实例刷新正确。生产数据写入次数为 0。
- 未登录访问缓存管理指标接口仍返回 401。
- 线上实际前端资源的浏览器验收覆盖 1440/390/360 像素宽度，账户菜单无“本次记录与确认”，chunk 分数类型及调试交互正常，没有页面异常和横向溢出。浏览器使用拦截的合成 API 响应，不登录真实账号、不调用真实模型、不写生产对话；这不等于真实供应商多轮对话评测。
- 历史回复不会被改写，旧回复的调试内容也不会被追溯删除。刷新页面后加载新版，后续新回复使用新的运行时规则。
