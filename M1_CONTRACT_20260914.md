# Module 1 契约与上线说明（2026-09-14）

依据：`M1字段标准表0914.xlsx`、`0913全局：人设与回复逻辑.md`、`0913模块一：理解行为激活.md`。

## 本次调整摘要

1. 将 M1 统一为三里程碑：具体经历／低披露选择 → 关系理解与缓解方法 → BA 理解及目标设定意愿。
2. 不再要求每个人都存在“抑郁循环”。只有用户提供相应重复证据，才使用回避、反刍、负性循环等机制标签。
3. 保留事件快照 `abc_event` 与分列事实。快照负责同一事件的完整性，分列用于检索与结构化分析，不删除任一类字段。
4. 严格区分未知方法 `null`、明确没有 `[]`、已有方法字符串数组。
5. 将认可当前总结、理解 BA、愿意目标设定分开验证；旧认可不能确认更新后的总结。
6. 增加低披露路线：拒绝分享／个性化分析后停止追问，允许一般 BA 教育，但不能跳过理解和意愿。
7. 全局、M1 默认提示词与不可覆盖的运行规则同步调整；网页展示里程碑、缺失项和方法状态。
8. 修复 M1→M2 衔接误导：没有目标不影响 M2 聊天，先 PA 教育，再探索、选择活动，由 Agent 写入目标。不存在草稿时不再要求去面板核对草稿，也不要求刚完成 M1 的用户另开对话。
9. 不迁移表结构、不回填历史记录、不导入本地测试数据库。仅通过现有 JSON 保存契约元信息，指定提示词配置单独发布并备份。

## 一、字段契约

下表的“必需”指个性化路线推进条件；低披露路线的个人事实允许为空。

| 逻辑字段 | 当前 V2 存储 | 规则 |
| --- | --- | --- |
| chief_complaint | 同名 TEXT | 用户最新明确的困扰／改善方向；不加诊断与推测动机 |
| distress_duration | 同名列 | 可选，仅明确时长；应用侧最多 64 字符，不更改线上列 |
| distress_frequency | 同名列 | 可选，仅明确频率，不从一次事件推出习惯 |
| trigger_situation | 同名 TEXT | 同一个真实事件的情境；不得拼接不同事件 |
| abc_event | event_experience JSON | trigger、feeling、behavior、consequence 为核心；thought／身体感受可选，不固定 B=信念 |
| coping_behavior | 同名 TEXT | 实际行动或不行动，不是未来计划；刷手机／躺下不自动等于回避 |
| coping_consequence | 同名 TEXT | 实际反馈，包括“没有明显变化”；不得写假设结果或未经支持的因果 |
| behavior_state_relation_summary | functional_chain_summary TEXT | 逻辑名称改为行为—状态关系；抽取接口暂保留 ai_depression_cycle_summary 以兼容既有代码 |
| user_approval_level | event_experience._m1_contract.user_approval_level | 0=不认可，1=部分，2=认可；只有当前总结及其后续认可证据匹配才作为阶段完成依据 |
| attempted_relief_methods | 同名 JSON | null=未知／不愿谈；[]=明确没有；字符串数组保留尝试过／正在做／想到过及实际结果 |
| exception_positive_scene | 同名 JSON | 可选，真实例外及结果；不是门槛，不编造成功经历 |

V2 没有独立 `user_approval_level` 列，本次不新增列。新增的证据元信息放在现有 `event_experience._m1_contract`，不把它当个人事件事实。低披露时该 JSON 可以只包含版本与契约，不填虚构经历。网页响应会去除内部原话证据，只提供独立的阶段状态。

## 二、三个里程碑

### m1_milestone_1：有足够的真实事例，或确认低披露选择

个性化：主诉与同一事件的情境、主要体验、实际行动／未行动、实际结果齐全。用户一次说出多个事实就复用，只追问缺失要素，不要求穷尽历史、思想和身体感觉。阶段 1 不额外进行总结确认，阶段 2 再统一核对。

低披露：必须有用户明确不愿分享／不愿个人分析的原话。信息少、沉默、客套不能被当成拒绝。难以回忆时最多两次低压力邀请，明确拒绝则立即停止。

### m1_milestone_2：当前事实关系基本认可，且方法状态已知

个性化：总结与教练实际说过的文本对应；其后有用户基本认可当前关系的原话；缓解方法明确为有或无。部分认可只有在不再存在未纠正事实时才可满足。

用户否定机制但认可事实时，应去掉机制标签，不要求用户承认“抑郁循环”。新的总结不能使用发生在它之前的旧认可。低披露路线跳过个人分析，不虚构总结或方法。

### m1_milestone_3：教育、理解和意愿

五项教育内容：

- 情绪、精力与行动相互影响。
- 活动和反馈减少可能维持困扰；这是一般模型，不强加给个人。
- 可控制的行动端能够带来新的反馈。
- 行动不保证立即开心或改善。
- 尝试—观察—调整，而非意志力要求。

以上内容需要教练发言依据；教育之后需要用户基本理解的依据，且没有未解决的核心疑问；最后需要明确目标设定意愿。用户可以在一句话中同时明确理解和同意，但不能把“认可总结”自动作为这两项依据。

低披露同样执行这些要求。教育后最多一次经历关联邀请，用户拒绝后不重启询问。犹豫、拒绝或核心疑问未解决，保留当前阶段。

## 三、如何执行，而不是只靠提示词

1. M1 每轮回复后进行结构化抽取，输入来自本用户本段聊天的持久化消息，不依赖最近 40 条短期记忆。
2. 抽取同时提出字段、路线与原话证据；这份输出仍是不可信模型输入。
3. `m1_contract.py` 独立核对类型、说话人、原话存在性、教育／总结与用户回应的先后顺序、当前版本和聊天归属。五项教育不能用同一段短引用重复凑数。
4. 使用当前完整快照计算阶段，不把旧完成步骤简单累加。未知值可清除旧草稿中的失效字段；历史已确认记录不在本次批量改写。
5. 证据绑定当前 assistant 消息 ID；抽取失败或返回截断 JSON，不能沿用上一轮的“可以确认”。M1 不采用截断 JSON 修复来判断完成。
6. Router 提出判断，但 M1 的服务端进度以本轮证据契约为准；Router 明确撤销仍可使受影响步骤不完成。
7. 网页 `can_confirm` 与确认接口都检查契约；确认仍需要记录哈希、运行版本及本人操作，过期／串聊天证据返回 409。接口额外比对数据库中最后一条消息 ID，即使后台任务中途失败，也不能用用户最新更正之前的旧草稿确认。
8. 真实用户确认后才进入 M2。M2 没有目标也可正常聊天，不把创建目标作为 PA 教育前置条件。

为兼容既有存储，保留四个内部步骤键：前两个对应前两个里程碑，`ba_education_completed` 与 `goal_setting_consent` 分别对应里程碑 3 的理解门与意愿门。用户界面显示三个里程碑。

既有已完成 M1 的用户继续复用原进度，不因本次发布被强制重做。旧的未完成草稿没有新版证据时，下一轮重新核对后才能确认，不把历史缺失内容凭空补全。

## 四、例子

### 例 1：不要求负性循环

用户：“昨天下班看到没收拾的桌子，很烦。我坐了半小时，还是烦，没明显变化。还没试过别的方法。”

保存真实四要素，方法为 `[]`。总结可为“看到未整理的东西后心烦，坐下休息后这一次状态没有明显变化”。不能改写成“你通过回避获得短期缓解，长期更焦虑”。

该总结获认可仍不等于完成 M1；还需要 BA 教育、理解、核心疑问解答及目标设定意愿。

### 例 2：低披露

用户：“我不想说具体经历，也不想分析我的私人事情。”

不再收集个人事件；说明个性化有限后提供一般 BA 教育。用户理解并愿意设目标才可确认进入 M2；方法保持 `null`，不是 `[]`。

### 例 3：更正旧总结

用户：“刚才说错了，坐下后其实心烦缓解了，不是没变化。”

更新对应结果及关系总结；旧总结的认可不再用于确认。未重新核对当前关系前，即使旧 Router 曾判断完成，也不能直接进入 M2。

### 例 4：截图中的 M2 卡点

正确顺序：M1 理解与意愿 → 网页确认 M1 记录 → 在当前聊天进行 PA 教育 → 探索意向／价值 → 用户选择活动 → Agent 建目标草稿 → 完善计划并确认。

错误顺序：M1 结束 → 必须先在网页手动创建目标 → 才允许 PA 教育。本次明确禁止这一前置阻断；“新对话选择旧目标”只是另一种继续已有目标的入口。

## 五、提示词适配

全局与 M1 内容按两份 MD 调整，保持一轮一个重点、最多两个相关问题、通常 120 字以内，必要教育允许展开。群体特征不是当前用户事实，最新明确纠正优先，记忆没召回不等于用户没说过；危机处理保留。

文稿中的 `chat_reply` JSON 与 `<user_message>` 包装没有直接照搬：现有网页消费自然语言回复／流式事件，实际角色由消息对象区分。服务器和提示词继续要求面向用户的纯文本，避免 JSON 原样露出。内部结构化证据不进入聊天正文。

线上管理员保存的提示词会覆盖代码默认值，因此部署不只替换 Python 文件：使用 `publish_m1_prompts_0914.py` 精确更新 `global`、`module_1` 两个配置项，保存原文备份并用状态哈希防止覆盖检查之后发生的编辑。其他模块、中介提示词不批量覆盖；不可覆盖的路由／回复状态规则随代码更新。

## 六、测试、监控与限制

验收涵盖个性化、低披露、可选空字段、四要素缺失、方法未知与明确无、错误说话人证据、教育／认可顺序、重复引用、拒绝同意、核心疑问未解、更正摘要、失败抽取、过期哈希、串聊天、旧 M1 复用与 M2 无目标对话。

四组真实模型合成对话检查（`deepseek-v4-pro`，每组一次，不写入用户数据库）：

| 案例 | 预期／实测 | 抽取耗时 | 输入／输出 token |
| --- | --- | --- | --- |
| 个性化、理解且同意 | 放行／放行 | 9.569 秒 | 1254／511 |
| 低披露、理解且同意 | 放行／放行 | 9.019 秒 | 1181／470 |
| 不愿开始目标设定 | 拦截／拦截 | 8.754 秒 | 1258／502 |
| 更正旧总结 | 拦截／拦截 | 8.880 秒 | 1290／499 |

这些是功能冒烟检查，不是准确率评估，也不是 100% 可靠性的证明。耗时为抽取调用，不是网页首字延迟。M1 抽取从“尝试转阶段时运行”改为每轮后台运行，增加成本，快速发出下一轮时也可能等待前一轮核对；现有日志记录 `clinical_extraction` 时长和 token。

建议按周观察：

- M1 抽取 P50/P95 耗时、失败／空 JSON 比率与 token。
- `contract_path` 个性化／低披露占比；不能把低披露率高简单视为优化成功。
- `missing_fields` 各项分布，尤其理解和意愿是否长期卡住。
- 总结更正后的重复确认轮数、误放行与误拦截人工抽样。
- M1 确认接口 409 比例，以及 M2 无目标状态是否出现“先建目标才能聊”的回复。

边界：原话存在性不能数学证明语义正确，是否真正理解／同意仍涉及模型语义判断；指令注入、反讽和复杂多重纠正仍需持续评估。低压力邀请次数主要由提示词约束，本次没有把每次邀请单独做成计数器。没有重置线上既有用户阶段，也没有针对历史误跳转记录做未经授权的回退。

浏览器验收使用隔离的模拟 API 响应，覆盖实际确认按钮、勾选、阶段状态和桌面／移动端展示；真实确认接口另由隔离 SQLite 集成测试验证。线上仅做发布和只读检查，不创建正式测试账号或聊天。

## 七、最终发布与验收结果

- 正式地址：https://bacoach.xyz/
- 最终生效目录：`/opt/bacoach/releases/20260914T020238Z`。
- 发布过程跳过数据库迁移和知识库导入；最终补丁复用已验收的前端构建，在独立发布目录切换，保留旧版本用于回滚。
- `global`、`module_1` 两条线上提示词已更新，读取后的内容哈希与新版默认值一致；原配置备份：`/opt/bacoach/backups/m1-prompts-20260914-0914.json`。
- 全套后端测试：535 通过、1 跳过；之后增加“后台失败仍需核对最新消息”的保护，相关确认／目标回归 27 项通过。
- 前端类型检查及正式构建通过。
- 真实 M1 抽取：上表 4 个案例符合预期。
- 使用线上生效的全局／M2 提示词额外检查“无目标先问 PA”：模型正常解释 PA、区分日常活动与运动并询问理解，没有要求先创建目标；调用不保存对话，不写用户数据库。
- 浏览器：桌面 1440×1000、移动端 390×844；核对三个阶段、低披露说明、null 与 [] 文案、勾选前禁用确认、确认后进入 M2、继续输入、无手动新建目标框、无横向溢出。阶段和正文可读、按钮可触达，确认后面板收起。线上资源另以模拟接口检查新文案存在，公开主页 HTTP 200 且无浏览器页面异常。
- 验收截图保存在本地 `.test-tmp/m1-browser-0914/`，不进入部署包。

本次不改写历史聊天中的错误回复，也不重置用户已有目标。若浏览器仍保留旧页面，请先保存尚未发送的文字，再强制刷新（Ctrl+Shift+R）。全局与 M1 新提示词对后续回合生效，首次开场文案仅影响之后创建的聊天。

回滚注意：回滚代码目录不会自动撤销已保存的提示词配置；如需完整回滚，必须另行核对并恢复上述备份中的两个提示词，不能恢复整份数据库。
