# BA Coach 实际工作流：回复、证据、数据库与模块跳转

核查日期：2026-09-23。本文解释现有实现，没有修改功能、提示词、数据库或线上部署。依据本地代码与本次已核对的线上配置；关键提示词、图节点、模型接口、抽取、Router、回复工作流文件已核对与线上一致。网页提示词是数据库中的可变配置，不能仅凭 Git 版本还原。

## 1. 先分清六件看起来相似、实际上不同的事

| 层次 | 含义 | 谁负责 |
|---|---|---|
| 聊天原文 | 用户说了什么、助手展示了什么 | 消息表保存 |
| 抽取字段 | 把自然语言整理为活动、时间、障碍等结构 | 模型提出候选，程序校验保存 |
| 证据 | 哪条真实消息支持这个字段或完成步骤 | 模型引用，程序核对原文、角色、顺序和归属 |
| 草稿完整 | 必需字段的类型、内容和关联符合规则 | 确定性程序检查 |
| 确认生效 | 用户同意的是已展示的这一版草稿 | 确认程序检查证据并写数据库 |
| 模块推进 | 当前会话的模块和周期状态发生变化 | 数据库事务提交，随后同步页面 |

因此，“助手说定好了”不等于保存；“字段齐了”不等于确认；“Router 建议跳转”也不等于已经跳转。没有一个模型回答可以直接替代这些数据库操作。

## 2. 从点击发送到看到新模块的完整顺序

1. **前端发送请求。** 请求带当前会话、用户输入及生成标识。后端校验身份和会话归属；生成控制器管理取消和并发。
2. **有限等待上一轮后台任务。** 如果上一轮抽取、路由尚未结束，先等待一段时间；代码默认等待上限为 2 秒，不是一直等到全部成功。超时本身不立即取消旧任务。
3. **恢复会话。** 从数据库恢复模块、活动目标、周期及历史，再与进程内会话缓存协调。当前缓存是进程内存，不应误称为 Redis。历史会裁剪，数据库消息和本周期证据读取承担持久历史作用。
4. **先保存用户消息。** 用户发言先获得真实消息 ID。以后证据、确认引用这个 ID，不能凭空制造一条“用户已同意”。
5. **尝试提前提交确认。** 普通 V2 会话处于 M2/M3，且用户本轮确认上一轮完整摘要时，先检查并提交确认，再生成下一模块回复。这个提前路径有适用条件，不覆盖所有模块和所有发言。
6. **读取图运行状态。** `extract_memory` 读缓存、模块进度和周期；这个名字里的 extract 不是本轮字段抽取模型。
7. **选本轮模块。** `analyze_intent` 主要根据显式指定模块或会话当前模块选择节点；它不是每轮让模型自由判断用户意图然后换模块。
8. **加载用户上下文。** 注册资料每轮读取；V2 临床/工作流上下文也按当前逻辑读取。旧版 Memos 不等于 V2 正在使用的记忆路径。
9. **风险分流。** 独立分类调用检查当前用户输入。触发则去危机分支；否则进入普通模块分支。详见第 9 节。
10. **普通分支先检查特殊情况。** 已结束/暂停会话、某些直接查询当前模块的请求、需要重新展示确认摘要的情况，可能由程序直接生成回复，不调用通常的主回复模型。
11. **按需要检索知识。** 先判断本轮是否需要，再查询目录、选择候选知识，经过知识中介筛选；失败则不用这批知识，继续回复。
12. **组装主回复提示词。** 不只是网页全局和模块提示词，还包括服务器规则、当前数据库状态、缺项、资料、记忆、知识和聊天历史。
13. **主模型生成。** 根据本轮策略开启或关闭 thinking；拆开正文与模型实际返回的推理字段，处理 JSON 回复外壳或思考标签。模型未返回推理内容时，不能凭空填充。
14. **程序检查候选回复。** 普通生产 V2 路径会先缓冲回复，读取权威状态并校验。通过才放行；不通过可能整段换成固定回复，或返回安全错误文本。
15. **发出正文与完成事件。** 回复完成并不代表这轮所有业务处理完成。
16. **保存助手消息。** 用独立数据库会话持久化最终正文、相关调试和执行信息。保存后才能可靠引用助手摘要的消息 ID。
17. **启动后台抽取与 Router。** 整理字段和原文证据、判断步骤、尝试创建目标、检验确认/流转条件，提交下一模块等状态。
18. **前端获得持久化信号并同步。** `persisted` 和模型生成 `done` 是不同信号；前端还通过状态同步和每 2 秒的兜底对账追上后台结果。刷新间隔不是后端完成时限。

一轮话可以经历风险、目录规划、目录筛选、知识中介、主回复、字段抽取、Router 等多次模型调用。缓存命中、跳过检索、固定回复、确认识别等分支会改变次数，不能认为每发一句只调用一次模型。

## 3. 数据库里的“已确认”究竟怎么来的

### 3.1 模型先提供结构化候选

抽取器通过 `provider.route_detailed()` 发起结构化抽取，提示词规定字段以及证据格式。它输出活动、时间、地点、时长、频率等，M1/M4 还输出里程碑证据，M2/M4 包括目标或活动相关证据。

M1/M2/M4 使用严格 JSON 解析；输出损坏或截断不会自动变成有效记录。抽取器的文本不是直接执行的 SQL。程序还要做类型检查、来源核验、草稿合并和契约校验。当前主回复和路由模型均配置为 qwen-max；抽取调用走 route 接口，不是直接把主回复正文当字段。

### 3.2 程序检查“有值”，不等于检查“用户同意”

M2 最小可执行计划检查包括：

| 字段 | 当前程序要求 |
|---|---|
| activity_content / schedule_text / location | 非空字符串 |
| duration_minutes | 1–1440 的整数 |
| frequency_rule | 结构对象，schema_version=1，text 非空 |
| potential_barriers | 非空字符串列表 |
| barrier_coping_plan | 非空列表，每项含 barrier 和 plan，且覆盖已有障碍 |

用户明确说暂无障碍，可以保存“明确暂无障碍/无需额外应对”；没有讨论过留下空列表则不能确认。障碍匹配有小范围归一化和睡眠、遗忘、天气、时间、精力等词族规则，不是无限制语义理解。

这意味着人看起来很完整的“一到两小时”，仍需抽取器形成程序支持的时长表示；程序不直接用人的直觉判定所有自然语言范围都合格。

### 3.3 程序还要求用户看过这一版

M2/M3 的确认标记会关联：会话 ID、周期 ID、助手消息 ID、草稿结构快照及 fingerprint（指纹）。指纹从确认相关结构字段计算；关键计划变化会使旧同意失效，时间戳等非实质信息不应代替计划变化。

有两类相关路径：结构化确认卡保存可核验的快照锚点；已有自然语言摘要的部分路径仍会检查摘要是否包含所需字段措辞。M3 记录办法仍存在规范化后的文本包含检查。因此当前实现已经有版本绑定，但不能概括为“彻底没有文字匹配”。

### 3.4 再识别本轮是否真的同意

常见确认先用明确肯定表达规则检查；不命中时，适用路径可以调用语义确认模型。修改、否定、条件性表达等不能随便当成同意。即使语义模型判定同意，也不能绕过草稿完整、摘要、版本、会话、周期和确认窗口检查。

### 3.5 最后由程序事务写入

检查通过后，确认函数执行数据库更新，而不是让模型自己把状态说成 confirmed：

- 通用确认记录保存 `record_status="confirmed"` 和 `confirmation_message_id`。
- M2 通过 `confirm_plan()` 确认计划版本、更新本周期 M2 步骤，并推进 M3。
- M3 保存记录约定的 confirmed/acceptance 状态、更新周期为 `waiting_execution`，推进 M4。
- 运行状态的版本号递增，同时留下带真实消息 ID 的决策审计证据。
- 事务提交成功后，内存与页面再跟进。异常或冲突不能当成已保存。

### 3.6 下一轮程序和模型怎么知道已经确认

`read_reply_workflow()` 查询会话运行状态，沿“当前目标 → 当前周期 → 当前计划版本”读取 `module_two_record.record_status`，据此计算 `plan_confirmed`。它还返回 current_module、goal_selected、flow_status、readiness、缺项和确认摘要。

这个对象一方面序列化进提示词供模型阅读，另一方面直接给回复校验器使用。因此有两条通道：**模型通过提示词知道数据库状态；程序通过查询结果硬判断。** 不是数据库自动通知模型，也不是模型记得说过“确认”就算数。

## 4. 什么叫“契约”，哪些会改变回复

这里的“契约”只是代码中对规则集合的命名，并非同一种技术。有些是提示词，有些是 Python 判断，有些同时用于提示词和程序。

| 契约/规则 | 做什么 | 对用户看到的回复有什么影响 |
|---|---|---|
| workflow_contract 步骤注册表 | 定义每模块合法步骤和完成口径，给 Router 和其他组件共用 | 决定哪些步骤算完成；间接决定一直问什么 |
| M1 证据契约 | 核对事件、关系认可、五项教育、理解和目标意愿 | 缺一个教育证据，就可能要求补讲；仅愿意定目标不够 |
| M4 证据契约 | 核对执行、ABC、教育、困难处理、最终决定和顺序 | 用户已说结束也可能仍被要求补前置步骤 |
| plan_contract | 检查 M2 字段形状和障碍覆盖 | 内容看似齐全但格式不合格，仍不能确认 |
| goal_contract | 区分活动建议、用户选定目标及对应证据 | 助手给出目标卡但用户选择证据不合格，可能创建失败 |
| workflow_readiness | 汇总缺字段、抽取新鲜度、摘要、版本与归属检查 | 形成“这轮还缺什么”，用于继续追问或阻止提交 |
| dialogue_confirmation | 把用户同意绑定已展示版本 | 决定这次“好的”是否真的推进 |
| program_confirmation | 统一确认和事务提交 | 决定真实状态何时改变 |
| 服务器模块运行规则/本轮执行规则 | 以 system 文本要求顺序、最早缺项、提问数量等 | 直接限制模型本轮能讲什么、先讲什么 |
| reply_workflow 动态规则 | 将数据库事实、缺项、下一动作加入提示词 | 强制回到缺项；限制宣称保存、锁定、结束 |
| 知识中介规则 | 筛选知识，保持来源边界，不允许知识冒充个人事实 | 主回复获得或失去某些建议和措辞依据 |
| answer_validator | 生成后做正则/文本和状态检查 | 可以将模型整段回答替换，网页提示词对此无能为力 |
| 风险分流规则 | 判断是否进入危机分支 | 改用另一套提示词并追加资源文本 |

**提示词里写“优先级最高”只是对模型的文字指令；Python 的拒绝提交和替换回复不受这句话取消。** 当前不能画成一个简单的“全局提示词高于一切”的优先级金字塔。

## 5. 主回复实际能看到什么

正常模块分支的输入大致包含：网页全局提示词、网页当前模块提示词、步骤说明、适用的目标/模块运行契约、当前模块声明、转换协议、本轮执行规则、注册资料、临床和工作流上下文、短期记忆、知识中介结果、数据库权威状态、聊天历史和本轮输入。

代码在前部使用有效全局/模块提示词，又在后部追加网页自定义模块和全局文本以加强影响。因此网页提示词有被使用，但同时会遇到服务器指令。后出现不等于模型绝对服从，重复也不等于逻辑冲突已解决。

本次核对的线上空上下文拼装，仅各模块系统段就约 1.75 万到 2.48 万字符；这不是 token 数，也还没有计入真实历史、资料和知识。普通回合不仅是在几行人格设定下自由聊天。

特别值得区分：

- **主回复模型**负责组织给用户看的语言。
- **抽取模型调用**负责从真实对话整理字段和引用。
- **Router 调用**提出模块与步骤判断。
- **风险调用**使用独立风险提示词。
- **知识目录和知识中介调用**分别选资料和整理使用建议。

网页主回复全局提示词不是上述所有调用统一使用的系统提示词。改它不能保证改变风险分类、抽取规则或 Router 的完成条件。

## 6. 知识库如何影响干预内容

当前线上启用 catalog 模式。查询使用本轮输入、最近少量用户输入和 PA 信息，而不是无条件把全历史交给搜索。

普通检索流程：判断是否需要 → 在当前模块/权限范围内获取目录 → 模型选择适用目录 → 获取候选片段 → 模型筛选结果 → 知识中介再次决定可用 ID、guidance 和 cautions → 注入主回复。

明确标题匹配和缓存可减少前面的调用。模块候选数量配置为 M1/M4 各 2，M2/M3 各 4；并不意味着最终都使用。知识中介最多处理 8 个候选、单片段限制 2200 字符，并有总预算；超时、错误、无效 ID 等会收回知识，不直接把原文全部塞给模型。

当前中介被关闭时返回空知识结果，不是“关闭筛选、原文直接通过”。这影响评估开关效果。

如果网页提示词没提评分而助手提了，候选来源可能包括历史、知识、代码提示或模型自行生成。必须查该条执行记录的实际分支和知识使用，不能只看网页提示词就断言来源。

## 7. 生成后的检查会怎样覆盖提示词

`answer_validator` 不是第二个模型审稿，而是程序规则。它检查空回复、过长内容、内部协议泄露、未知知识引用、部分诊断/用药/保证效果/羞辱表达、模块越界、未确认却宣称定好计划、要求去目标面板确认等。

它只获得部分输入，包括当前用户发言、模块、知识证据 ID 和权威工作流状态；并不拥有完整人的语义理解。一些匹配会排除否定、问句、条件句，但依然是有限规则。

| 检查结果 | 程序动作 |
|---|---|
| 通过 | 放行缓冲的模型回复 |
| 仅部分工作流声明违规 | 用 `truthful_workflow_reply()` 固定文本替换 |
| 其他可恢复违规 | 使用模块固定恢复回复 |
| 内部协议等完整性失败 | 安全文本/错误，阻止正常推进 |

替换不是让原模型“再自然地重写一次”。所以不同输入可能得到相同句子。被替换时还会清除原推理内容，避免展示一段对应不上最终回复的思考。部分仅工作流违规的 M2/M3 情况仍允许后台继续处理，其他 held/error 情况会冻结推进。

生产 V2 权威路径自身也要求检查；仅关通用 answer_validator 开关不一定能关闭这条路径。

## 8. M1 → M4 到底逐关检查什么

### M1：理解问题并进入目标讨论

依次核对具体经历（或明确低披露选择）、行为与状态关系及认可、BA 教育和理解、明确目标设定意愿。五项教育证据分别为：情绪/精力/行动相互影响；活动与反馈减少可能维持困扰；可控行动能带来新反馈；行动不保证开心；通过尝试观察调整而非要求意志力。

教育引用须来自助手原文，理解/意愿等来自用户原文，且有先后关系。允许同一消息中的不同子句分别证明，不要求五段话。用户纠正事实或新增实质教育会影响旧证据有效性。

这与网页提示词“不要求一次覆盖所有原理”不完全等价：不一定一次讲完，但服务器仍要求最终补齐对应证据，动态指令还可能要求本轮补齐遗漏主题。它会实际塑造对话节奏。

### M2：选活动、创建目标、确认计划

PA 理解 → 个人意义/意向 → 用户选择活动 → 可执行计划完整且确认。建议可以由助手提出；但助手提出不算用户选定。

创建目标与确认计划是不同操作：创建需要可核验的选择证据；计划确认还需要完整字段、展示、版本和用户同意。卡片文本不是目标表记录。`proposal_evidence_missing` 的准确含义是系统没有形成通过核验的选择证据，不足以推论用户从未表达选择。

### M3：约定记录

说明记录内容、用途与操作 → 商定可行记录办法 → 用户同意执行和记录约定。数据库最小检查含 record_requirement、feedback_mechanism、negotiated_record_plan。

确认后立即进入 **M4/waiting_execution**。无需已经实际执行；真实执行是随后复盘所需。旧 Router 文案中“执行后才到 M4”与这一口径冲突，代码追加的契约是在纠正它。

### M4：等待执行、核对事实、复盘及下一周期

先获取真实执行/未开始信息与可核验结果，按 A/B/C 情景分类；再核对 ABC；再做情境化 BA 教育并确认理解；处理真实困难；最后确认继续、调整、更换或结束/暂停。

继续原目标通常建立同目标新周期并留在 M4 等待执行；调整建立可编辑计划版本回 M2；更换解除当前焦点进入 M2；结束/暂停改变目标及会话状态。并非每一轮都机械回到 M1，也不是“我做完了”就算整个复盘完成。

`barriers_identified` 这个旧步骤键当前显示意义是“BA 教育与理解”，不能只按英文键猜业务功能。

## 9. 风险分支为什么会突然出现热线

风险门主要向独立分类器传当前输入，而非把普通分支完整网页全局、模块提示和对话上下文都传进去。返回可用风险状态就分流；分类异常则记录后继续普通路径。

危机节点使用 CRISIS_PROMPT、聊天历史和当前输入生成回复，再由程序追加固定资源文本。它不走普通模块本轮的 RAG/知识中介，也不走相同的普通回复校验链，因此不应把该条热线回复里的所有内容都归因于本轮知识库。

本次查到过“我已经走完了”被风险分类误解的记录。这说明普通上下文中的一句话可能在单独分类时被误判。网页全局写“普通反馈不是危机”不会自动进入这个独立分类器。

## 10. 后台如何让下一轮继续追问或跳转

助手消息持久化后，后台按模块读取对话证据并抽取。V2 部分路径会读取当前周期较完整的数据库对话，而不仅是内存最近消息。已有可核验确认卡时有减少重复抽取的路径。

随后 Router 得到当前模块、对话、最新双方发言、已完成步骤和 Router 提示，返回目标模块与步骤候选。合法方向有限制；最终还要经过服务器契约和数据库条件。

提交时核对任务是否仍是该会话最新有效任务，短时间持有会话锁，使用数据库事务写步骤、目标/周期、模块和诊断，再发布内存状态。新后台任务可以替换旧任务；这些保护旨在防止旧结果覆盖新状态，但不消除回复与业务状态的时间差。

后台失败会留下旧模块或缺项。下一轮 `read_reply_workflow()` 再读到同一个缺项，就再次把它写进提示词；模型继续追问，或者回复被校验器再次替换。这样形成“抽取/核验失败 → 缺项不变 → 提示词要求继续补 → 重复回复”的循环。

## 11. 页面上几个容易混淆的地方

- **我的目标**读取实际目标和关联记录，不从聊天气泡识别目标卡。所以“卡片已出现、列表为空”必须查目标是否创建及查询/刷新链路，不能只看卡片。
- **模块/进度**最终以持久工作流状态为依据，Router 思考里建议跳转不是最终结果。
- **回复深度思考**应展示主模型真实推理字段；没有字段就没有可展示内容。
- **路由深度思考**包含路由诊断信息与可用的模型推理，诊断文字不等于模型原生思考。
- **知识使用/参考内容**要区分检索到的候选、经过中介选择的片段和最终注入内容。
- **看到回复**不等于助手消息已保存，更不等于后台路由已提交。前端对保存失败有单独提示。

## 12. 如果要调整体验，应该定位到哪一层

| 想改变的现象 | 需要检查的位置 |
|---|---|
| 语言刻板、总列问题 | 网页提示词、服务器本轮规则、注入知识、是否实际被固定回复替换 |
| 愿意定目标还反复教育 | M1 五项证据、抽取的原文引用及动态缺项指令 |
| 目标卡有内容而列表为空 | 目标创建证据、创建事务、列表接口及同步 |
| 用户同意了仍反复确认 | 当前草稿、完整性、确认卡锚点、指纹、确认窗口、确认识别 |
| Router 说可以跳却没跳 | 服务器契约、readiness 原因及最终事务记录 |
| 总问已有内容 | 抽取失败或过期、证据来源不合格、历史范围、固定兜底 |
| 正常聊天突然热线 | 独立风险分类输入、返回结果和危机分支 |
| 网页提示词看起来失效 | 是否走普通生成、实际拼装文本、冲突服务器规则、生成后替换 |
| 深度思考框空白 | 调用参数、上游真实响应字段、被替换后是否清除 |
| 发完消息模块稍后才变 | 回复持久化、后台任务耗时、同步状态，而非只查 Router 文本 |

后续对任何一条异常，都应串起：用户消息 ID → 当前运行状态 → 实际调用/提示输入 → 原始模型回复与最终回复是否相同 → 抽取结果与证据 → readiness 原因 → 最终事务结果 → 前端同步。只查最后气泡，无法区分这些来源。

## 13. 代码阅读入口

- 请求/持久化/后台调度：`backend/app/routes/chat.py`
- 图顺序：`backend/app/graph/builder.py`
- 普通回复、危机、校验、后台路由：`backend/app/graph/nodes.py`
- 提示词拼装：`backend/app/prompts.py`
- 权威状态读取及固定兜底：`backend/app/reply_workflow.py`
- 回复规则检查：`backend/app/answer_validator.py`
- 抽取及风险提示：`backend/app/clinical_extraction.py`
- 步骤/模块证据：`workflow_contract.py`、`m1_contract.py`、`m4_contract.py`
- 字段及确认：`plan_contract.py`、`workflow_readiness.py`、`dialogue_confirmation.py`、`program_confirmation.py`
- 知识目录：`backend/app/catalog_knowledge_base.py`
- 页面流式接收和对账：`frontend/lib/api.ts`、`frontend/components/ConversationWorkspace.tsx`

配置备注：本次核对主回复和路由模型为 qwen-max；底层仍有 deepseek 兼容命名。配置中的主回复 temperature=0.3 没有由当前主回复调用传出，不能据此解释风格；路由调用实际传 temperature=0。本文没有将计划中的 Jev 接入当作现有能力。
