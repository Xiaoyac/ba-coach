# 关闭网页后接收提醒：实现与验证

## 本轮解决什么

原先已经是「服务器定时扫描 → 浏览器厂商推送 → Service Worker显示通知」，不是在网页中设置倒计时。但只有服务运行状态，无法让用户方便地验证关页接收。

本轮新增**一分钟后测试通知**与**浏览器显示回执**。测试由服务器保存并发送；关闭网页、刷新网页不取消安排，服务器重启后仍会处理。无需编造目标、改活动时间或等一个真实PA计划到期。

仍不能绕过设备权限、系统后台限制或网络不通。**关闭网页、退出账号、强制结束浏览器是三个不同动作：退出账号取消提醒；强制结束应用可能阻止后台接收。**

## 用户操作

1. 打开 https://bacoach.xyz/ ，进入「我的目标」或账号菜单里的「活动后提醒」。
2. 点击「在此浏览器开启提醒」，亲自允许系统通知。
3. 点击「一分钟后测试通知」。网页确认预约成功后，关闭本站所有标签页；iPhone离开主屏幕应用即可。不要退出登录或强行停止浏览器。
4. 约60–90秒后查看系统通知；网络／系统调度也可能继续延迟。
5. 点击通知或重新打开提醒设置，点击「查看测试结果」。每次测试窗口为到期后10分钟；超过窗口不会补发旧测试。

iPhone/iPad通常需iOS16.4+，先在Safari分享菜单中「添加到主屏幕」，从主屏幕图标打开再授权。微信内置浏览器不承诺支持；电脑可能需要允许浏览器后台运行。

## 如何判断结果

| 结果 | 代表什么 | 不代表什么 |
| --- | --- | --- |
| 已预约 | 数据库已有待发送任务 | 还没有发送 |
| 推送服务已接收 | 厂商HTTP 2xx | 设备已显示／用户已看到 |
| 浏览器报告显示成功，仍有本站窗口 | `showNotification`完成并回报，存在本站窗口 | 尚未验证关页场景 |
| 关页接收已验证 | 回报时接收前没有本站窗口，`showNotification`完成 | 不保证锁屏横幅可见或用户阅读；仍受系统通知中心／勿扰模式影响 |
| 没有回执 | 没获得足够证据 | 不能反推通知一定没有显示，回执本身也可能断网 |

回执是浏览器报告，不是可信硬件证明；不能把它当医学依从性、研究参与者阅读率或个人行动完成率。

## 实现方式

- `backend/app/pa_push_checks.py`：任务排队、发送、接收回执。沿用现有ECS与推送worker，没有新服务器或额外LLM。
- `pa_push_checks`：独立诊断表，只记录设备关联、到期时间、传输状态、回执时间与「是否有本站窗口」。不写入PA目标、活动反馈或每日记录；7天清理。
- `POST /api/push/checks`：只接受本人当前登录会话已授权的设备，没有自定义收件人或文案；每5分钟最多一次、24小时最多5次。
- 任务原子抢占后最多尝试一次；进程崩溃或网络结果不明不自动重复催促。发送前复核订阅、会话和档案拒绝提醒偏好。
- `GET /api/push/checks`：只能查看本人的最近5次测试，不返回endpoint、加密密钥或回执凭据。
- Service Worker独立运行，通过`clients.matchAll`判断有没有本站窗口。**先等待原生`showNotification`成功，再发送回执**；显示失败不谎报成功。
- 回执不携带登录token。每次发送生成随机256bit短期凭据，通过加密推送交给目标设备，数据库只存哈希。接口仅能更新这一次测试，校验到期、订阅和凭据；第一份回执不被重放修改。
- 回执POST使用固定同源地址、禁止重定向、`credentials: omit`，没有账户、聊天正文或窗口URL。
- 通知点击打开本站提醒设置的新窗口，不覆盖已有未保存的表单。安排测试前更新已安装的Service Worker，兼容上一版已订阅设备。
- 使用`frontend-design-codex`沿用暖色／黑金、统一按钮、44px触控与可滚动弹窗；没有改变原活动后15分钟提醒规则。

## 验证与真实限制

- 后端143项通过：原提醒、目标、每日记录、账号回归，以及延迟发送、重启后继续、并发不重复、退出／取消／过期、限频、跨账号隔离、不可伪造／不可重放回执。
- Service Worker独立测试通过：无窗口回执、有窗口回执、显示异常不回执、撤销后不展示、同源跳转与原提醒回归。
- 本地Chrome独立测试环境中：真正关闭本站页面，保留浏览器进程；DevTools注入模拟推送事件，真实Service Worker仍执行原生`showNotification`并回报`had_open_window=false`。
- 上述Chrome检查**绕过了真实厂商传输**，且headless环境的通知回读数量为0，因此仅证明关页后的代码路径，不证明操作系统横幅或真实设备送达。
- 提醒界面8组桌面／手机、暖色／黑金回归通过，新增预约、限频提示、查看结果与区分有／无窗口结果的检查。业务API和浏览器权限为合成数据，不向线上写测试账号或目标。
- 本地生产构建通过，主页面约71.2KB、首次加载178KB。目视检查1440px暖色与360px黑金，字体、对比度、按钮、换行与弹窗边界正常；长内容在弹窗内滚动。
- 当前会话缺少`playwright-interactive`要求的`js_repl`，使用项目Playwright脚本完成同等桌面／手机检查；没有声称用了不存在的工具。
- 服务器复测：Firefox、苹果推送入口可建立HTTPS；Google FCM仍无法连接。这不是多加网页定时器能修好的问题，也不能通过网页代码绕过系统权限。
- **截至编写此报告，还没有真实设备授权／收通知的最终验收证据。**要完成最后一步，需要用户在实际设备上开启订阅并运行一次测试；不得用模拟回执代替真机结果。

## 发布状态

已发布到 https://bacoach.xyz/ ，版本`20260917T100414Z`，沿用原ECS、生产环境与VAPID密钥。前端、后端、推送worker均active，worker重启次数为0，生产构建与`--check`通过。

最终线上资源回归：提醒设置8/8、每日记录9/9、目标时间线6/6，共23组通过。包括通知深链接直达设置、重复预约限频、厂商接收与浏览器回执区分，以及订阅410失效后仍能查看失败结果。均使用隔离浏览器和合成API；不算作真实厂商或真机送达验收。

生产库`ba_coach_260908`已先dry-run、再新增空表`pa_push_checks`。原两张推送表未改列，其他业务表未更改，本地测试数据未上传。发布包约482KB，复核无数据库、测试目录、密钥或知识库导入文件。

线上首页和Service Worker为200，SW维持no-cache/no-store；未登录的测试创建与历史查询均401。随机无效回执凭据返回404且没有写入记录。真实生产MySQL上的只读状态／历史／诊断查询通过。

验收时`pa_push_devices`、`pa_push_deliveries`、`pa_push_checks`均0条；没有替用户授权、创建虚假订阅或发送真实测试消息。

服务器尝试了DNS返回的4个FCM IPv4地址（保留域名与TLS校验）及IPv6，均未连接成功；苹果与Firefox仅证明入口可达，真实收通知仍待设备验证。没有新增代理、修改TLS校验、购买资源或依赖开发电脑持续开机。

如需回退，本轮上一版本是`20260917T094115Z`。先停止推送worker再切换`current`并重启服务；保留新增诊断表，不删除业务数据。旧版本不处理诊断队列，回退期间已预约的测试不会按时发送。
