# AI 回复气泡 · 2026-09-17

## 后续调整：生成状态外移（已上线）

- “正在准备回复”“正在深度思考／生成回复”以及等待超过 20 秒的耗时与“请勿重复提交”，改为气泡外的独立状态行。
- 没有回复正文时不显示空气泡；流式正文出现后，状态仍在气泡下方，计时不因正文出现而重置；完成或停止后移除状态。
- 输入框下方改为“内容由 AI 生成，仅供参考，不能替代专业建议。”；Enter 发送和 Shift + Enter 换行行为不变。
- 实现位于 `frontend/components/Chat.tsx`；回归脚本为 `infra/qa/stop-timing-0917.cjs`。
- `npm run build`、`npm run typecheck`、`git diff --check` 通过。以 `BUBBLE_QA=1`、`STATUS_QA=1` 运行浏览器脚本，1440/390/360 浅色、1440/390 黑金五组均通过。
- 测试使用模拟 SSE 和模拟时钟覆盖准备、超过 20 秒、流式正文、停止和完成状态，并验证复制、换行、草稿保留、停止失败重试与完成竞态；没有调用真实模型或写入生产资料。
- 截图保存在 `.test-tmp/generation-status-0917/`。已人工检查桌面与手机截图中的状态位置、空气泡、文字对比、边距、换行及输入框警语，未发现遮挡或页面横向溢出。当前工具无 `js_repl`，使用本地 Node Playwright 完成浏览器验收。
- 本节已随 `20260917T064202Z` 发布到 https://bacoach.xyz/。线上页面 HTTP 200，实际加载的 JS 已确认包含 `data-generation-status` 和完整 AI 警语；管理员每日记录接口未登录返回 401，生产查询、版本/用户筛选和 CSV 通过只读检查。
- 发布前与线上业务源文件逐项校验哈希，差异仅为 `frontend/components/Chat.tsx`。本次复用上一轮本地构建及五组浏览器验收，服务器重新构建成功；部署使用 `-SkipDatabaseTasks -SkipChecks`，不运行数据库迁移或知识导入。
- 发布包 191 个条目通过排除检查，不含本地数据库、测试数据、演示账号种子脚本、知识库目录或环境配置；没有同步本地测试资料。下方发布记录保留上一版气泡上线历史。

## 调整

AI 回复从无背景文字改成左侧圆角气泡，左上角收小，与右侧用户气泡形成方向区别。浅色采用柔和暖米色，暗色采用深蓝黑底并保留金色操作元素；两端底色不同，不加明显边框，沿用轻阴影。

本次依照 frontend-design-codex 保留既有页面布局与业务行为，不增加装饰图片或额外交互。只修改 `frontend/components/Chat.tsx` 的消息容器与 `frontend/app/globals.css` 的主题色值。

## 验证

- 前端类型检查、生产构建、`git diff --check` 通过。
- `BUBBLE_QA=1 node infra/qa/stop-timing-0917.cjs`：1440/390/360 浅色、1440/390 黑金五种组合通过。
- 覆盖短回复、长文和代码块、Markdown、复制、两种思考展开、计时更新、停止／续聊、草稿保留、停止失败重试及完成边界。
- 检查了气泡与背景分离、左右方向、文字对比、边距、圆角、手机换行与横向溢出；没有发现浏览器运行时异常。
- 气泡修改后的管理员页面也再次通过五种视口／主题组合的浏览器回归。
- 截图 `.test-tmp/assistant-bubble-0917/` 使用模拟对话，不是真实用户资料。

## 发布范围

**已上线：`20260917T062204Z`，https://bacoach.xyz/。** 生产页面 JS 已验证包含管理员入口、AI 气泡容器及样式类，后台服务正常，管理员接口未登录访问返回 401。生产 MySQL 列表、筛选与 CSV 再次通过只读验证，历史记录与备份保持一致。

管理员数据页首先随 `20260917T060729Z` 发布，生产查询与 CSV 检查通过。气泡作为随后的纯前端变更发布，沿用同一后端；全量后端测试在前一轮发布已通过，气泡版本重新检查了前端并执行浏览器回归。

本次没有数据库结构迁移，没有知识库导入，也没有上传本地测试资料。生产登录后完整交互此前未用真实管理员会话执行，部署检查使用公开接口权限拦截、服务器只读查询与前端产物校验。
