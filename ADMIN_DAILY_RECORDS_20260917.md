# 管理员每日记录数据页

## 状态与入口

**管理员数据页已部署线上，首次发布版本 `20260917T060729Z`。** 前一版本 `20260917T051029Z` 包含用户每日记录新版及聊天修复。本次仅发布代码，未执行数据库迁移或导入本地资料。

当前线上后续版本为 `20260917T062204Z`，还包含 AI 回复气泡样式，管理员功能不变并已再次通过生产只读验证。

生产验证：网站 HTTP 200，未登录访问列表和导出返回 401；实际 MySQL 列表、版本筛选、用户筛选和两种 CSV 生成通过。检查在服务器内存中执行，不保存导出文件、不输出用户正文、不创建账号或登录令牌；并非使用真实管理员会话在浏览器中导出。普通用户 403 由部署前的权限测试验证。

入口：管理员登录 → 右上角账号菜单 → **每日记录数据**。

用户已填写的资料直接从现有数据库读取，无需重新填写、不新增表。本页只读，不替用户改分、补分、删除记录。

## 设计

沿用暖浅色／黑金主题，按 frontend-design-codex 的工作台设计方式组织：

- 顶部筛选：账号、昵称#标签或用户编号、起止日期、量表版本。
- 数量概览：当前筛选的记录份数、用户数、新旧量表数量，只计已提交记录。
- 左侧列表：用户、日期、完成程度、活动量、心情、活动数量，每页 20 份。
- 右侧详情：身份、原始总体评分、全部活动时间与内容、心情、四项选填感受和备注。电脑随滚动保持可见；手机跳至详情并支持返回列表。
- 查询、重置、刷新、分页、两种导出均可用。筛选改动未查询时禁止导出，避免下载错范围。

## 导出

**每日汇总**：每份每日记录一行，适合按用户和日期统计。

**活动明细**：每项活动一行，附每日字段便于关联。每日评分会重复，不能按活动行重复累计，应按 `record_id` 去重或用每日汇总。

CSV 包含记录与用户编号、账号、显示身份、当地日期、时区、量表版本、分母、原始评分和备注。旧版人际联结与面对／回避字段也保留。UTF-8 BOM 兼容 Excel 中文。

- 新版总体 `/5`、旧版 `/10` 不折算、不混算。
- 空白选填导出为空，真实 0 分保留 0。
- 无计划为完成分数留空加 `completion_not_applicable=True`，不是 0 分。
- 危险公式开头的用户文本加单引号，避免 Excel 执行公式；这是导出层保护，数据库原文不变。
- 单次最多 1000 份记录，超过需缩小日期或用户范围，不静默截断。
- 日期筛选包含起止当天，按用户记录的当地日期。

## 安全与范围

后端接口均验证管理员角色：未登录 401，普通用户 403。不是只隐藏前端入口。普通用户自己的历史接口保持原有隔离。

不返回密码、邮箱、登录令牌等不相关资料。接口禁止缓存；CSV 不在服务端存为文件，导出日志只记操作者 ID、类型及数量，不写活动正文。下载后的文件仍需管理员妥善保存。

管理员可查看所有已提交记录，未新增分组授权。实际研究应确保知情同意与管理员授权覆盖数据使用范围。没有计算响应率，因为尚未定义应填写人群与观察时间窗；提交人数不能当作填写率。

## 文件与验证

- 后端：`backend/app/routes/admin_assessments.py`，在 `routes/__init__.py` 和 `main.py` 注册。
- 前端：`frontend/components/AdminDailyRecords.tsx`、`frontend/lib/adminAssessments.ts`；挂载到 `Chat.tsx` 与 `ConversationWorkspace.tsx`。复用历史记录组件与统一选单。
- 新增 15 项测试覆盖权限、筛选、分页统计、新旧版本、空值、导出上限、CSV 公式防护及普通用户隔离。
- 最终完整后端回归：**734 passed, 1 skipped, 1 warning，93.48 秒**。
- 前端类型检查、生产构建、`git diff --check` 通过。
- 浏览器 `infra/qa/admin-daily-0917.cjs`：1440/390/360 浅色与 1440/390 黑金五种组合通过，覆盖查询／重置／翻页／详情／下载／空结果／错误重试／退出／普通用户无入口。未发现横向溢出或运行时异常。
- 人工检查文字层级、对齐、筛选布局、按钮间距、移动详情和深浅色对比。截图 `.test-tmp/admin-daily-0917/` 全部使用模拟数据，不是真实用户资料。
- 当前未提供 js_repl，使用本地 Playwright 脚本完成验收，没有修改 Codex 配置。

## 部署说明

在已经完成每日记录结构迁移的当前生产库上，这是纯代码更新，不需再次改表或导入资料。继续禁用本地知识库与测试数据上传。上线后需用真实管理员会话核对列表入口，不应为验收在生产批量创建测试资料。
