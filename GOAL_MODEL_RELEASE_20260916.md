# 主要／次要目标、计划与执行周期改造

## 本次调整摘要

这次不再只处理“复盘后继续下一轮”，而是把目标分类、计划安排、日常执行、复盘决定和网页入口串联起来。主要／次要是目标类型，周期性／单次是安排类型，两者互不替代。次要目标独立存在，不需要父目标。

目标只能由 Agent 在对话中结合用户明确选择和可追溯原话建立。网页总览只读；新对话必须选择已有目标，或选择“和 Agent 讨论新目标”，没有强制填写目标名称的表单。

## 业务例子

| 用户情境 | 保存与流程 |
| --- | --- |
| 为恢复精力，每周五天站桩十分钟 | 主要目标关联长期方向；计划为周期性；每次实际执行另记活动 |
| 周末想去游泳一次，并明确决定尝试 | 可作为独立次要目标；计划为单次，不要求先找一个主要目标 |
| 站桩复盘后愿意按原计划继续 | 原目标、已确认计划及 M3 约定复用，新开复盘周期，不强迫重复 M2/M3 |
| 希望把十分钟改成五分钟 | 同一目标建立可编辑的新计划版本，旧计划保留 |
| 今天没站桩，但去散步了 | 未执行站桩与实际散步分开记；散步不算完成站桩 |
| 游泳可能不错 | 记为想法，不自动创建目标 |
| 暂时不想继续站桩 | 明确暂停；保留记录，新对话选择时明确恢复 |
| 想换一个目标 | 明确旧目标保留还是暂停，不替用户自动结束旧目标 |
| 刚才说错了，昨天没有散步 | 能唯一匹配的原事件标为已被更正，保留历史但不再送入当前有效上下文 |

一段聊天只有一个焦点目标；无法可靠归属的活动保持未归属，不猜测关联。活动发生频率和复盘频率分开，例如每周站桩五天、周日复盘一次。

## 实现方式

- `goal_contract.py` 定义提取契约、原话核对、目标分类、计划安排、活动归属、复盘动作与更正逻辑。
- M2/M4 每轮在路由判断前提取；模型给出的字段需通过会话所属用户、当前周期、最新回复和原话证据检查。旧轮次的迟到结果不能覆盖新周期。
- 全局构建提示词时补充不可被旧保存提示词遗漏的业务契约；保留现有可编辑提示词，不覆盖线上管理员内容。
- M4 的继续、调整、暂停、结束、替换保留、替换暂停分别处理。原复盘数字决策保留兼容，新增动作表明确含义。
- M2/M4 确认同时核对当前周期和最新消息的成功提取标记；空结果、失败或仅有活动旁路资料不能刷新旧计划。前端入口和直接确认接口都拒绝过期草稿。
- 目标总览支持分类、搜索、长期方向、生命周期、当前周期、安排类型和未归属活动；新聊天选择器支持明确恢复暂停目标。
- 删除对话清理新增关联资料；共享目标不会被某一段聊天删除连带清空。

## 数据库详情

仅新增四张表，不复制本地数据库，不回填或重分类历史用户数据。

| 表 | 主要字段 | 用途 |
| --- | --- | --- |
| `pa_goal_details` | goal_id、goal_kind、long_term_direction、source_message_id、evidence | 目标分类、长期方向和证据；无父目标字段 |
| `pa_plan_details` | plan_id、schedule_kind、review_cadence、difficulty、resources | 计划版本的安排类型、复盘节奏及支持条件 |
| `pa_activity_events` | user_id、可空 goal_id/cycle_id、event_kind、status、source_quote、superseded_by_message_id | 实际完成／未完成／想法，以及可审计的更正记录 |
| `pa_review_details` | review_id、action、source_message_id、source_quote | 用户明确选择的复盘后动作 |

旧目标缺乏分类证据时显示“待归类”，不能自动猜成主要或次要。历史待确认 M4 如果没有新契约所要求的动作证据，需要继续对话补齐后再确认。

迁移入口：`backend/scripts/migrate_goal_details_0914.py`。默认只读，要求明确目标库名；只有 `--apply` 才创建缺失表。MySQL DDL 会自动提交，部分成功后应核查并重跑，不声称可以事务回滚。

## 验收与限制

- 本轮完整后端回归：562 通过、1 跳过，80.67 秒；已有依赖弃用警告 1 条。随后 MySQL 编码适配的定向回归：4 通过（含新增编码适配测试）。
- 前端正式构建已通过；桌面 1440×1000、手机 390×844 使用完全拦截 API 的合成资料检查，不使用真实账号或数据库。
- 已检查分类筛选、搜索空结果及恢复、暂停目标选择请求、讨论新目标不创建目标、失败重试、手机滚动和阅读布局。
- 样式保持现有舒缓浅色及黑金主题；检查文字对比、换行、间距、按钮可达性和弹窗边界。
- 本地截图：`.test-tmp/goal-qa-20260916/desktop-light.jpg`、`desktop-dark.jpg`、`mobile-light.jpg`、`pause-panel.jpg`。该目录不在发布包内；验收结束已关闭本轮浏览器及 3102 预览进程。
- 浏览器合成数据验收不等于真实 LLM 语义准确率评测。当前没有足够标注样本，不能宣称提取准确率达到某个百分比。
- 原话匹配偏保守，模糊活动、更正、隐含决定可能需要用户继续澄清；优先避免错误建目标或错误归属。
- 本次没有实现日历打卡次数自动结算、定时提醒调度或跨聊天模糊事件合并，不把复盘周期当作这些功能。

## 后续监控建议

以下是建议持续测量的指标，并非已经测得的改善百分比：

1. 目标误创建率：抽查 `agent_goal_created` 审计与用户原话，检查想法是否被误当作决定。
2. 活动归属准确率：抽查已关联活动；同时观察未归属比例，避免只追求关联数量而错归目标。
3. 提取失败及旧证据拦截次数：结合 `clinical_extraction` 事件和步骤审计，区分模型失败、证据不足与用户正常更正。
4. 周期续接正确率：同一计划继续后应保持 goal_id、计划和约定不变，仅 cycle_id／轮次改变。
5. 暂停与恢复、替换保留／暂停的错误率：核对用户明确选择，不能把“这周休息”统计成永久结束。
6. 响应延迟：M4 现在每轮提取，模型调用的延迟和成本需单独观察；界面构建成功不能证明延迟改善。

建议先建立包含口语、省略、否定、更正、一次反馈多项活动的标注集，再比较改造前后准确率与 P50/P95 延迟。日志涉及心理健康与用户原话，只向授权成员开放，不公开上传。

## 发布记录

2026-09-16 发布前只读核实生产为 `20260914T111517Z`，前后端服务正常。生产 `ba_coach_260908` 原先没有四张新增表。

首次建表被 MySQL 3780 拒绝：库默认排序规则与既有外键父列不同。没有切换代码，也没有修改旧数据。迁移脚本已改为查询父列实际字符集及排序规则，仅为新表关联列使用相同编码；生产实测为 `utf8mb4 / utf8mb4_unicode_ci`。四张空表随后全部创建成功，未执行 INSERT、UPDATE、DELETE 或导入。

建表后只读复核：四表行数均为 0，所有 V2 预期表及新增列齐全。V2 启动路径强制关闭自动维护，只检查结构，不运行旧数据回填。发布包使用显式目录白名单，并额外排除数据库文件和 Python 缓存。

已于北京时间 2026-09-16 22:42 左右完成前后端切换，线上版本 `20260916T143404Z`，网站 https://bacoach.xyz/ 。

上线核对：

- 当前发布目录 `/opt/bacoach/releases/20260916T143404Z`。
- 前后端服务均 active，后端 `/health` 正常，公网首页 HTTP 200。
- 未登录访问目标总览接口 HTTP 401，权限边界仍生效。
- 目标契约、工作流、业务路由和三个目标 UI 组件共六个关键文件的 SHA-256 与本地一致。
- 四张新表在发布后检查时仍为 0 条记录，没有同步本地测试资料；之后用户真实使用会正常写入。
- 启动日志确认自动数据库维护关闭，读取原有知识索引 1331 个片段；没有执行知识导入。
- 重启期间旧服务等待长连接关闭达到 30 秒退出时限，随后新服务正常启动。此次不是零停机发布，切换期间可能短暂请求失败。

真实线上用户对话未用于验收，也未创建测试账号。完整业务规则由本地隔离测试覆盖，线上核对属于部署／权限／结构冒烟检查，不等于真实模型对话质量评测。

发布顺序：完整验收 → 明确库名创建四张空表 → 代码发布（跳过通用数据库任务与知识导入）→ 服务与公网核对。

回退：切回发布前版本并重启前后端。新增表保留，不通过删除新表回退，避免丢失上线后用户生成的记录；旧版本不会消费新增分类等资料。
